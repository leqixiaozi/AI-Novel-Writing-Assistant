# 参考工作流实施队列

授权只覆盖新版代码、真实操作合同、可审核迁移文件、隔离空库验证、文档、功能分支提交推送。禁止用户／开发库迁移与重置、seed、在线发布／安装定义、指定旧书写入、真实模型调用，以及含迁移或后台恢复的启动。不使用子 agents，不重做功能审计。

## 当前批次映射

| 旧版实际操作 | 新版页面／按钮 | 差距 | 验收步骤 |
| --- | --- | --- | --- |
| 角色外显页单个 AI 补全、指导输入、原内容／建议比较、保存到角色卡 | 本书故事设定→人物→外显；AI 补全外显资料、AI 调整外显资料；逐字段选择→载入编辑器检查→标准保存 | C1 代码与隔离合同完成，服务端未在线加载、用户未验收 | 已有人工外貌／0／false 保持默认不勾选；明确选一字段，检查并保存；核对真实来源与原请求 |
| 外显页批量补全、逐角色结果、写入批量结果 | 同页批量补全角色外显资料→逐字段勾选→确认→写入批量结果 | C1 原子批量保存已验证；尚未真实 AI／页面验收 | 第二角色保存失败整批回滚；同原键并发仅一批；丢失响应只读核对原批量结果；刷新失败保留凭证 |
| 旧固定外显字段 | 模板→人物外显字段→预览→明确发布；书内逐书安全新增 | 不默写已有旧书，布局和实际可用字段须在明确安装后由用户验收 | 保留已有样貌／语言／动作；新增体态、穿着、细节、声音、登场印象；不回填、不建立正式状态 |

## 后续顺序

下表为已授权的实施范围，未开始部分的旧页具体按钮和完整输入合同在进入该批次时只读核对，不当作已完成的等价声明。

| 阶段 | 旧版要求恢复的实际操作 | 新版入口与缺口 | 验收目标 |
| --- | --- | --- | --- |
| C2 | 资源账本、回填最近章节 | 人物→资源；缺正式持有关系投影、确切采用正文候选与既有结算接入 | 普通关联和图片不构成持有；未知非 0；正文版本／锚点来源固定，人工确认后原结算 |
| C3 | 人物小传→经历／时间候选 | 人物→时间线；缺有来源候选、review、实际时间命令接入 | 候选与实际发生分别保存；保留出处和时间原请求 |
| C4 | 公共角色→书内私有角色导入 | 创作资源→本书；缺字段映射预览与精确版本创建 | 不按重名合并、不带原书状态／认知／持有；额外字段缺映射须拒绝 |
| D | 公共世界安装、拉取／推送 | 世界→同步与资产；缺冻结包、三方差异、候选发布和原请求 | 可审核迁移不加入自动启动；只在隔离库激活和备份恢复；多书独立，扩展／事实／历史保持 |
| E | 方向→设定→规划→正文导航 | 四节点独立菜单；缺完整总览／进度／下一步／原位置返回 | book＋object 精确路由、未保存填写保护 |
| F | 自动推进、分步 AI、人工接管并继续 | 创建准备＋生产导演；缺统一引用政策与生成前来源预览 | 暂停／修改／继续用同一正本；固定引用版本与理由，规划显式引用优先，状态按章节截止 |
| G | 正文诊断→修复→比较→采用→完成 | 正文与结算／完成导出；缺完整人工＋AI＋恢复闭环 | 采用正文与人物／世界状态结算联动，完成检查与导出可核对 |
| H | 封面提示词、优化、进度、主封面替换／选择；书架搜索排序回收站阅读导出 | 书架／封面；保留已通过的统计与只读功能，补实际缺失操作 | 真实按钮、候选与主图明确选择、错误／未知恢复 |
| I | 题材、推进策略、公共人物、提示词、拆书／雷达／知识专用编辑试用引用 | 创作资源和研究；缺专用合同及结果带回创建／本书 | 实际专用输入／结果、固定来源版本，不据通用卡片宣称完成；修正读取触发研究恢复写入 |
| J | 简易创作与短篇完整流程 | 独立创作入口；缺专用端到端使用链 | 同新版正本，保留短剧／漫画独立菜单，不扩展其业务 |

## 生效与验收边界

B 代码已推送；C1 定向 55 项检查与双端编译通过。运行中的 5301 是无 watch 进程，B／C1 服务端尚未加载。5273 能读取最新前端，不代表对应服务端操作可用。阶段完成须分别说明提交、隔离验证、在线生效、真实模型和用户页面验收；不自动执行后二者，也不凭页面 HTTP 200 宣称操作验收通过。
