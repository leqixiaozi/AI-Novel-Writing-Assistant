# 知识索引正向 PG 验证

日期：2026-09-17。接在原批限定提交 `f8a1c3c6` 及[统一验证](current-batch-unified-validation.md)之后，属于原授权知识索引数据闭环的补验，不启动下一批或真实模型服务。首次阶段的 380／63 项通过与 72 条迁移仍为当时版本证据，不覆盖后续改动。

## 隔离与实际执行边界

仅使用已核实的 `ai-novel-new-design-test-20260917-unified`、本机 `55583`、独立 tmpfs 数据库。原开发 `55432` 不迁移、不重建、不清理；已有文件和凭据不复制、不读取。测试在本包 `.data/unified-validation/` 下创建唯一临时 appRoot，只复制原本包未修改的编译产物；原文件适配器按真实目录结构自然写入该根下的 `data/assets` 及 `data/ai-receipts`。测试生成文件保留，不删除用户目录或测试目录。

> 白话比喻：像把同一套机器搬到练习车间，机器按原流程收料、加工、留单，但不用正式仓库的货。对应验证：真实文件与数据库适配器执行，只有向量服务回复明确替换为测试 fixture，不修改生产路径，也不伪造数据库写入成功。

> 速记方法：文件真落地、账本真事务、回复标 fixture；通过只认实际断言。

## 本切片必要修复

静态交叉核对发现 067 的共享资产预览及挂载函数未固定 schema。`DECLARE assets%ROWTYPE` 在进入业务分支前编译，即使知识归档没有 visual 标记也可能失败。074 只固定三个原函数路径为 `pg_catalog, new_design, public, pg_temp`，不禁用触发器、不改业务条件、不更新历史来源。实际服务端编译通过；隔离库完整迁移 ID 集合 73/73 通过（跳过编号 064）。正式开发库未应用。

实际首次上传在原 `asset_content_objects` INSERT 报 `2201B invalid repetition count(s)`，原因是 027 定位符正则 `{0,999}` 超过 PostgreSQL ARE 重复上限。上传事务确实回滚，模型发送为 0，当时后续步骤未通过。075 仅把此重复改为 `*`，保留原独立 1..1000 长度、首字符／字符白名单及全部路径排除条件；不改已应用 027、不删除数据、不放宽路径边界。

全部余项编码及静态完成后的首次集中测试：服务端构建与两份新增用例语法检查通过；当时隔离完整迁移 74/74 通过。正向 10 项中 7 通过、3 失败（父项及最后两子项）。后两子项的新原 key 准备触发旧 chunk 的 no-op UPDATE 与原不可变触发器冲突。必要实现改为 DO NOTHING 后按精确唯一键读取并复核书、正文、hash、锚点、类型和状态；不修改触发器或旧分块，完成静态复核后集中复测。

分块复用修复后的集中窄复验：服务端构建通过，8 份相关 unit 共 62/62 通过；当时两份 PG 共 18 项中 16 通过、2 失败（父项与归档）。错误模型／维度已正确拒绝。归档原事件 INSERT 报 42702，静态定位为 061 桥的局部 `event_id` 与两条相关查询的 `impact.event_id` 列歧义。076 只区分局部变量身份并固定原函数路径，保留全部原事件、失效传播和派生状态处理；不重写 061 或历史事实，完成静态复核后集中复测。

## 正向实际结果

全部任务编码与逐项静态 review 完成后进入集中测试；测试失败必要修复也先静态复核再集中窄复测。最终结果：

- `npm run build:server` 通过，完整原迁移 ID 集合 **75/75** 通过（001..076，缺编号 064）；AGE 1.7.0、vector 0.8.6、pg_trgm 1.6。
- `knowledge-index-pipeline.postgres.test.cjs` **10/10 通过，0 fail／0 skip**：一个父项和九个真实流水线子项。
- 原 `knowledge-index.postgres.test.cjs` 8/8 在分块复用修复后通过；076 只修归档桥，未改其配置／规格／负向准备路径，复用此最近证据，不重复累计。
- 八份相关 unit **62/62 通过，0 fail／0 skip**；076 和后续文档不修改这些已覆盖的 JS 路径，复用最近证据。首次批次 380／63 仅为 `f8a1c3c6` 基线，不声称全部在补验提交重跑。
- 缺少隔离 preload 时，在文件和 Pool 访问前拒绝，独立安全断言通过。

精确原件和规范化解析版本／SHA-256、真实受控文件、原 key 查询、profile／connection／model／三维配置、完整分块正文发送均实际断言。同原 key 并发只有一次原尝试和一次受控发送，重复查询不重新调用。真实本地回复保存后在数据库 retain 处注入一次除零失败：数据库回滚，但原回复仍可读取；明确完成恢复录入原结果，重复完成不重复发送。

三个真实三维向量录入原 `embedding_vectors` 分代，数值匹配原 retained reply 的归一化结果；真实 HNSW 索引存在且 ready／valid，维度和 opclass 核对通过。原语义消费者返回精确本书原件、解析版本和 hash。仅证明真实距离查询与有效索引，不声称小样本查询计划实际选中了 HNSW。标准受控 fetcher 四次（三个分块、一次查询），另有两次专属错误模型／维度 fixture 回复；**在线模型调用为 0**。跨书、错版本、错 hash、错配置在调用前拒绝；两条错误回复不能进入 applied 结果。实际归档提交后，旧来源再次准备／领取／构建／语义查询均拒绝且无新发送，历史回复和 applied 结果保留。

实际集中命令（显式子进程环境 `AI_NOVEL_NEW_DESIGN_DEV_RUNTIME=1`；不是用户开发启动命令）：

```powershell
npm run build:server
node scripts/validate-isolated-postgres.cjs
node --require ./tests/unifiedPostgres/bootstrap.cjs --test --test-concurrency=1 tests/knowledge-index-pipeline.postgres.test.cjs
```

非敏感原始输出保留在忽略目录 `.data/unified-validation/knowledge-pipeline-archive-final.log` 与 `knowledge-units-postfix.log`；失败日志保留，不替换为通过数字，不提交生成文件。本文是可随 Git 同步的验收结论，临时库无备份承诺，容器停止后 tmpfs 会消失。

## 未验收项目

真实文本／嵌入模型、用户开发库升级、UI／主题／输入法／长文、打包发布及未接入图像生成继续未验收。测试向量回复只证明受控执行器、数据库持久化与查询合同，不证明真实模型质量、连接或费用。push 暂缓；首次发布历史包含既有非新设计路径和私有数据库备份类别，需明确决定及敏感数据边界核对，不读出备份内容。
