# 独立运行与失败恢复验证

验证日期：2026-09-16。当前受控分支 `codex/new-design-card-kernel`，仅修改新设计包，无新分支、无根业务或根锁文件改动。

> 白话比喻：店门能打开、收银能查账，不代表配送员已经送出真实订单。对应系统：独立页面／HTTP／数据库就绪，与真实模型创作通过是不同证据，必须分别记录。

> 速记方法：构建看产物，接口看数据，模型看实测，点击看用户验收。

## 集中验证结果

- 本完整功能连续完成受控资产、模型适配、独立入口、失败恢复界面和文档后集中检查。首轮发现 Zod 多类型联合的数组类型断言不成立；修复为明确前两项与剩余列表。追加发布回执核对时发现版本 DTO 没有类型身份字段；改为通过按类型读取的版本列表及精确发布指针核对，不虚构 DTO 字段。
- 最终 `npm --prefix new-design run typecheck` 服务与客户端通过。其服务编译产物同时供后续启动／单测使用；最终 `build:client` 通过。Vite 报告主包约 722 KB、压缩约 200 KB，存在超过 500 KB 的拆分建议，未隐藏告警；性能体验未实际浏览器验收。
- 集中七文件首轮28项通过；追加兼容接口可选字段请求用例及发布身份防错后，最终七文件29项集中通过：`type-editing.unit`、`type-issue-locations.unit`、`presentation.unit`、`controlled-prompts.unit`、`independent-ai.unit`、`standalone-boundary`、`independent-http.postgres`。
- HTTP／PostgreSQL 用例按已发布类型规格先静态确认修改应拒绝，再经真实 PATCH 得到 422 与 `fieldKey.<稳定键>`；校验前后类型修订、草稿字段和发布指针完全一致。最终29项集中复测包括这条扩充用例，不以旧结果代替新增代码验证。
- PostgreSQL 实读为 17.11，AGE 1.7.0、pgvector 0.8.6、pg_trgm 1.6，登记 51 条迁移。本切片没有新增迁移、复制数据或重置数据库。
- 自有 `dist/server/app/main.js` 实际启动 5301；只读 `independent-health` 返回数据库及已注册后台处理器 ready。构建后的内容类型深链接返回 200 自有应用文档。
- 5174／5175 被其他进程占用，未终止该进程。自有 Vite 用命令行端口 5194 实际启动，模型设置深链接返回 200 自有入口，同源代理返回 5301 的健康状态；默认配置仍为 5174，不自动接管他人端口。

## 证据边界与剩余事项

- 专用配置 `NEW_DESIGN_AI_MODEL` 未指定。真实 `/models/status` 返回未配置，`/models/probe` 返回 422、失败步骤“配置模型”及“打开模型设置”来源。未借旧模型配置或自动选择模型，真实模型生成未执行。
- 受控传输用例使用测试回复，只证明请求合同、白名单、解析／校验步骤、无盲目重试及脱敏；不是模型能力或内容质量证明。远程兼容接口尚未做真实服务验证。
- 模型页面当前为开发环境专用配置只读检查，不具备完整持久化默认／备用／任务路由中心；后续复用现有路由、凭据引用和快照，不另造事实表。
- 内容类型错误定位、跳转、键盘聚焦、错误恢复按钮、七主题、输入法、窄屏与实际体验未做浏览器测试，交由用户验收。
- 错误通知在自有独立壳提供可点击设置入口；旧 5173 过渡壳不算独立运行证明，不能宣称所有旧页面都已统一恢复。
- 运行记录仍只读。人工草稿保留只限当前页面内存，不承诺刷新／关闭／跨设备后的未保存草稿恢复；已保存 PostgreSQL 事实保持原有跨机器备份／恢复路径。
