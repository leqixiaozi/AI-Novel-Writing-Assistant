# 稳定章节补充的历史依据

## 来源页与未知结果恢复

人物资源页负责明确维度、章节和完整资源范围；稳定章补充及既有冲突分别创建独立清单，然后返回既有章节结果确认表单。AI候选必须带冻结的完整资源范围，沿专属受管资产执行；人工确认和正式保存分开，影响确认不会解除冲突。实际冲突入口在人物资源与原章节，运行记录保持只读。

独立创建、影响确认、正式保存各自先持久完整原命令，再发写请求。来源页只读原结果匹配书籍、会话和完整输入，不按原键单独判断。COMMIT响应丢失时锁定编辑和重复写入，保留完整凭证；重开原来源页即使会话已稳定，也必须保留原正式保存核对入口。正式保存面板保持同一挂载位置，避免刷新父页面为稳定态时提前卸载而丢失原请求收尾。成功原结果读取不再调用模型或提交修正，刷新失败不清凭证。

隔离浏览器已实际验证补充、冲突修正及真实COMMIT后响应丢失的新页面恢复。临时HTTP直接挂载完整生产路由；只替换受管provider请求，原上下文、任务、用量、候选、确认和正式事务均走实际模块。作者库安装、现有服务加载、真实模型和用户验收仍分别报告；下面阶段性关闭说明保留为历史，不代表已在线开放。

实际修正正式提交（手动094）：新合同`resource_supplement_correction_commit_v1`与普通v1原输入／原键／原回执解释分开。完整作者影响确认保留新修正原来源、原章末全部确认、原错误0→2与实际章前1。物理事务合并原全确认+作者明确确认的新1→2、只排队新增状态，建立真实下游日志，再为每个确切关联issue写入完整不可变证明，重建真实章序投影并保存含所有解除原行的完整正式原回执；原正文／稳定会话／结算及全部确认不重开或复制。

解除证明包含完整原修正及作者影响回执、完整正式输入、实际新提案／状态／正文／锚点／合并检查点／结算及原完整章前来源。实际SQL独立重读并验证规范全文哈希，最终087延迟守卫再核验真实prefix、完整解除覆盖、实际下游全部来源、语义依赖与投影。普通正式命令不能解释修正；回执插入失败、重算哈希的假来源和回执后实际prefix变化均回滚全部新状态／解除／投影。真正COMMIT后的ACK丢失保持unknown，完整原键找回唯一原结果；并发重复只取原结果，不同完整输入unknown。

隔离验证实际解除后，原0→2仍在完整确认清单，新的1→2成为本章有效记录，投影健康且指向新记录；正常下一章上下文读完整合并确认，连续性读取含新有效来源。停用094只关闭新证明并恢复普通正式证明，保留原历史及健康投影，归档后完整正式／修正创建原结果仍可读取。094不注册普通启动、能力标志false，没有用户库安装／在线加载／真实模型／HTTP/UI验收；C2整体尚需源页接通及端到端验证，下列较早阶段的关闭状态保持为历史记录。

独立修正候选与人工核对（手动093）：从091不可变原输入和完整原来源读取，保留原章末全部确认、原0→2记录和实际章前1证明；仅派生该真实issue的唯一字段作为可操作目录，原完整资源范围仍进入受控输入。实际已发布字段／字典及人物／资源／关系版本变化拒绝；实际SQL独立重读最新章前提案、正文SHA／证据／合并确认与完整关联冲突，拒绝缓存来源失效后的新领取，不退回更早前值。专属注册提示词走既有受控任务／路由／尝试／用量及原结果导入，输入新标签与普通v1互斥，正文原计划保持。

人工创建、编辑、明确确认沿用正常作者流程，仅修正的该真实冲突字段允许正文证据支持的1→1与delta=0；其他字段和普通v1不放宽。停用093关闭新领取／人工写入，已完成模型原结果和人工原回执仍按完整原键读取，源投影继续过期、没有解除行。正式修正预览／合并仍503，090解除证明仍关闭。093不注册启动，本段为隔离验证合同，无用户库安装、在线加载、真实模型调用或UI验收，不据此记C2完成。

稳定正文已形成正式事实、读者／人物认知和状态变化，补充遗漏的资源变化不能把原稳定会话重开或重新采用相同正文。重新采用会触发原来源失效链；复制相同正文会产生另一个版本，仍不能代表原确认记录。独立补充应引用原检查点和确切采用正文，并最终把原确认来源与新增确认一起提供给下一章。

另一个风险是前值的时间范围。正常可编辑章使用当前正式投影；补充早期稳定章使用所选章结束时的正式状态，包含该章已经确认的变化。否则已有“0→1”的获取会再次被当作遗漏重复结算，或后续“1→2”泄漏成为早期章前值。

`database/resourceSupplements`的基础读取核对完整原检查点清单，逐类核对事实正文证据、认知提案版本和状态结算及提案。事实／认知／状态不互相替代。冻结正文对应的原计划版本，后来编辑计划不能替换它。清单、证据或正式来源失效时拒绝生成补充依据，原记录保留。

历史状态按实际书籍、对象、状态键和章末范围读取有效状态变化，校验结算、提案、当前采用正文和有效证据。明确的故事顺序优先，没有独立故事顺序时使用该变化实际所属正文的章节顺序。只有在原检查点形成之前已经存在的不可变初始版本才能作为回退；后来新建或修改的初始状态不能反向提供旧章前值。没有来源时保持未知，不返回0或false。

所有读取由调用方的同一个可重复读事务负责，仅SELECT，不生成候选、不写状态、不恢复任务。本基础合同不开放稳定补充按钮。完整链还需要独立补充会话／完整原请求、同事务保留原结算并合并确认清单、冻结实际下游来源和逐项复核。源页必须明确展示这些影响及恢复位置，不能用新的普通可编辑会话绕开它们。

## 独立会话的存储边界

手动087在准备、会话及结算中增加原检查点引用，补充会话复用原采用记录，另有完整输入／冻结来源／原回执不可变表。原稳定会话保持终态，原committed结算保持，正文指针不移动；普通采用与切换的唯一约束保留。原计划后来被编辑时，独立补充准备仍读取原正文的冻结计划。

资源候选只能属于冻结范围中的道具或持有关系，不能复制原事实／认知作为新候选。新补充提交必须原结算仍有效、同事务将原检查点标记superseded并创建同正文的继承检查点。合并清单必须精确等于原确认集合与本次正常确认来源的集合，不能丢项、重复或添加无来源ID。

实际下游复核尚未实现，087在上述保护后仍无条件拒绝正式补充提交；operational字段约束为false，普通编辑／初始状态命令与受控AI领取也按实际补充类型拒绝，用户没有开放入口。这是存储基础，不能据它宣布稳定资源回填、自动批量或UI等价完成。完整下游闭环需后续明确手动合同，不能通过翻标志绕过。

## 安装及停用

普通启动不登记或执行087，不在现有开发／用户库执行。隔离验证从template0创建新空数据库后应用真实注册迁移与087，保留生成的数据和完整守卫。正式安装须另有明确范围、可用备份路径及新空库恢复核对后，由维护人员在一个明确事务中应用；不能为安装启动会自动迁移／恢复后台任务的服务。

停用文件只将能力保持关闭，保留原输入、回执、会话、结算及兼容索引和守卫。补充会话复用原采用记录，存在历史后不能恢复旧adoption_id全表唯一约束；也不能DROP补充表或列以假装回退。备份恢复只应在新空目标库验证，原库保留；本阶段没有用户库安装或恢复执行。

## 原请求与查看合同

资源预览冻结当前明确选取的人物、道具、持有关系版本、确切采用正文锚点及所选章末正式前值，并采用正常字段／字典规格。完整源帧使用实际持久JSON计算哈希，不能只比较正文或原检查点哈希。后续章节已有状态不会改变旧章源帧。

内部创建命令在SERIALIZABLE快照前取得原请求锁和书级锁，先核对完整原输入的原结果，再重新核对源帧；同键不同内容不能覆盖。独立准备／会话和完整回执必须同事务，失败不可留下半个子会话。真实提交已成功但响应丢失时只读原回执；原查询中断不等于没有旧结果。归档不重建或隐藏原回执。创建仅建立待处理来源，不移动正文指针、不重写正式事实／认知／状态。

只读结算视图由结算模块拥有，校验原源帧、原输入哈希、原回执及实际会话／正文身份后显示冻结章末清单。没有完整源帧就拒绝，不能按最新状态补齐旧回执。普通会话不受影响。补充模型领取的SQL守卫在普通代码门禁之外仍关闭，保护运行旧缓存代码的调用者；完整业务尚需专属注册提示词、受控候选、合并结算、真实下游完整性复核以及源页入口。

## 专属历史来源提示词

`new_design.character.stable_resource_supplement@v1` 已注册，使用创建时完整源帧，核验正文SHA、源帧／基础哈希、旧计划版本及三类原确认清单。模型资料明确包含原计划与原确认来源；候选仅限显式资源及持有关系，拒绝未知／过期前值、无变化、重复确认变化及越界类别。证据和字典规则沿用正常受控结构化合同。

`prepareStableResourceSupplementPrompt` 仅在独立只读事务读取实际待处理子会话及原帧；原正文仍须为当前采用版本。它不领取任务、不发布配方、不调用模型、不写候选；通用生成入口的类型范围明确排除该任务。087的SQL领取守卫和普通编辑／结算门禁仍关闭。专属提示词完成不代表受控生成、人工确认、合并结算、下游复核或页面可用。

## 候选受控流程（仅手动088）

088不注册普通启动迁移，要求085与087，保留已有触发器／函数身份。它只允许稳定章专属资产的精确原来源领取：源帧、历史清单、人物／资源／关系版本、原正文锚点及完整合同均核对，普通旧提取不能领取子会话。既有受控路由、配方、预算、上下文、任务／步骤／尝试、模型输出和原请求回执完整复用；上下文另外固定实际原正文计划版本，不跟随后来采用的新计划。

正常人工候选创建／编辑／确认及模型结果导入经候选合同门禁后使用历史章末清单。最新状态可变化，原字段／字典／资料版本不得替换；原事实、认知、状态和计划内容若失效则拒绝。原计划后来成为历史版本是合法状态变化，不改变原正文依据。重复变化／无变化仍拒绝。读取原编辑回执失败、同键不同内容和提交ACK丢失保持unknown，不能根据确认回滚推断没有历史结果。完整原结果先于当前门禁读取。

**正式结算与回溯建立初始状态仍无条件关闭**，087的合并／下游守卫未变，operational仍false。只有候选和确认决定，不产生正式资源变化，不重开原会话，不复制正文或改写后续章节。未挂新HTTP/UI入口，未用户库安装、在线加载或真实模型验证。088停用脚本仅重新关闭领取／候选门禁，保留原输入、输出、候选、尝试和回执；不得删除记录或恢复不兼容旧索引。合并结算、真实下游复核及资源页入口仍待完成。

## 实际下游影响预览

`previewResourceSupplementSettlement` 是未挂HTTP/UI的内部只读方法。正常结算模块先验证实际补充会话、全部确认／拒绝决定、领域提案、冻结编辑合同、历史前值及原确认来源。按实际后续采用正文读取所选字段的已确认账本，核对原提案、已提交结算、当前稳定检查点及证据。不能用丢失／失效来源的空清单继续。

从本次真实补充后值顺序核对后续章实际before值；章内同字段保留全部来源，以最后正式记录作为有效链。冲突与倒置故事位置明确保留原因；不猜测未知时间，不自动修改后续记录。原0→2在补充0→1后应显示预期前值1／原前值0，不能凭勾选复核变成1→2。

完整原输入、实际下游正文／旧正文计划／当前采用计划／检查点／状态来源和确切计划引用共同计算影响哈希。后来采用的新计划改变哈希而保留旧正文计划；重复预览不写标志、不增加结算／正文／状态／AI记录。仅预览，尚无合并结算、复核日志、有效源阻断／解除或页面入口，不能记为真实下游复核完成；正式补充保护仍关闭。

## 合并后的来源读取准备

下一章稳定上下文按检查点明确确认的状态ID读取实际已提交来源，验证本书／本章／当前正文／原确认提案／证据。全量记录保留，章末有效值取同字段最后正式记录；不能按当前单次结算过滤而丢掉原结算状态。正文连续性也按当前稳定检查点的完整确认清单判断原状态是否保留，保持原state_change及其原settlement_id，不能把旧状态改指向新结算。

此阶段只调整读取：正常真实SQL来源与缺失清单拒绝已验证，合并来源的连续性条件以当前编译代码的结构化请求桩验证。未发布实际合并检查点，不能将请求桩记为合并SQL结算完成；正式结算仍关闭。接下来须在独立空库验证完整合并、实际下游标志与复核解除后才可开放入口。

## 影响确认原记录（手动089）

`confirmResourceSupplementSettlementImpact`在独立SERIALIZABLE事务中先读取完整原请求，再核对当前候选及完整实际下游影响哈希；来源改变则拒绝沿用旧确认。同键不同输入、原查询中断和提交ACK丢失保留unknown。原请求与书级锁先于快照，原结果读取使用同一请求锁，不能将尚未完成的原提交解释成无记录。确认记录、完整输入、完整下游快照及来源页回执同事务保存，不更新会话版本、正文、状态、投影或AI记录。

089仅是未注册启动的手动确认存储，要求088。数据库守卫核对当前子会话／原检查点、全部实际确认候选、真实后续正文／旧计划／当前计划／确切引用与完整状态来源，并重新推导状态链；重算哈希不能把冲突改成兼容或省略后续来源。原确认不可更新或删除。应用与数据库会话时间戳以应用现有毫秒精度比较，其余持久身份／版本仍完整核对。停用仅关闭写入，保留全部来源及回执，归档或当前来源变化不会重新解释原结果。

作者必须明确确认全部实际冲突ID；这只记录已知影响，不代表修复、解除标志或同意伪造前值。未来正式事务消费`readResourceSupplementImpactReviewForSettlementInTransaction`时还须在同事务重新取得完整真实影响并核对，不能只凭reviewId或说明放行。087的正式关闭守卫及operational=false没有改动，尚无实际合并、有效源阻断／实际解除或HTTP/UI入口；确认原记录不是下游复核业务完成。

## 合并写入基础与发布保护

`chapterSettlement/supplementWrite`是caller-owned SERIALIZABLE事务内的基础写入方法，没有独立COMMIT、HTTP或产品命令。它重核真实候选、完整影响及确认原记录，使用历史章末前值确认本次状态，不按最新全书投影替换前值。只保留原确认ID并新增本次实际状态ID；原会话／结算／采用／正文不变，原检查点只改变status，新检查点previous指向原检查点。只登记新增状态依赖及语义来源，不重新登记或复制原事实／认知／状态。

`getNextChapterStableContextInTransaction`供一致事务内读取实际下一章来源，普通只读方法仍拥有自己的只读事务。真实SQL隔离验证已在未提交事务中创建合并检查点，并核对全部原确认、原行不变及下一章上下文；随后强制执行原延迟守卫，明确收到downstream closure is not operational，ROLLBACK后原检查点恢复、新结算／检查点／状态不存在。没有修改、删除或停用087守卫，也未发布可提交合并。

基础方法没有投影闭环、实际下游日志／源阻断／修正解除及完整正式提交原回执；必须由后续完整命令在同一事务补齐，不能调用该方法后绕过关闭守卫，不能记为稳定资源补充业务完成。尚需处理有效故事位置与章序共同决定投影的来源顺序，不能让后写入的早期章补充覆盖后续实际章末状态。

投影重建已按实际采用正文／已提交结算／确认提案筛选来源，顺序使用coalesce(effective_story_order,chapter.logical_order)、章序及同位置sequence。后写入的早期章资源补充不能凭大sequence覆盖后续已确认状态；存在明确故事位置的记录也不能只因其他记录位置为null就无条件优先。无章节来源的未知值不按流水号推断时序。实际合并事务隔离验证中第一章补充0→1后，当前投影仍保留第二章0→2的实际来源ID及值2；之后关闭守卫仍阻止发布，全部回滚。该来源顺序修正不等于已阻断下游冲突，仍缺实际冲突标志与修正解除。

## 真实完整性日志及有效源阻断（手动090）

090不注册启动，要求089，只增加事务基础。日志绑定实际新增结算／合并检查点／完整影响确认；实际冲突行绑定原后续正文／状态／字段及完整原链条。行均不可更新或删除。每条冲突插入必须找到对应实际投影，并同事务标为is_stale；缺投影会拒绝写入，不能发布没有阻断的标志。

投影守卫按本书具体字段的未解除真实冲突校验。重建、置is_stale=false、删除或移动字段身份都不能绕过；停用脚本仅拒绝新增日志，保留实际历史与既有源阻断。章节连续性在解析状态之前检查过期，拒绝用缓存状态生成。它是实际来源完整性拒绝，不把普通章节质量债务转成全书暂停／replan。

解除没有可修改的resolved标志，只能由未来实际来源校验的不可变证明行代表。当前证明插入守卫无条件关闭，作者勾选知悉或伪造证明均不能清除冲突。087正式合并关闭守卫未改；真实SQL验证在合并事务内建立日志／冲突／阻断、验证连续性拒绝及各种绕过被阻止，然后由087拒绝发布并全部回滚。没有已提交／在线冲突流，不能据此宣布修正解除或资源回填业务完成。

`readResourceSupplementCorrectionBasisInTransaction`是明确issue-owned章前读取，独立于正常章末v1。它保留受影响章原完整确认及原0→2，同时读取当前实际前章的补充后值1，以新合同固定1→2修正所需来源。原flag期望值保留，不根据新的prefix改写原记录。选取最新实际行后核验原提案／结算／采用正文SHA／当前合并确认清单／证据；失效时拒绝，不能过滤掉最新坏来源再取旧行、章末值2或后来建立的初始值。全文源帧及明确flag／正文共同哈希。该读取不创建修正候选、不修改状态、不解除标志；后续须绑定独立修正模式、受控候选、人工确认和实际合并证明。

版本查询使用不与version列同名的表别名，避免to_jsonb(version)解析为整数版本号；初始／认知／关系版本及计划引用保留实际完整行。计划引用保留数据库原生JSON时间戳，与SQL守卫保持一致，不用应用Date格式悄悄替换。原已保存源帧不动态改写；缺失完整来源或与当前来源合同不符时拒绝续写，原输入和原回执保留。实际空库另验证正常初始0／false及已确认认知的完整版本可被连续性读取。

普通章末补充预览及候选操作还须核对selected subject-kind／ID的真实冲突，且只拒绝影响位置不晚于所选章的开放冲突。不能通过重读仍active／confirmed的原2把is_stale清成false；更早健康prefix与无关对象／类型保留可读。090的首个SQL领取守卫独立拒绝新普通领取，保护旧缓存代码；原请求读取／已有结果保留。实际验证中先创建第二章正常待处理子会话，再在第一章合并事务内产生第二章真实冲突，该子会话的候选门禁及独立SQL新领取均被拒绝。修正所需before=1必须使用新issue-owned合同，不能让普通v1子会话绕过来源完整性。正式发布与实际修正解除仍关闭。

`previewResourceSupplementCorrectionInTransaction`另有严格的`stable_resource_correction_preview_v1`合同，输入仅为真实issue与完整资源范围，不接收任意章前值或忽略冲突选项。读取章前完整实际证明及原章末确认，然后按正常已发布字段／字典／资源／关系／锚点冻结清单，只有明确冲突字段改用已证明的章前1，其余字段保持实际章末合同。所选范围的其他开放冲突不能忽略，同字段同正文的关联冲突全部固定到来源哈希；超过5000拒绝而非截断。若章前来源所在位置还有更早同对象冲突则拒绝，不拿尚未修正的prefix继续。

该专属预览保留原0→2及原章末2，可供后续修正模式比较；不能传入普通v1创建／候选流程，预览本身不创建会话或原提交回执，不调用模型或解除标志。只读比较可在旧待处理清单保留时提供依据，实际创建必须明确处理原活跃清单并核对未知结果，不能自动取消／改写旧输入。实际SQL验证清单前值1、原完整确认保持、关系版本／锚点固定及同源重复哈希稳定，最终由087拒绝发布并整体回滚；业务闭环仍未完成。

## 明确冲突的独立原请求（手动091）

修正和普通章末补充使用不同输入／回执合同。`stable_resource_correction_start_v1`固定实际检查点、明确issue、完整资源范围及完整来源哈希，没有任意前值、忽略冲突或通用允许绕过选项。事务写入从当前实际预览重新核对正文、资源规格、章前证据及全部同字段同正文关联冲突，已有待处理清单时拒绝；不得自动取消、改写或复用旧清单。本阶段的隔离测试只在可回滚fixture中模拟明确取消空待处理会话的合法状态转换，生产写入方法没有取消逻辑。

独立准备和会话复用原采用记录及正文的原计划来源，原稳定会话／结算／正文和全部确认不变。普通087原来源核验保持原函数及触发器身份；091另存不可变原证明，重读实际最新章前行及完整提案／结算／采用正文SHA／证据／当前合并清单，拒绝从章末值或过滤坏最新来源取得前值。完整输入和来源的规范文本必须分别匹配JSON和实际哈希，所选全部实际关联冲突必须逐项一致；延迟守卫拒绝缺少原证明的会话。实际SQL守卫独立拒绝已重算哈希的章末替代值、伪造关联冲突和遗漏原证明。

原读取先于当前资料及写门禁，匹配原完整输入、原来源、全部关联issue和原回执；输入不同或读取中断均为unknown，不能当作没有写入授权替代请求。停用只拒绝新增原证明，原全输入记录仍可读取；投影过期和既有源阻断不变。来源页回执包含session及resourceIssue，但页面尚未消费这套修正协议，不能据路由存在宣布操作入口完成。

091不注册普通启动，不修改087原文件或090解除证明守卫；独立候选领取及人工编辑SQL守卫仍无条件关闭。`startResourceSupplementCorrection`已经拥有物理创建事务，按原键及同书清单锁定后先读取原完整回执，在当前实际冲突／章前源重新核对的前提下保存新独立请求。实际COMMIT确认丢失保持unknown，随后以同完整输入读取唯一原结果；不同完整输入不能覆盖。新空库已在实际提交的第一章补充和第二章真实冲突上验证独立修正创建及ACK恢复，不需要将用户库或关闭功能在线化；专属候选／人工确认、实际修正合并证明解除和页面仍缺。

## 普通补充的正式事务闭环（手动092）

普通章末补充的正式命令将新鲜完整影响／作者确认、实际合并原全部确认+新资源状态、真实投影重建、下游不可变日志／真实冲突／源阻断、依赖／语义来源登记及完整正式原回执放进同一个串行化物理事务。原会话／结算／采用正文及原确认均不重开或复制。仅新资源状态加入语义排队；已确认的事实、认知和状态不重复登记。本阶段验证包含实际活动语义配置：仅新增状态建立匹配实际依赖哈希的待处理来源／排队，没有运行worker、生成块或调用模型。

092明确替代087守卫的最后关闭分支，使用CREATE OR REPLACE保持原函数及触发器OID和原全部确认守卫；没有删除／禁用保护或翻operational标志。完整原回执与实际合并日志、原创建回执、完整影响确认及所有真实issue逐项一致。SQL插入校验和最终延迟校验都重读实际历史章末前值、后续所有实际正文／当前及原正文计划／确切引用／完整状态来源，再由新增实际后值推导状态链。每条真实冲突必须有不可变来源行及投影阻断，投影值／来源ID必须按实际章序选择，所需依赖及语义排队不能缺失；缺同事务完整正式回执直接拒绝。回执插入后再修改真实下游版本也会在COMMIT时拒绝并整体回滚，不能用较早的一次预览替代最终实际核验。

只有普通`stable_resource_supplement_preview_v1`可使用这条正式闭环；修正模式候选／人工编辑和090解除证明仍关闭。完整正式原读取不因后续资料变化、归档或写门禁停用而重新解释；原键完整输入不同、原读取中断或COMMIT确认丢失均为unknown。实际新空库已经验证COMMIT成功后模拟ACK丢失，再找回唯一完整原结果；并发同请求只返回原结果。停用092保留实际已提交合并／原回执／下游冲突及源阻断，并恢复087原关闭保护，091停用也保留独立修正原结果。能力标志保持false；092不注册普通启动、未安装用户库或加载在线服务，HTTP/UI及完整修正解除流程仍待接通，不能记C2整体业务完成。
