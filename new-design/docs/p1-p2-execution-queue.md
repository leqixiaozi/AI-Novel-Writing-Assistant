# 新设计 P1／P2／P3 唯一执行队列

更新日期：2026-09-17。需求来源：讨论任务 `01a0a2ed-21ae-7182-ae8e-3daceb0139e2` 的追加派发。本队列接在通用业务表单 AI 辅助之后，不重做已经交付的树资源与统一开书表单。

## 最新执行策略（覆盖以下阶段状态）

### 新授权批次：剩余能力 0..8 编码／静态收口中

2026-09-17 用户要求“把没有做完的分发下去”，讨论任务正式启动[剩余实施计划](next-batch-legacy-reference-plan.md)。本段覆盖下文旧“下一批待明确启动”门禁，但不扩大目录、正式库、用户服务或 push 边界。唯一顺序为：语义消费衔接 → 世界一致性 → 写法提取／应用 → 标题 → 图像独立能力 → 专业图形 → 人物截至章对话 → 跨书导演跟进 → 独立脚本启动／打包。0 精确段落消费及失效保护静态收口中；1 世界修复主链已接，人工修改与登记恢复收口中；2..5、7、8 模块编码及静态审阅已完成，中央入口已接；6 人物截至章会话与页面正在收口。未完成项不能打勾。整个 0..8 先完成授权编码及逐项静态 review，再统一安装／类型／构建／测试／隔离迁移／合法配置实际模型／运行；此阶段不执行。原批有效证据保留，不重复重做；具体状态以计划表为准。

### 历史执行状态：原批统一代码级验证与集中修复已完成

首个原批限定阶段提交为 `f8a1c3c6`，未 push。原授权知识数据闭环正向 PG 补验已完成：正向 10/10、相关 unit 62/62、隔离迁移 75/75 及服务端构建通过；074..076 和分块复用必要修复不动用户库。准确覆盖与证据复用见[本切片记录](knowledge-index-pipeline-review.md)。下方 380／63／72 为前阶段最终证据，不冒充后续版本全覆盖。不启动下一批；真实模型、UI、用户库升级仍未验收。

全部原授权编码及最终静态 review 完成后，监督任务明确续派同批统一验证。实际已完成本包锁安装、双端类型／构建；集中修复后的最后复测，53 份单位／局部 HTTP／独立边界文件 380/380、13 份真实 PG／HTTP 文件 63/63，0 失败、0 跳过。专用 tmpfs 空库完整迁移链最终 72/72，包括必要增量 072／073。首轮 PG 52/55 及随后知识规格失败保留历史，根因与集中修复见[当前原批统一验证](current-batch-unified-validation.md)。不称真实模型、全产品或界面通过。下方“未安装／新用例未跑／未应用／待统一验证”等为进入统一阶段前的静态盘点：用户开发库仍未应用迁移，但独立测试库已应用；不沿用过时措辞报告当前状态。历史失败与静态记录继续保留。

真实模型缺明确可用专属配置和安全执行目标，未执行；UI 交用户，下一批未启动。只在 `new-design/` 范围准备当前阶段提交；当前分支没有远端对应分支，首次推送会包含既有非新设计历史，发布该完整历史需额外明确决定，不 force、不新建分支、不重写历史。

用户再次要求：所有当前已授权任务（含已发现问题的必要修复）完成编码后，再一次性进入统一测试阶段。每处理一个任务只做源码、合同、依赖、失败恢复及数据安全的静态 review，不执行测试、构建、类型检查或运行验收。当前已知必要修复及最终静态交叉 review 已收口，条件和实际剩余以本页清单为准，不用此前“进入统一验证”措辞代替当前判断。本切片仅完成编码和静态核实，未续跑动态检查。已经执行的检查及失败记录保留，不冒充后续修复后的通过证据。接下来可安排同一批统一检查、集中修复和复测；下一批待执行计划不纳入当前任务。

## 下一批计划隔离（只登记，不属于当前存量）

[旧版对标后的下一批功能计划](next-batch-legacy-reference-plan.md)已独立登记，所有项状态为“下一批待执行”。下一批启动须用户后续明确执行，现有十分钟监督不得自动派发这些新增能力；本链接不新增当前剩余编码或统一验证项，不扩大当前收口、验证及已授权提交／推送范围。此次仅保存计划，不实现、不验收、不单独测试、构建、迁移或提交。

## 进入统一验证前的源码盘点（2026-09-17，历史静态状态）

本表依据文件、导出接口、中央页面派发、API 与 HTTP 挂载进行只读核对，不查询运行数据库或调用模型。**编码完成不等于测试通过**；当前规则为全部授权阶段编码结束后才一次性统一验证。下方过去的构建／测试证据只说明当时版本，不能验证本轮新增代码；未勾选不意味着代码尚未编写。

| 切片 | 前后端与入口 | 正本的实际消费者 | 文档／用例 | 当前状态与真正剩余 |
| --- | --- | --- | --- | --- |
| P1-A／P1-B 开书与空白 AI | `bookCreation`、`creationReviewAi`、`creationDirector`、`bookCreationProduction`；中央创建页及 HTTP/API 已接 | 精确临时资料 ID 审阅→模板安装真实 ID→原关系版本／四层规划／采用；阶段候选不会自行覆盖人工 | 模块 README、`creation-production-review.md`、开书单位／PG 用例 | 编码和静态 review 完成；060 来源桥增量未应用，后续模型／界面与新增回执等待统一验证，不重做七起点 |
| P1-3 结算类型化 | `chapterSettlementEditing`、`chapterSettlementAi`、本书关系配置、章节工作台；专用 HTTP 已接 | 原 session→提案→明确审阅→原结算／状态／认知流水／快照；普通资料保存不伪结算 | `chapter-settlement-editing-review.md` 及模块 README | 历史定向验证已记录；本轮来源对象消费增补仅静态审阅，仍待统一复验，不声称全站恢复通过 |
| P2-2 知识与参考 | `knowledgeReference`、`knowledgeEmbedding`、`knowledgeIndex` 应用／数据库／HTTP／API及知识源页实际索引面板已接 | 原资产版本／UTF-8解析／精确引用／manifest；4个知识消费者及真实向量执行／原attempt/result／原索引分代／语义检索。显式同原key恢复只允许新领取者调用，已有尝试只读 | 模块README、知识桥／实际执行／原恢复单位与HTTP用例、[恢复静态记录](knowledge-index-recovery-static-review.md)已写 | 主模块编码与静态交叉review完成，等待全批必要修复及最终静态review收口后统一验证；中央generation回执合同已同步，不重复开发。061／069／070尚未应用；旧缺身份／坏存储／失效来源保持真实阻塞，不冒充全站可恢复或真实模型／索引动态通过 |
| P2-5 多维／统一编辑 | `multiviewAuthor`、`BookViewsPage`、`businessForms`、`authorMaterials`；中央 HTTP/API 已接 | 正式版本字典／标签筛选→原资料集合；内嵌统一表单→原资料版本和来源账本／原请求回执 | 多维、资料写入 README 及单位用例 | 编码和静态 review 已收口；062 未应用；树范围、草稿、未知提交、主题／窄屏待统一验证 |
| P2-1 作者任务中心与用量 | `authorTasks`；页面、筛选、详情、中央 HTTP/API 已接；实际详情挂载 `AuthorUsagePanel` | 只读聚合原来源；实际原 tasks→attempts→LEFT usage 汇总，缺失记录／未知 token／费用分别标识，非 AI 记录仅展示明确书级范围，不假称导演精准费用 | 前后端 README、任务／来源／用量单位用例已写 | 编码和静态 review 完成；运行与实际来源恢复待统一验证；`exact=false` 不冒充精准恢复。没有已落库估算不猜单价，不把估算当账单 |
| P2-3 模型／路由 | `modelManagement`、模型页面、专用 HTTP；原持久路由执行器 | 原 configs/versions/fallbacks/credentials/snapshots/attempts/usage；没有环境模型隐式回退 | 路由 wiki／历史定向验证记录 | 基础已有历史验证；本轮新增任务类型与066联动仅编码，真实模型可用性需最终实际检查，不能把配置源码当已配置模型 |
| P2-4 分类／组合／试运行 | `promptManagement`、`promptComposition`、应用 `runPromptCompositionTrial`；页面与 HTTP 已接 | 原组件正文／recipe/version/slot→受控 PromptAsset→精确上下文／模型快照→原单次 claim/attempt/result/usage；专业试验复用同执行器 | 组合 wiki／README／历史验证和本轮知识桥用例 | 基础历史验证保留；本轮知识 exact-source 与应用 facade 静态完成，模型／界面与回执新改动待统一验证 |
| P2-6 专业创作资源 | `professionalResources`；中央专业页注入 catalog/command/receipt 与原组合 trial API；HTTP 已挂载 | 原资源 Card 版本、resource_adoptions、质量问题；收藏／安装／反馈使用引用和不可变技术回执，试验不写正文或质量正本 | 前后端 README、`professional-resources.unit.test.cjs` | 编码和静态 review 完成；063 未应用，用例未跑；真实兼容组合／模型试验与安装原子性待统一验证，不另造试验表 |
| P2-7 Tiptap 正文选区 | `bodyEditor` 已被 `ChapterWritingPage` 实际渲染；依赖及锁文件已编码 | 同一个正文 draft／原 Markdown 保存，选区冻结与替换只改原草稿，不新增 HTML／JSON 正本 | `tiptap-editor-scope.md`、模块 README、`body-editor.unit.test.cjs` | 已编码，不再是“仅登记”。依赖版本锁定不表示已安装／运行；输入法、跨段、撤销、长文、主题与保存重开待统一验证 |
| P3-A 全书编排 | `bookComposition`、中央页面／HTTP/API 已接；原 `storyTimeline` 中文时间、因果、并发、跨章 occurrence 和原请求回执已编码 | 原规划／采用／叙述顺序／时间关系；实际五视图导航沿原工作台与多维来源页，原编辑器保持挂载／草稿。065排序及068时间回执，不复制事实；规划精确 plan 参数与锚点被原页面消费 | 模块 README、排序／时间／HTTP／来源选择用例已写 | 编码与静态 review 完成；065／068 未应用。完整全书交互、排序、时间回票及窄屏／主题留待统一验证，不声称已动态通过 |
| P3-B 开书后导演 | 手动章节与导演共用 `chapterProduction`、受控 chapter_generation、原 HTTP 11 路由；中央/API/来源页已接 | 同事务连续性6类原来源→真实resolver hash→实际 Prompt／manifest；显式知识原版本全文。真实模型输出先受控本地凭证→DB原输出→原候选→原账本／章边界；采用／结算仍由原创作链明确执行 | 模块／wiki、连续性5项、原回复7项、原HTTP5项、导演／章节知识及恢复用例已写 | 编码与静态交叉 review 完成；多事务入库恢复结果未知、精确原范围链接、原用量缺失与已保存证据不冒成功。066／069 未应用，全部新用例未跑。本地凭证不证明DB提交或采用 |
| P3-C 世界／人物专业维护 | `worldCharacterMaintenance` common/database/http/client；中央专业页与 API/HTTP 已接 | 原档案／正式字段→真实关系版本／当前来源有效状态与认知／原快照；统一编辑器和AI候选写回原资料。来源 subject 仅空草稿预选／真实清单滚动，不改决策 | 模块 README、6项单位／静态用例已编写 | 编码和静态 review 完成；无新迁移。真实 PG 来源校验、档案编辑／草稿恢复和界面待统一验证，原未解决冲突只读不等于AI一致性验收 |
| P3-D 本地视觉资产维护 | `visualAssets` common/database/http/client、中央注入 API／HTTP 已接；没有虚构 application 层 | 原 assets/versions/content/adoptions；受控本地图片上传、原版本说明、明确采用／绑定／预览／归档回执，不开第二视觉事实链。GET错误不携带写入结果，未知写入不伪称回滚 | 前后端／HTTP README、本地视觉8项及回执静态用例已写 | 本地维护编码与静态 review 完成；067 未应用，全部新用例未跑。图像生成执行适配未接，`generation=false` 是实现能力缺口，不能把本地维护当生成闭环完成；不扩外部服务 |
| P1 中文输入与原请求尾项 | `DynamicForm`、`MarketRadarPage`、`BookAnalysisPage`、`ContextManagementPage` 与原 facade；中央 Market3合同／Context2只读端点、结构071回执和模板升级只读核对已接 | 字典精确新UUID；研究／Context精确原来源输入；结构save/publish同事务技术回执不复制正式定义或版本，原模板sync核对不重放升级；坏存储和错范围保留原稿与凭证 | 中文界面当前表、typed run input及回票、研究和结构写入用例、模块README已写 | 编码与静态 review 完成；TemplateGroups／FormDesigner草稿、读代次、中文冲突和原请求恢复已收口，Context完整回票与P3-C原编辑器保护已修。071未应用，新增用例未跑；旧扫描、拆书／参考包全站未知采用仍未冒称闭环 |

迁移 060／061／062／063／065／066／067／068／069／070／071 当前仅保留 SQL／注册，**尚未应用或动态验证，编码静态阶段禁止执行**。070 原嵌入请求技术冻结及消费者已编码，不代表已迁移。新增单位、HTTP、PG用例只写，不运行；历史类型／构建失败见[当前批失败与修复静态记录](current-batch-failure-static-review.md)，没有修复后动态通过证据。统一阶段才进行依赖安装、类型、构建及核实隔离后的真实 PG/HTTP、可配置模型和独立启动；禁止盲跑用户开发库重建或迁移。UI验收默认交用户，不冒称浏览器通过。

### 历史必要修复清单：进入统一阶段前剩余编码 0 项，最终静态交叉 review 完成

历史失败及此次静态交叉 review 已逐项登记在[当前批静态记录](current-batch-failure-static-review.md)。导演语法、getBook 导出、审核 action 缩窄、migration 作者输入边界、FormLocalFieldDefinition 公共属性及嵌入完整原请求解析已修复。新发现的向量 trim／原fullhash／原publication修订、trim后临界长度／存储容量，以及表单已定义 defaults／trim／原raw保存／旧pending只读匹配均已完成编码及最终静态交叉 review。各原分工 DONE，当前已知必要修复剩余0；全部未复测。Tiptap 依赖声明和锁文件齐全但未安装，是统一阶段环境项，不以假声明／any掩盖。未完成编码或静态 review 时不续跑检查；当前条件达到仅意味着可安排同一批统一验证，不等于已通过。

1. 原向量配置、真实适配、索引执行与知识页语义检索编码及静态收口完成，不重做主模块。中央构建／原构建查询已同步 `KnowledgeGenerationReceipt`。初始无领取／无lease未知准备已补完整原输入与同 key／fullhash／锁下显式恢复：GET失败／阴性不清键，已有尝试不再调用，恢复失败保留原凭证。可信未写证明还须成功同锁核实原准备确无领取及回滚确认，不能用当前局部回滚冒充原key无结果。旧缺身份、坏存储、失效分代和非受控尝试仍真实阻塞；不是自动恢复闭环，见[静态收口记录](knowledge-index-recovery-static-review.md)。
2. 模板／表单草稿切换、读代次、中文正式冲突、原结构 save／publish 同事务回执与来源恢复，以及 Context 完整冻结回票和 P3-C 原编辑器保护，已完成编码和静态收口。结构原请求只读 GET、模板升级只读 GET 与页面实际核对入口已接；071 未应用，新增用例未运行，不宣称动态通过。
3. 原授权主切片及上述必要修复已编码，各原分工 DONE，主任务最终静态交叉 review 结束，当前已知必要剩余0。统一验证计划：按本包锁文件安装依赖→双端类型／构建→明确选择新增单位／负向HTTP／独立边界→先核实目标隔离再验证PG／HTTP→有明确配置才实际模型／输出／用量／独立启动。默认test仅三份旧用例，不能冒充全批新增覆盖；不得无选择遍历测试误触开发库或模型。此切片未执行统一检查，没有修复后动态通过证据；模型配置与历史坏凭证阻塞单列，界面交用户体验。下一批仍待用户明确启动，不进入本批验证、监督存量或实现范围。

图像生成未接为保留能力缺口：当前授权本地维护已编码，未经明确扩展不能接新外部服务或假生成。真实文本／嵌入模型配置可用性、所有迁移和运行检查均待统一阶段；P4／P5排除。上表覆盖历史checkbox措辞，不重复重做已编码主模块。当前不push。

> 白话比喻：源码状态像新房里已经接好的水管和电线，验收是最后开水、通电逐项检查。对应到系统：文件和调用入口存在只证明接线，不证明数据库、模型和界面实际运行成功。

> 速记方法：接线是编码，开机是验证；旧验收保历史，新改动等统一检查。

## 实施边界

- 仅修改 `new-design/` 及其文档；旧目录只允许阅读参考，禁止直接／间接 import、运行调用、旧网关注入或旧 SQLite 数据链依赖。已有旧壳挂载是过渡耦合，不算独立运行验收。
- 根 PromptAsset／registry／网关写入例外申请撤回，用户明确否决。受控 PromptAsset、注册、结构化输出、模型适配和独立启动／构建入口均归属 `new-design/`；提示词治理保留，不在业务服务散落内联提示词或原始模型调用。
- 每项必要补缺先记录：无法闭环的用户动作 → 必要缺口 → 旧系统参考行为 → 新设计最小实现 → 验收点。只补已授权 P1／P2，不照搬旧模块、不扩张 P3／P4／P5。
- PostgreSQL 是正本，AGE 与 pgvector 是同一底座上的投影。已有卡片、表单、关系、状态、规划、正文、任务、研究和资产不得再复制成第二套事实。
- 人工、模板与 AI 使用同一合同；AI 提供候选，用户明确采用。人工未保存草稿必须保留。
- 普通用户只操作中文和类型化组件，技术键、UUID、哈希和 JSON 仅限明确折叠的系统信息。
- 失败必须标识具体阶段／可识别资料与字段、已保存内容及明确恢复入口。动作位于对应来源页，运行记录只负责查看并跳回；幂等回执不确定不重复创建，人工接管拒绝迟到覆盖。这条要求接入后续所有专业流程，不只开书页。
- 提示词组合调试沿用相同恢复要求：冻结预览和运行标识保留，模型调用失败、结果入库失败、结果读取失败必须分阶段；未知先只读核对，禁止自动再次调用模型。当前切片的合同见 `wiki/prompt-composition-execution.md`，完成全部编码后才集中测试；真实模型／界面验收仍须单列。
- 失败提示旁必须有明确可点击入口，而非仅写“请稍后重试”。校验失败定位到中文字段；先保存后发布的流程列出已确认步骤；断连／无效回执先核对再决定重试；启动／模型失败分别引导运行维护／模型设置，不把技术堆栈直接堆在页面。当前追加先落内容类型和独立运行，其他流程逐项接入，不宣称全站恢复完毕。
- 修订防覆盖、来源审计、历史引用和作用域校验属于各阶段的安全要求。
- 所有失败分支静态 review 使用 `wiki/failure-recovery-gate.md`：作者必须能回答“哪一步失败、哪些没丢、点哪里、点击的后果”；旧门禁仅供参考，局部质量债与全局运行失败分开。
- 用户追加授权 P3 全书编排、开书后导演、世界／人物专业维护与已有视觉能力最小闭环，放在 P1／P2 基础之后；不做 P4 高级版本专项、P5 登录权限，不扩展漫画／短剧或新外部服务。

> 白话理解：现有业务数据是一本账，界面和索引只是不同的查账方式，不能每换一个页面就抄出另一本账。

## 依赖顺序与验收点

先盘点每项已有实现，再补缺；每项编码完成后只做静态 review，全部授权任务完成编码后才统一测试。列表中的未勾选项均未宣称验收通过。

用户最新锁定策略（2026-09-17 再次确认，覆盖此前逐切片验证节奏）：全部剩余授权任务先完成前后端、落库、界面与文档编码；每处理一项只做源码、依赖、合同、失败恢复与数据安全的静态 review，然后推进下一项。全部任务完成编码后，一次性安排类型检查、构建、单测、集成和必要模型验证，再集中修复、复测。此期间不执行测试、构建、类型检查、迁移应用或运行验收；只读源码／依赖分析和必要数据安全检查仍允许。此前实测作为历史证据保留，不冒充后续改动验证。测试用例继续随编码编写，不执行；不重做已交付功能。

### 最新有效顺序（覆盖下方原派发顺序）

2026-09-16 独立运行基础与失败定位切片验证见 `independent-runtime-review.md`：真实 PostgreSQL／HTTP与29项检查通过。后续模型路由中心复用既有模型事实保存默认／任务／备用设置，冻结版本并按统一预算执行；集中类型／构建与51项检查通过，失败定位、真实数据库隔离及结果核对见 `model-route-runtime-review.md`。实际模型尚无明确可用配置，真实生成未执行；全站恢复和集中用量展示仍按队列推进，不沿用前一切片证据，也不以测试桩或启动替代真实生成验收。

1. 独立提示词分类树最高优先；先只读盘点直接／间接依赖。其运行闭环所需的新设计自有 Prompt 注册、结构化合同和模型适配先补齐，不等待根目录例外。
2. AI 模型与路由基础：独立配置、探测、默认／备用、中文任务路由、超时与预算。
3. 提示词组合／调试：分类选择、排序启停、参数、上下文与最终预览、试运行，保留系统合同和账本。
4. 剩余中文类型化界面与章节结算。
5. 统一开书审阅逐资料空白 AI；导演通过自有阶段合同生成资料／关系／正式规划提案，走新设计事务校验与明确采用，不以资料卡冒充 PlanningObject。
6. 知识与参考输入、解析／索引／重试、检索与绑定。
7. 多维筛选／分组、编辑器与草稿保护。
8. 作者任务中心聚合原状态，恢复动作在来源页。
9. 专业创作资源视图。
10. 执行下方 P3，全部授权任务完成编码和逐项静态 review 后，统一进行独立启动／构建、真实 HTTP／PostgreSQL／模型与完整闭环验收。各项同步讨论任务并明确“编码／静态审阅完成，动态验证待统一执行”；全部授权 P1／P2／P3 完成或准确单列外部阻塞并汇报后，核对远端和当前分支正常推送，不强推、不合并其他分支。

### P3（新增授权，基础闭环之后）

- [ ] P3-A 全书编排／编辑工作台。依赖 P1 类型化规划与结算、P2 多维与自有 AI。参考指定分支交互但不搬代码；统一章节／叙述顺序／故事时间、因果并发、角色关系／道具状态／伏笔等事实，接卷策略、节拍、节奏张力、承诺回收与角色弧。拖拽排序先影响预览、明确确认事务保存，必要局部重规划，AI 与人工可切换。当前已有独立工作台、排序预览／回执及中央入口编码；原时间／因果／并发微调与跨章引用正在收口，全部待统一动态验收。
- [ ] P3-B 开书后全书导演／批量生成。依赖正式规划采用、独立模型、正文／结算／任务运行。按规划生成正文，暂停／继续、失败定位恢复、可控重试、来源、质量与局部修订；局部质量欠账不泛化阻断整书，完整正文与下游读取须验收。当前已有批量导演、受控章节执行及中央入口源码；精确连续性来源与整阶段静态收口正在完成，实际模型／运行验收仍未执行。
- [ ] P3-C 世界／人物专业维护。依赖统一资料、关系、状态、认知与结算快照。组合一致性、人物动机／认知／关系／状态／声音，AI 候选经编辑和明确采用，复用正本。专业组合视图、真实来源读取、统一编辑器／AI、精确来源对象消费已编码并静态审阅；用例未跑，实际验收待统一进行。
- [ ] P3-D 已登记视觉资产最小闭环。依赖独立视觉模型能力与资产唯一正本，只生成／保存／引用；能力未配置如实单列阻塞，继续其他独立项。当前资产正本、前后端入口和中央接线已有源码，最终静态收口待主任务确认；独立视觉模型能力未实际验证，不宣称可用，不扩展外部服务或新产品线。

### 提示词分类切片历史需求说明（已有编码／历史定向验证）

用户动作“按中文分类寻找并编辑提示词”无法完成：当前仅有通用类型目录。必要缺口是独立分组树、分类内新建、主分类与额外组引用、中文编辑。参考旧系统只限树管理行为，最小实现复用新设计提示词公共空间、分组版本／成员引用及动态表单，新增受控主分类组织标记，不复制正文。验收：分类创建／子级／移动／排序／归档，组件保存与正文版本、主分类唯一、兼容额外成员、作用域／并发／回执校验，移动不改变配方／运行历史。实际 UI 未验收必须单列。

下方保留原编号方便追踪，旧的“P1 先于全部 P2”文字不再代表优先级。依赖盘点见 `independent-runtime-inventory.md`。

- [x] 第三阶段：通用业务表单 AI 辅助。共用业务表单与多维编辑器；填空、补充、查矛盾、多方案、按句修改、字典／标签推荐；冻结未保存草稿、规格、关联与允许节点。中文差异逐项采用到普通草稿，正常保存后留来源，越界、过期、重复请求有服务端校验。定向运行证据及未执行项见 `business-form-ai-assist-review.md`，不代表 P1-2 全量验收已完成。
- [ ] P1-1 中文界面与技术输入清理。盘点开书、资料、多维、研究、雷达／拆书、模板、上下文、运行维护、规划、正文、结算；用中文对象选择与类型化输入替代原始 JSON 和技术键。系统固定状态语义不交给业务字典修改。
- [ ] P1-A 空白表单 AI 填写。当前已开始切片收尾后优先处理；中文动作“AI 填写必填项”“AI 补充剩余内容”“AI 全部准备”。生成前不要求填完必填项，正式保存／确认开书才校验；有效规格、资源归属与运行安全仍校验。支持一句想法、参考资源或 AI 推荐方向，默认填空不覆盖人工内容，明确改写才提出替换候选。真实验证空白人物／世界／事件入口可生成、采用必填内容并正常保存。
- [ ] P1-B 自动导演开书两种模式。接在 P1-A 后、P2 之前：一键连续准备至统一可编辑审阅；分步由 AI 准备当前阶段，用户修改／重生成后下一步，可返回、保存、恢复。共用阶段执行器、动态表单、创建会话／草稿、来源和 PostgreSQL 正本，仅暂停策略不同，均须明确确认开书。阶段按新设计规格组织创作方向、项目设定／故事规划、世界观、人物／关系、必要卷／事件／章节骨架、统一审阅；可失败保留、人工接管、重试和切换模式。现有空白／模板／研究／续写等来源保留，旧导演源码只读参考，不扩张为开书后全书正文自动生产。完成真实 UI／API／模型、幂等与恢复验证。
- [ ] P1-3 章节结算类型化。对象选自本书资料／关系，字段选自正式可结算规格；前后值按文本、数值、字典树、布尔等渲染。人工与 AI 共用提案确认清单，不自动改正式状态。
- [ ] P1-2 第一至第三阶段运行验收。核实 046／047／048 及后续迁移真实执行，检查树范围／层级／单多选、模板只增同步、七种开书审阅、草稿保护、逐项采用；核实两条规划样例的幂等键失败。运行 API、可行模型调用与针对性 UI 验收，记录实际未执行项。
- [ ] P2-5 多维增强。字典节点／分支／后代、标签维度筛选与分组；切换视图保留搜索、选中与条件；统一正本编辑器及第三阶段 AI 辅助。
- [ ] P2-1 作者任务中心。聚合领域待办并引用原状态，标签筛选与标记，跳正确来源页处理；与管理员运行维护页分开。手工创作任务具有独立明确语义。
- [ ] P2-2 知识与参考中心。可靠本地文本／Markdown上传、已有解析、索引状态与重试、查看与检索、按书／章／资料／任务绑定、AI 引用片段；资料提炼经候选确认，归档前影响预览。复用资产、研究、向量与上下文。
- [ ] P2-3 AI 模型与路由中心。中文任务类型、连接测试、默认／备用、任务路由、超时／重试／回退、预算／用量；复用合同、路由快照和账本，凭据使用既有安全机制，全链脱敏。
- [ ] P2-4 前置（最高优先）：独立提示词分类树管理。在“新设计 → 创作资源”提供独立提示词入口，左侧紧凑分类树、右侧中文编辑／预览、分类内新建；复用提示词公共资源空间的分组树及已有移动／排序／搜索／归档基础。保存仍为现有提示词组件资源，分类、标签、配方各自独立；组件正文不复制，分类移动不影响配方或运行历史。完整要求见下方“提示词分类树追加范围”；依赖自有受控运行基础，不新建任务或分支。
- [ ] P2-4 提示词组合与调试。组件选择／排序／启停、配方、合同、可视化参数、上下文与最终提示词预览、路由、试运行结果；系统 schema 不卡片化，试运行不改小说正本。
- [ ] P2-6 专业资源视图。标题候选比较收藏与明确采用；写法资源／清理／局部试验；质量规则启停范围试验与效果反馈；资源组合安装书内独立快照。全部复用资源卡与已有质量账本，不建专用重复事实表。
- [ ] P2-7 Tiptap 正文编辑器。讨论任务 `01a0a9cc-294c-72e2-83af-b15fb725a6d1` 派发，不打断当前提示词功能、不升 P1。依赖独立前端入口／包配置和正文草稿保存合同，安排基础闭环之后、P3 全书编辑之前。复用正文唯一数据源，中文固定工具栏／跨段选区 BubbleMenu，失焦保留选区快照，文档变化拒绝无提示替换，确认仅替换目标范围并可撤销；提供自定义按钮／对话框接口，不另做 AI。范围及许可／输入法／长章节验收见 `tiptap-editor-scope.md`。当前已编码并嵌入章节正文 draft；官方版本／MIT许可已只读核对，依赖及锁文件已保存，未安装运行或执行动态验收。
- [ ] 最终检查与推送。核对各切片验证证据、未执行项、迁移与数据同步说明；仅推受控新设计工作分支，不强推，不包含无关文件。

完整验收覆盖中文、结算及前三阶段；各前置切片仍分别定向验证。执行优先级以“最新有效顺序”为准，模型基础先于组合调试，专业资源最后复用这些能力。

## 提示词分类树追加范围

2026-09-16 由讨论任务追加派发，随后用户锁定为最高优先。本节保留派发要求；当前编码状态以顶部源码表为准，复选框未勾选表示最终验收未完成，不再据此认定尚未编码。

- 独立“提示词管理”入口，不要求普通用户从通用内容类型目录查找提示词。左侧选择分类／提示词，右侧编辑、预览；分类内可直接新建。
- 分类树与字典、标签在业务上独立；复用已有树形组件和提示词资源空间的资料分组引用，不另存提示词正文或建立事实孤岛。
- 中文初始分类：角色与职责、任务指令、写作要求、约束与质量、示例、上下文组织、用户自定义。支持子分类、原地新增、重命名、移动、同级排序和分类／提示词搜索。
- 每个组件默认一个主分类，标签支持额外多维归类。兼容已有多组成员时不丢引用，明确主分类与额外分组；移动只改组织引用，不改正文、配方或运行历史。
- 使用既有 `prompt_component` 资源，通过中文表单维护名称、内容、说明、适用任务与启停。分类不是执行顺序，也不会自动加入所有 AI 任务。
- 同一组件可被多个配方引用，禁止复制正文。分类用于查找维护，配方列表决定组合／排序；独立管理页与后续调试台互通。
- 资源管理、配方选择器、AI 组件选择处共用分类树查找／选择；普通用户不输入英文键、UUID 或 JSON。
- 作者组件与系统 PromptAsset 注册是不同层，均归属新设计。系统任务合同、schema、路由和凭据不改成普通组件卡；根模块写入例外明确被否决，需在新设计实现自有受控基础。
- 仅改 `new-design/`，不提前 P4 版本专项／P5 权限；完成编码后补充类型检查、分类移动／归档／引用稳定性测试及文档。真实界面未验收时明确说明，不能勾选完成。

> 白话比喻：分类树像图书馆书架，标签像检索贴纸，配方像某次阅读清单。对应到系统：移动书架位置只改变分类引用，同一提示词正文能被不同配方引用；分类顺序不等于运行顺序。

> 速记方法：分类管找，标签管筛，配方管组合；移分类不改正文，不改旧运行。

## 逐切片交付记录

### P2-2／P2-5／P2-1 接线进度（历史编码快照；当前已静态收口）

以下三段保存当时接线盘点；“仍在进行／正在收口”不是当前剩余。实际知识组合消费者、统一表单提炼、正常资料保存来源再校验、多维内嵌编辑与原请求凭据、任务研究／导出精确来源消费均已完成编码及静态 review，当前只等待全部授权任务编码结束后的统一验证。历史依赖状态缺失需真实重新上传，语义索引执行能力仍未实现；不据此宣称迁移已应用、模型可用或界面通过。

知识与参考中心的独立导航、本书导航、页面派发、API 及 HTTP 入口已经接线；上传 UTF-8 文本／Markdown、原请求回执核对、解析显式重试、正文查找、准确版本绑定、归档影响预览与确认、分页正文查看和显式引用选择均有源码。引用必须复用真实上下文清单及允许资料类型的位置，不能把 asset mount 绑定冒充模型注入。解析及引用增量 061 已注册但未应用，060 同样未应用；最终上下文消费者与增量静态收尾仍在进行。语义检索尚无本轮执行器，页面只声明正文查找，不造向量。

多维模块的正式类型版本字典筛选、标签维度、分组和本机条件保留已编码；统一 `BusinessFormWorkspace` 编辑器正嵌入原多维页面，未知保存的准确原请求凭证是必要收口项，不能凭内容相等解锁。原资料版本增量 062、`authorMaterials` 写入及原请求 GET、API 和 HTTP 入口已编码注册，但尚未应用／实测。运行记录的只读聚合、筛选、来源详情和导航已接 API／HTTP／菜单／路由；研究记录和导出来源的准确链接消费正在收口，不以其他首项代替目标记录。

证据入口：`src/client/knowledgeReference`、`src/common/knowledgeReference`、`src/server/http/knowledgeReference`、`src/client/multiview`、`src/client/businessForms`、`src/client/authorTasks`、`src/server/database/authorTasks`；各模块 README 和新增用例随编码保存。所有新增用例均未运行，未进行类型检查、构建、启动、数据库迁移应用、运行 HTTP 或模型验收。完整交付状态更新后接续专业资源、Tiptap 和授权 P3；复选框不勾选通过，不等待阶段测试，也不重做已完成切片。

### P2-7 正文选区编辑（已编码接线，未运行验证）

必要缺口：正文原来的 textarea 无稳定选区对话框，失焦及文档修改后不能保证只替换目标。最小实现 `src/client/bodyEditor` 复用 Tiptap 核心及 React BubbleMenu，直接消费原 `draft`；段落是内存投影，Markdown 按原文保存，不新增 HTML／JSON 正本或 AI 功能。冻结 PM 位置、正文 UTF-16 偏移、原文及完整编辑修订；确认前核对结构，事务草案先核对目标替换后的完整正文，一次 dispatch 可撤销。输入法组合态不执行替换，外部来源变化使选区失效。章节工作台已接该编辑器；AI 选区仅在本地稿件已保存后提交，避免位置属于未保存稿而来源指向旧版本。

官方文档、MIT 许可及 npm 元信息已只读核对，同版 3.31.3；独立 `package.json` 与 `package-lock.json` 已锁定，未安装／运行编辑器代码，未执行 CLI 模板生成器。5 个纯函数／静态用例已写入 `tests/body-editor.unit.test.cjs` 但未运行。中文输入法、跨段、撤销、保存重开、长章节及主题仍待最终统一验证，详情见 [编辑器范围](tiptap-editor-scope.md)。

### P1-A／P1-B 统一开书空白 AI 与正式结构（编码完成，动态验证暂停）

章节结果确认切片 `c167985f` 收尾后，讨论任务派发接续此完整功能。保留七起点、统一表单和共用阶段执行器；补逐资料空白 AI 填必填／补剩余／全部建议、明确采用保护人工，以及真实关系和规划的中文草稿、采用与确认开书原子落库。创建起点、保存表单、控制阶段与确认开书补原请求查询及结果证明；未知先只读核对、已保存输出只续后续阶段，不重复调用模型或开书。

正式规划口径由讨论任务明确为实际全书 → 卷 → 章节 → 场景四层，不新增事件规划层。事件使用独立资料正本，通过 `planning_version_references.reference_role='event'` 精确引用，可跨章共享；不伪装场景、不以文本代替引用、不复制事实。盘点与必要补缺见 [本轮盘点](creation-production-inventory.md)。本项编码及静态 review 已完成。切换最新策略前执行过部分验证，发现开书前模型快照与既有依赖桥兼容缺口，已编写增量 060 修复及用例，但尚未应用／实测。后续动态验证暂停，留到全部授权任务完成编码后统一进行，详见 [验证记录](creation-production-review.md)。模型／界面验收单列，未完成项不勾选；本项不扩全书正文生产或 P4／P5。

### P1-3 章节结算类型化（2026-09-16，编码与定向验证完成）

关系状态结算的必要恢复补缺已由讨论任务确认纳入同一阶段：正式关系属性、显式能力和真实维度缺少落地配置页，补最小中文草稿／校验／明确发布与本书范围；复用原正本，不自动提升能力、发布或覆写共享来源。进程中断而提取租约过期的未知结果，提供显式结束并保留未知用量，不能永远只查或偷偷再调用模型。这些依赖连同章节表单连续编码完成后才集中测试。

用户动作：确认采用正文后，在本书对象与正式可结算字段范围内记录前后变化、保存提案、审阅并明确结算，随后查看已有状态账本与稳定快照。中文选择、类型化表单、人工／AI 共用审阅、精确正式规格、原子回执、草稿保留、原请求只读核对与来源页恢复已编码；必要关系配置使用草稿和明确发布，并保留实例正式版本。原结算提案、正文版本来源、关系、字典、状态能力／映射与稳定快照仍为唯一事实。全部编码后集中修复验证，86 项单位／负向 HTTP／静态回归及 10 项真实 PostgreSQL／独立 HTTP 通过；最终双端构建与服务端补充复验通过。054／055／056／057 已应用。实际模型能力检查仍不可用，模拟响应不是实际模型；浏览器／主题体验未验收。详见 [本阶段验证](chapter-settlement-editing-review.md)，不勾选整个 P1-3 或全站恢复，不提前 P4／P5。

前置缺口历史见 [章节结算类型化与恢复盘点](chapter-settlement-editing-inventory.md)；最终证据单列，不把盘点当作验收。失败按具体步骤、保留内容和按钮说明，未知回执只能先核对，保存成功后的读取失败只重读。讨论任务确认本轮仅对象／资料状态与资料关系状态，故事时间／事件因果／并发提案留后续多维／P3，复用原合同，不伪装状态 JSON、不重复事实；有真实前置依赖时展示条件与原来源，不自动绕过。

### P2-4 提示词组合与恢复（2026-09-16）

组合页、中文参数、显式精确来源、受控最终请求、模型路由、单次试运行、只读结果与阶段化恢复已编码；迁移 053 已在真实开发 PostgreSQL 应用。全部编码后集中验证，67 个不同检查通过，最终构建通过。真实模型尚未配置，用户界面未验收；不以此勾选整个 P2-4 或全站恢复完成。证据与未执行项见 `prompt-composition-review.md`；模型 P2-3 基础已交付，集中用量视图与其他全局恢复仍未完成，后续顺序不改变。

每项记录：已有实现盘点、补缺代码／界面／接口、迁移及数据影响、静态审查、实际执行命令与结果、环境阻塞、提交号、讨论任务同步结果。模型不可用或浏览器未执行时明确列出，不以静态检查冒充动态验收。

代码和迁移进入 Git；已有业务数据通过逻辑备份／恢复同步，镜像只保留可重建 Dockerfile，不提交镜像或数据库运行目录。不能只拉取代码就期待作者已有数据自动同步。

## 当前记录

以下为交付历史；涉及“请求根目录例外／待批准”和旧优先级的历史结论已由上方独立门禁及最新顺序覆盖，不是当前卡点。

- 最高优先提示词管理完整功能：独立入口、分类／组件同树查找、中文右侧编辑／正文预览、分类内新建、子级／移动／事务排序／归档提升、主分类唯一、额外分组与标签引用保留、受控保存回执已完成编码。最终双端类型检查和单位／真实独立 HTTP PostgreSQL 共 11 项通过，证据见 `prompt-management-review.md`。UI／主题未验收；标签写入增强、配方选择接线、自有系统 Prompt／模型／独立启动仍待后续完整功能，P2-4 全项不勾选。
- 历史派发记录：当时 P3 与 P2-7 Tiptap 仅纳入清单，自有受控 Prompt、模型适配与独立入口尚待实现。当前这些基础已有源码及历史验证，Tiptap 已编码接入；最新工作状态以上方当前源码表为准，不再据此重复开发。

- 用户失败定位要求的内容类型切片提交 `50a4c5b1`：保存／发布具体步骤、服务器结果核对、差异确认继续、规格重读、原页草稿保留及维护新标签入口；双端类型检查通过，最终客户端复查及定向 9 项通过。浏览器、主题／窄屏未执行，未宣称全部页面统一恢复。
- 独立边界更新已撤回根目录例外。只读扫描 188 个新设计源码／脚本文件无静态相对包外导入，但旧 app 注入旧提示词网关、旧前端壳及包外构建配置仍存在；详见 `independent-runtime-inventory.md`。提示词分类树和自有 Prompt／模型基础尚未完成编码，独立启动／真实模型验收未执行。

- 已交付统一树资源 `d87233de`、统一开书审阅表单 `7032d6a5`。
- 已交付紧凑字典／标签目录 `3f838063`；客户端类型检查通过，浏览器交互未执行。
- 通用表单 AI 辅助已完成编码与定向验证，包含真实 PostgreSQL／HTTP、实际模型及界面采用草稿检查；补充 050 修复触发器搜索路径运行缺陷。
- 第三阶段提交 `22d590fb`。P1-1 正在分切片清理：研究提案中文类型化表单、普通服务提示及概览任务名称，边界与剩余范围见 `chinese-author-interface.md`；全项未勾选。
- P1-1 第一切片 `4828d399`。研究正式采用复用树校验和快照，真实 HTTP／PostgreSQL 用例通过，证据见 `research-adoption-tree-safety.md`；继续上下文中文对象／版本选择。
- 全部 P1／P2 尚未完成；最终 push 尚未执行。
- 上下文中文选择第一切片编码与定向类型／HTTP／PostgreSQL 验证完成，证据及未执行项见 `context-author-selectors-review.md`。本切片收尾后先执行新增 P1-A／P1-B，再继续其余 P1、最后进入 P2。
- 上下文切片提交 `dcf15033`。P1-A 通用业务表单空白 AI 切片完成，真实人物／世界／事件模型及界面生成、采用和保存通过，双端类型检查、单位及 HTTP／PostgreSQL 共 8 项通过；关联工作区 SQL 别名缺陷一并修复。证据见 `blank-form-ai-review.md`。统一开书审阅的空白 AI 操作随 P1-B 补齐，故 P1-A 全项仍未勾选；继续共用开书阶段执行器，不进入 P2。
- P1-A 通用空白表单切片提交 `1c4d321b`。P1-B 共用阶段准备／两种模式／中文失败恢复／确认原子发布切片已编码并执行定向验证，范围与待验收见 `creation-director-review.md`。正式关系与四层规划所需的新 PromptAsset／registry／根网关接线与仅 new-design 可改门禁冲突，讨论任务正在请求最小例外，尚无授权；不得把资料草稿当作正式关系或规划，P1-B 全项仍未勾选。统一开书审阅逐资料空白 AI 也仍待补齐。
- P1-B 独立阶段准备及明确恢复入口切片提交 `70681e22`，最新双端类型检查与单位／真实 HTTP／PostgreSQL 共 33 项通过；新界面实际点击和完整真实模型未验收。提示词分类树追加需求已纳入 P2-4 前置，仅更新队列，未开始实现。
