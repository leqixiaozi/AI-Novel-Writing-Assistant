# 作者界面中文与类型化输入边界

## 决策与维护规则

作者编辑的是小说内容，不负责输入 JSON、UUID 或内部英文键。动态业务字段沿用当前已发布内容规格与 `DynamicForm`；内部稳定标识和数据库正本不因中文显示改变。

> 白话理解：作者看的是商品名称，不需要记住收银系统里的条码。中文名称对应显示，稳定键对应内部识别，两者不能混用。

> 速记方法：显示用名称，保存用标识，编辑看已发布规格。未匹配内容保留待复核，不猜类型、不偷偷丢字段。

系统固定运行状态采用受控中文标签，只影响显示，不成为可修改的业务字典，不参与 AI 路由。任务名称优先读取已发布任务合同；未取得名称时使用“创作任务”，不把内部任务键直接展示给作者。

## 当前切片：研究提案与普通服务提示

- 参考包的书内提案编辑共用 `researchAdoption/AdoptionItemEditor`；本书发布类型优先，公共发布类型只按现有采用合同兜底。字段取精确发布指针，不读未确认草稿定义。
- 姓名、文本、数值、布尔、选项及字典树直接复用通用表单。不存在已发布类型时阻止选择采用，允许忽略并返回本书设置。
- 不属于当前表单的来源字段继续保留，提示数量并阻止界面采用；需要先调整规格，而不是擅自删除或推断新字段。
- 请求失败不重置当前填写；字段错误映射到表单。切换书籍时清除旧列表，并忽略旧请求的迟到响应。
- 普通页面的 5xx 提示引导重试与运行维护；原始错误仍留在 `ApiError.technicalDetail`，不把 Docker 构建和数据库堆栈散布在普通界面。4xx 保留具体校验／冲突指导。
- 创作概览最近任务读取正式合同名称与受控中文状态，不使用技术键作为作者标题。
- 不新增 SQL 或业务事实表。本切片不执行数据库恢复、清空或备份。

## 历史切片验证和剩余范围

编码完成后已执行 `pnpm --filter @ai-novel/new-design typecheck`，服务端构建及客户端类型检查通过；`node --test new-design/tests/unit.test.cjs new-design/tests/form-ai.unit.test.cjs new-design/tests/presentation.unit.test.cjs` 共 32 条通过。随后仅补充局部 CSS，静态审查排除了旧 JSON 编辑样式对通用表单的两列／等宽字体／复选框宽度干扰；使用既有主题变量，不新增主题体系。此切片未执行研究提案的浏览器交互及真实采用 API 验收；类型化表单原有行为通过共用组件复用，但不冒充本页面动态验收。

仍需盘点上下文的来源对象／版本选择、运行参数、模板冲突、拆书字段、章节结算、多维值显示及管理界面的折叠技术信息。研究批次正式采用的服务端树校验与来源快照已补齐，实际证据及未执行项见 [研究采用安全](research-adoption-tree-safety.md)。本记录不是 P1-1 全量完成证明。

相关：[执行队列](p1-p2-execution-queue.md)、[通用表单 AI 辅助](business-form-ai-assist.md)。

## 2026-09-17 当前实现静态盘点

下表是读取当前消费者代码后的状态，不是运行验收。上面的类型检查与 32 条用例是历史切片证据，不能覆盖随后新增的页面与恢复操作。本轮没有执行测试、构建、类型检查、迁移、浏览器或运行时请求。

| 作者入口 | 当前真实实现 | 仍需补齐的作者动作 |
| --- | --- | --- |
| 上下文管理 | 原来源对象／版本／作用范围已有中文控件；“运行前确认”按 exact 发布输入规格类型化，unsupported 规则折叠显示并阻执行，安全检查点只放高级区。原 frozen task version 的 inputSchema 同 reader 读取，旧 ready 也经同 validator，不能借目前合同绕过；输入／提交原键持久保留、只读核对端点/API真实挂载在研究恢复前。 | 完整冻结输入／回票末校验仍静态收口；普通规则的保存、采用、归档不冒称全站unknown闭环。所有运行验证未执行。 |
| 开书模板 | `TemplateGroupsPage.tsx` 已保存草稿、发布不可变快照、显示安全追加与冲突，并显式应用升级。 | 新模板仍暴露可编辑稳定键；冲突显示内部类型键与字段键。切模板没有草稿确认与版本读取代次保护，迟到的版本列表可能覆盖当前模板版本。 |
| 创作表单设计 | `FormDesignerPage.tsx` 已隐藏资料位置稳定键，以 `CardTypeTreePicker` 选择中文内容类型，关联选项按正式关系端点限制；局部补充字段显示名称。 | 切表单、重新加载仍可能覆盖未保存草稿；保存／发布失败尚未接原操作核对。不要重新引入英文允许类型输入。 |
| 拆书与诊断 | `BookAnalysisPage.tsx` 已用中文用途、预设、文本版本、范围和预算；候选名称、字段、选项与字典路径读取当前已发布规格，原键／值在折叠只读区域保留。历史读取与轮询按原报告和版本控制迟到响应；切报告清当前选择，原版本勾选可明确恢复，分析参数填写不被重置。 | 启动／采用失败仍需完整原操作凭证接线，本轮没有伪称这些动作已经可安全重发；当前规格标签不证明历史候选与最新规格兼容。 |
| 市场雷达 | `MarketRadarPage.tsx` 已按真实平台分组扫描源；“按当前选择新建 AI 分析”与“明确重跑原输入”分开，重跑校验精确扫描版本、作品顺序、关注点与预算。原研究版本的 `sourceScope` 保存请求键与完整输入哈希，同键已保存返回原回执不再次执行；未知分析持久保留原输入，按钮只读核对原键及确切版本。切扫描与轮询有来源代次保护。 | 中央 API／路由由主任务接线；原分析尚未读到记录不证明未执行，不能解除未知锁或新调用。旧扫描及信号保存的历史命令恢复不属于本次分析最小补丁，仍需单独核对。 |
| 参考包与书内研究提案 | `ReferencePacksPage.tsx` 已严格消费单个书籍／批次 UUID，并只选真实已加载同正本；切书清旧列表，迟到结果丢弃；`AdoptionItemEditor` 回传草稿脏状态，切批次与刷新保护未保存填写。 | 失败位置只保留书籍／批次／提案标识，未保留原采用请求键与完整载荷；未知正式采用后按钮仍可能重新生成键。应在原来源核对，不能把“打开原批次”当成已确认未写入。 |
| 通用业务表单与字典树 | `DynamicForm.tsx` 已按正式字段类型、中文名称、可见条件和默认值编辑；树选择实际消费范围规则。`DictionaryTreeField` 已加字段／字典来源代次保护、禁用和忙状态防双写；原 UUID 与中文输入在浏览器持久保留，只按原 UUID 接受回执，未知时仅核对原字典。选择控件按原排序值传入节点；已保存但不可选或字段被改动时不覆盖当前字段。 | 原节点未找到不证明未保存，保持未知锁；没有用同名节点或重新生成节点绕过核对。本轮未扩字典业务正本或其他整树命令合同。 |
| 本书关系配置 | `relationConfiguration/index.tsx` 已中文选择正式内容类型、关系属性、结算能力与维度；草稿先保存、再明确发布本书范围，切换既有关系需逐项确认；未知原键只读核对，填写与凭证持久保留。 | 本轮没有发现需要重建自由文本关系编辑的缺口；不得以系统参考规格覆盖本书正式属性。当前交互与并发边界仍待集中验证。 |
| 章节变化与结算 | `chapterWriting/AdoptionSettlementPanel.tsx` 和 `settlementEditing` 已选择正式对象／字段、类型化前后值、冻结字典与正文证据；人工与 AI 进入同一核对清单。原键核对、旧结果导入、运行回执完成、保留旧结果结束和过期未知结束均有实际按钮。 | 已保存结果不等于已结算，未知用量不能视为零；当前代码存在恢复路径，不应重复增加模板提案或自动确认。各恢复动作与租约仍待集中验证。 |
| 开书 AI 与正式关系／规划审阅 | `bookCreation/ReviewForm.tsx` 已保留原键，按原请求只读核对；候选逐字段显式采用，普通资料和正式关系／规划审阅同源，保存草稿后才准备。 | 读取模型配置只是确认配置存在，不证明实际模型执行通过；候选、采用与最终安装是不同回执。当前全阶段验证尚未执行。 |
| 知识语义索引与检索 | 原知识与参考页实际挂载 `knowledgeIndex` 四步中文工作流：明确连接版本与维度、保存规格、准备真实解析正文分块、逐块生成并启用原索引分代后按意思查询。关键词入口与语义入口分开；原模型回复保留在原尝试／运行字段或精确本地证据，未知不按零用量处理。原键与完整来源／规格／连接／分代身份持久保留，已有尝试绝不再调用；初始领取未知可经确认按同键核对恢复，只有同锁证明未领取的 winner 可能首次调用。 | 070 仅编码注册未应用，真实向量模型／pgvector／页面与恢复均未验证；旧凭证缺完整身份或存储损坏仍锁写并导航运行维护，不补成当前版本。实际本地回复证据尚未产生或备份，不冒称全站恢复通过。 |

### 必要补缺边界

优先补精确来源与未知结果治理，再处理作者输入和标签；不是给所有页面增加一个通用“重试”按钮。

> 白话理解：付款断网时，先查原订单是否扣款，再决定下一步，不能直接另建订单再付一次。对应到这里：保留原请求键、完整原提交与回执来源；返回空列表不证明原写入失败。

> 速记方法：先定位、再核对、后行动。明确未写入可重新准备；结果未知只核对；旧结果已保存只恢复旧结果，不再生成。

- 通用字典子控件：只能按新节点原 UUID 接受回执；不得按同名项代替。来源切换与禁用状态必须阻止旧请求回写另一表单，不改字典业务正本和已有树规则。
- 雷达：当前选择应形成新的冻结输入；原版本重试必须明示继续使用旧输入，两者不能复用一个含糊动作。
- 上下文：运行参数需要对应真实任务的类型化输入与来源选择，不能用中文标题包住必须填写的 JSON。冻结内部内容可以折叠查看，但不是作者编辑入口。
- 研究与模板：已保存版本、候选选择和草稿需要作用域／代次保护；失败恢复需先确认现有服务合同能按原键读取回执，不能只保留一条错误文字后重新发新键。

本轮先静态报目标并获得分配，再只修通用字典子控件、雷达分析来源与原键核对、拆书标签与报告作用域；没有修改上下文模块、唯一执行队列或新增 SQL。`research-input-review.unit.test.cjs` 编写了 7 项范围比较、哈希、中文显示和原请求回归用例，未执行。原分析执行继续使用既有服务，本补丁不引入新模型、凭据、提示词或试运行事实。
