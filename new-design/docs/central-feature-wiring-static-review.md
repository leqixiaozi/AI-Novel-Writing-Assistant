# 本批中央接线静态记录

更新：2026-09-19。本记录仅描述源码审阅，不代表构建、测试、迁移、模型或页面运行通过。

## 全批最终补差静态节点

人物倾向以原谈话输出保存结构化草稿，明确决策追加不可变原回执与确切正常GUIDE版本；后续冻结只接受真实章内人物、有效来源和窗口，原指导只读链接按版本ID查找，缺失不回退当前版。公共标题使用真实公共资料范围及独立受控合同，原安全回复、Task/Step/Attempt及明确库／书名选择承载恢复；库与记录按真实游标分页，不把历史数量作为新操作门禁。

本书历史保存确切正式规划／采用正文和完整当前指纹，准备恢复时锁原对象、先保存当前、复用原规划采用及正文复制作者写法；目录和实际稳定版本逐项核对后显示完成，不复制正式领域事实或stable标志。手工资料复用原表单；未知规划完整原帧、原键、输入哈希和版本回执核对，已保存资料GET失败／迟到只读带回保持父草稿。102/103/104仅手动默认关闭，默认81与手动20清单一致。必要函数保持原OID和原守卫，没有以临时字段猜正式来源。

协调任务已指出确切GUIDE链接曾误入编辑当前版，现改只读原版本并取得独立页面证据。上述源码节点与最终隔离／页面／作者未验状态均分开记录在[逐页施工记录](legacy-page-replication-progress.md#2026-09-19-全部批次统一收口结果)。下方早期描述保留为历史。

## 已接的实际入口

创作提炼和标题使用本书概览的专项链接；本书、模式必须明确。视觉资产使用专属图片生成接口，不借文字默认模型。专业图形通过本书导航打开，接口由 `createFeatureApi(request)` 注入中央 API，不在旧项目另写一份。导演总控台通过独立导航及创作首页链接打开，只读聚合原记录并回原来源页操作。

人物对话的受控 Prompt、模型任务、合同快照、HTTP、类型化 API、本书导航和页面已接，来源语义、发送前校验与恢复尾项最终静态收口仍在进行。SQL 077..081、083 仅登记；082 未新增文件，不假称迁移已应用。

> 白话比喻：大厅的指路牌连到真实窗口，但装好指路牌不等于窗口已经营业验收。对应到系统：路由与类型化传输是接线，运行验证还需全批编码后单独执行。

> 速记方法：看源码叫接线，跑过才叫验证；各窗口仍看原账。

## 必要保护

知识段落携带精确原件、解析版本、全文校验、分块和字位。来源快照与分块必须同书、同规格且有真实有效依赖状态；旧缺状态不自动提升。只在真实新建来源／分块时初始化状态。实际参考消息使用原正文的精确截取，失效不回退全文。已选其他范围须点击显式替换按钮，页面展示实际范围。

人物模拟的模型快照核对原会话、精确截至章、本人档案，以及会话内本人私有认知／关系／状态；其他参与者只提供公开标识和姓名。不能把当前关系规格的更新时间当成历史版本证明，缺历史证据必须真实阻断。

快照也核对原会话情境及此前成功回合的实际公开发言，不接受另一本书或另一个沙盘拼入的对话。规划运行／审阅记录的来源链接改为原规划页实际支持的 `?plan=UUID`，不放宽任意 hash 导航。

恢复链接仅接收固定新设计路径、合法 UUID 与对应中文操作名，不接受任意 URL。人物回合保留会话／回合参数；世界资料正常保存与质量修复登记分别核对，登记失败不能冒充原资料保存失败。

总控台没有跨来源合法批量恢复合同，按钮保持禁用，不批量重发模型、采用或取消任务。运行记录保持只读。

## 待统一验证

中央接口映射用例已编写，未执行。全部 0..8 编码、必要修复和静态审阅完成后，再统一检查类型、构建、明确选择的单位与 HTTP 用例、隔离 PostgreSQL 迁移和恢复链。用户 5173／5301／55432、正式附件、凭据与历史备份未操作；没有本批真实模型或浏览器通过证据。
