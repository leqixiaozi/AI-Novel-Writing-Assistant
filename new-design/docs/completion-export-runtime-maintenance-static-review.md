# F8 静态审查记录

审查覆盖 044 表/约束/触发器/索引/种子、030 Outbox 桥接、完本来源读取、快照时效、连续稳定范围、409 漂移保护、三种导出生成器、私有目录约束、页面路由/主题/移动布局，以及 001—044 注册表和运行包规格。

安全边界：正式导出读取精确正文版本；manifest 和产物只追加；请求只允许触发器首次绑定 job；生成目标位于私有目录并拒绝路径穿越、符号链接和覆盖；维护接口不展示连接串、密码或绝对路径；旧 Prisma/SQLite 不参与新设计写入。

静态审查不能证明真实 PG17 扩展可加载、迁移可在真实库完整执行、DOCX 可由目标办公软件打开、后台进程可在崩溃后恢复，也不能证明安装器、备份恢复、中文长路径和大书性能。上述项目必须保留为 Release Gate 的未执行、失败或阻塞状态。

## 本批静态审查与误触记录

- 已核对迁移编号 001—044 连续、运行包规格 JSON、路由/调用链、路径边界和提交差异；动态验证未作为本阶段验收结论。
- 本批曾误触 TypeScript、单元和 PostgreSQL 集成入口。其有限输出不能代表 F8 或 Release Gate 通过：其中单元入口曾显示 21 项通过；PostgreSQL 入口因私有运行包 manifest 未装配返回 503，并按安全设计没有回退到系统 PostgreSQL。
- 真实迁移、后台恢复、三种文件打开、主题终端、安装包、备份恢复和大书性能均未执行，继续以 Release Gate 的未执行或阻塞状态管理。
