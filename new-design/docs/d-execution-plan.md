# D：世界库与本书世界执行计划

2026-09-18，经用户要求使用第三方 Product Design 的 get-context 工作流重新规划，随后用户授权执行。依据项目 AGENTS.md、旧版 NovelWorldHandbookDialog 的 SyncPanel／SourceAndLibraryPanel 及来源选择组件。用户上下文预检未发现保存资料，沿用本会话已明确的旧版功能与布局要求。

目标：本书世界 → 公共候选与首次发布 → 固定版本导入另一书 → 查看差异 → 明确拉取或推送候选 → 明确发布 → 另一书主动拉取。旧版六分区、紧凑布局和实际操作入口保持。每步完成接口与对应页面操作，不用基础实现代替完整功能。

| 任务 | 范围 | 完成条件 | 状态 |
| --- | --- | --- | --- |
| D0 | 当前编译、值合同及基础问题 | 双端编译和实际基础检查通过 | 执行中 |
| D1 | 首次保存本书世界到世界库 | 实际选择公开内容、候选、明确发布，不依赖测试预置公共包 | 待执行 |
| D2 | 六分区资料、字段、字典与完整网络导入 | 另一书独立创建，不按同名合并、不丢项 | 已有基础，待完整验证 |
| D3 | 三方拉取，含新增／移除对象、字段与关系 | 逐项保留、采用、转换，只保存明确选择 | 已有字段实现，待完整闭合 |
| D4 | 推送候选、独立公开发布、同步基线 | 公共新增固定版本，其他书不自动改写 | 已有实现，待业务验证 |
| D5 | 旧版来源创建／生成／更换、同步与历史页面 | 入口逐个对应真实功能，密度与操作顺序对照旧版 | 待完整复刻验证 |
| D6 | 离页保护、刷新、响应丢失、跨标签页 | 完整填写与原请求恢复，只读核对，不重复提交 | 已有恢复实现，待验证 |
| D7 | 集中完整流程、必要异常、备份恢复、文档、提交推送 | 代码及隔离操作闭合，可审核交付 | 待执行 |
| D8 | 当前服务与作者数据生效 | 在已有授权边界内单独安排并核实页面操作 | 未授权数据安装，不自动执行 |

D内依赖保持D0→D1→D2→D3→D4→D5→D6→D7，D8单独受在线生效授权约束。整体顺序已由最新授权调整为G接线后继续D及其余差项、最后统一验证；早期D先于E–J不再作为门禁。已完成C直接复用，自己执行，不使用子agents。

只在新建保留隔离库安装、启用和验证。现有作者服务与开发／用户库不重启、不迁移、不 seed、不写入；不调用真实在线模型。手动合同不登记普通启动。保留私有扩展、事实、状态、认知、正文、历史及用户未跟踪文件。

当前基础证据：字典完整版本快照修复后 3 项定向基础检查通过；保留 nd_reference_test_9b5712b528394069b375bf5b8dd01808 与恢复库 nd_reference_test_8d49e6f3353b47c3ab2949e01ed1b61d。备份 world-package-96e2cc823b9d4453862d5fe7f90fbac2.dump（2440080 字节）位于 C:/tool/codex-home/tmp/world-package-backups。后续完整网络及同步新增守卫尚未据此验收。D 尚未提交、推送、在线加载或用户验收。

## 当前执行记录（2026-09-18）

用户要求本轮只写代码、不测试，随后要求优先修复 bug，让页面可见。本轮补写新增对象与关系同步、实际字段及属性转换、发布后的双方确认基线、完整同步历史分页、原请求恢复及原生世界创建／AI 候选入口。新增逻辑未运行编译或测试，不能复用此前验证结果作为本轮通过证据；D 完整功能、复刻验收与在线启用仍未完成。

页面加载阻塞已定位为世界合同在浏览器 ESM 下的循环导入。基础合同移入 base.ts，公共库／同步合同不再反向引用汇总入口；草稿规格覆盖使用 safeExtend 保留原校验。当前 5273 浏览器标签页刷新后实际显示新版内容与原侧栏菜单。本次只处理当前页面可见性，没有运行测试、数据库迁移或服务重启，不将页面显示作为同步功能完成证据。
