# U6 上下文装配静态审查

审查日期：2026-09-16。

> 🏠 **白话比喻**：静态审查像开车前检查图纸、钥匙和仪表，不等于真正上路。对应到本批：代码类型、SQL 合同、路由边界和界面文案经过检查，但没有启动 PostgreSQL、服务、浏览器或模型执行。

> 🧠 **速记方法**：**类型看接线，SQL 看护栏，路由看门禁，动态验证看真车。**

## 已检查

- 服务端与客户端 TypeScript 使用 `tsc --noEmit` 静态检查通过。
- migration 037 已纳入迁移注册、运行时 manifest 与运行包清单，运行范围连续到 037。
- 规则版本与选择器不可变，正式运行快照及子项不可变；修改采用 expected revision、幂等键和归档语义。
- 规则条件只接受白名单结构，并在 API 与数据库限制最大深度和数量；未开放任意 SQL、JSONPath、Cypher、JavaScript 或提示词表达式。
- 预览逐项记录确切版本、优先级、装配位置、资料角色、Token、决定、原因和层级；必须事实超预算或确切必须来源不可用时失败关闭。
- 正式 finalize 在事务中冻结任务合同、提示词配方、模型路由、规则版本、来源决定和既有检索轨迹；未复制资料正文、向量、提示词正文或密钥。
- 读取预览和运行快照要求书籍范围；侧栏、移动导航与新设计路由都包含“高级设置／上下文管理”。
- `git diff --check` 未发现空白错误；客户文案使用业务词，高级模式才显示版本和哈希。

## U1—U6 静态收口

- U1：卡片内核、内置小说资料规格、模板与仙侠生产样例。
- U2：动态业务表单、书籍任务视图、章节正文与事实／状态／知情／时间／规划版本链。
- U3：提示词配方、任务合同、模型路由、通用 AI 任务与质量审计账本。
- U4：依赖失效、附件、AGE 投影、pgvector 检索、Outbox、传输备份与私有运行时合同。
- U5：字段作用域、关联局部值、标签／分组／智能视图和安全归档。
- U6：上下文绑定、白名单激活规则、装配预览、运行快照与来源追溯。

## 未执行的动态验证

按本轮要求未运行单元测试、集成测试、构建、服务、浏览器、真实 PostgreSQL 迁移、AGE、pgvector、AI、打包、备份或恢复。migration 037 的实库执行、接口事务行为、主题视觉和端到端运行仍属于 F2／Release Gate 验证项。
