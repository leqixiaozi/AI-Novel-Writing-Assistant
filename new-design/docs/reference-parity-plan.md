# 人物与世界资料功能实施清单

授权范围：新版代码、页面操作、合同、可审核迁移文件、隔离库验证、文档与功能分支提交。禁止开发／用户库迁移、seed、重置、发布安装定义、指定旧书应用和真实在线模型调用。旧版 client/server/shared 不修改。不使用子 agents。

## A：人物规格与原请求回执

- [x] 可选性别、背景、成长方向规划、当前行动目标、起始处境草稿；长期目标稳定键保留。
- [x] 发布规格／表单／冻结模板版本与逐书新增差异预览，既有值不回填。
- [x] 字段扩展完整输入哈希、原结果不可变回执；同键不同输入及旧不完整回执保持 unknown。
- [x] 隔离事务／历史／跨书测试、noEmit、文档、提交推送。

## B：关联表单安装与升级

- [x] 人物／世界实际已发布类型适配的可选槽位。
- [x] 明确源版本、映射、预览、安装升级命令与书内快照；保留旧挂载与历史。
- [x] 明确活跃表单选择，关联不等于持有／生成上下文。
- [x] 并发／未知提交／局部输入保护测试、文档、提交推送。

## C：候选工作流

- [x] C1：外显准备／调整结构化批次，已填写字段默认不采用覆盖；单项标准表单保存、明确批量写入与原请求恢复。
- [ ] 确切采用正文的资源回填候选，人工确认后复用结算。
- [ ] 有来源经历／时间候选与 review，不将规划写成事实。
- [ ] 精确角色版本导入映射预览与创建，不按重名合并、不携原书状态认知持有。
- [ ] 单人物／单章／单来源到完整批次的隔离验证、文档、提交推送。

## D：冻结世界包与可恢复双向同步

- [ ] 根、冻结版本集合、安装映射、原请求回执迁移文件及备份恢复方案；普通启动不自动应用。
- [ ] pull 三方差异逐项选择；push 公共候选、明确发布；多书独立。
- [ ] 保护扩展、历史、正式事实、并发及未知结果；隔离迁移／恢复验证、页面操作、文档、提交推送。

A 验证：服务端编译、新版客户端与旧宿主客户端 noEmit 通过；24 项定向检查通过（含独立空数据库完整迁移与真实事务、丢失提交响应／刷新失败原凭证保护）。5273 旧与新健康接口均 200。页面验收由用户完成；未在用户库发布／安装／应用，未调用真实模型。

实际开发库只读核实：001–083 已登记（遵循项目现有跳号），071 原请求回执表存在。后续迁移只在独立新测试库验证。代码完成、用户库实际生效、真实 AI 验收分别报告。

B 验证：服务端独立测试目录编译与新版客户端 noEmit 通过；31 项定向验证通过，含 3 个新空数据库的完整真实迁移、人物／世界关联发布、逐书版本映射、精确字典节点与独立局部选项、保留旧实例及历史、并发过期预览、跨书／不同输入／历史不足回执、只读 HTTP 核对、丢失响应／刷新失败／迟到响应保护。071 足以持久完整原回执，无新增迁移。5301 无 watch 服务尚未加载 B，未重启；未在用户库发布或安装，UI 与在线模型未验收。

C1 验证：服务端独立编译与新版客户端 noEmit 通过；55 项定向检查通过，含独立空数据库的外显规格发布、逐书安全字段同步、实际历史表单选择、受控结构化候选、选择字段后的正常人物保存与 AI 来源、批量第二项失败整体回滚、同原请求并发只保存一批、丢失提交响应只读恢复、关联回归及客户端恢复。未新增迁移；5301 无 watch，未在线加载 C1，未修改用户书籍、调用真实模型或执行浏览器验收。C 整体未完成，资源回填、经历和公共角色导入继续进行。

后续 E→F→G→H→I→J 同属既有授权队列，见 [参考工作流实施队列](reference-workflow-queue.md)。各阶段独立报告代码、隔离验证、在线加载与用户验收，未完成项不据通用表单表达能力标记等价。

C2a：资源账本明确选真实人物→道具关系和状态维度，仅展示通过原来源校验的正式状态；可编辑会话的确切采用正文回填候选经过原人工确认与结算。22项定向检查通过，新空库验证实际受控领取、stub模型、原请求、防篡改、只读HTTP和085守卫回退；双端编译通过。085为普通启动不注册的手动迁移，未改用户库或在线安装。稳定章节补充结算、批量回填和资源策划字段仍未完成，C2不勾选完成。

C3a：明确小传字段的单人／最多20人候选冻结真实原文字位、人物与事件版本；人工选择实际事件，载入正常时间编辑器检查、预览、保存并单独审核。候选保持planned，保留原AI出处，不伪造正文证据。双端编译和32项定向检查通过，含独立空库正常时间／关系／叙述位置操作、完整原请求恢复、并发与未知结果保护。无新增迁移、未加载在线、未真实模型或UI验收。最近正文同步和超过20人的自动分批仍待完成，C整体不勾选。
