import { useEffect, useMemo, useState, type Dispatch, type SetStateAction } from "react";
import type { BookSummary, ContextActivationField, ContextActivationOperator, ContextActivationRule, ContextBinding, ContextBindingVersion, ContextManifest, ContextPreview, ContextScopeKind, ContextSelectorKind, ContextSnapshotSummary, ContextSourceSelector, ContextSourceType, TaskContract } from "../common/contracts";
import { ApiError, newDesignApi, type ContextBindingVersionInput } from "./api";
import StructureShell from "./StructureShell";

type Tab="rules"|"preview"|"snapshots";
type Draft=ContextBindingVersionInput&{bindingKey:string;name:string;description:string;scopeKind:ContextScopeKind;scopeRef:string;spaceId:string;selectorKind:ContextSelectorKind;selectorSourceType:ContextSourceType;selectorPrimary:string;selectorSecondary:string;selectorRangeEnd:number};
const emptyRule:ContextActivationRule={kind:"group",operator:"and",items:[]};
const emptyDraft:Draft={bindingKey:"book.core_context",name:"本书核心资料",description:"为创作任务提供人物、世界与事件等本书资料。",scopeKind:"book",scopeRef:"",spaceId:"",inheritanceMode:"inherit",activationRule:emptyRule,slotKey:"book_context",priority:500,contentRole:"reference",tokenBudget:6000,trimStrategy:"lowest_priority",dedupeStrategy:"stable_source",selectors:[],selectorKind:"card_type",selectorSourceType:"card_version",selectorPrimary:"character",selectorSecondary:"",selectorRangeEnd:100};
const scopeLabels:Record<ContextScopeKind,string>={system:"系统默认",public:"公共资源",task_group:"任务组默认",task_node:"任务节点",book:"本书",volume:"卷",chapter:"章",scene:"场景",one_time:"本次调整"};
const decisionLabels:Record<ContextPreview["decisions"][number]["decision"],string>={included:"已选入",excluded:"已排除",deduped:"已去重",trimmed:"已裁剪"};
const uuid=()=>crypto.randomUUID();

function draftFrom(binding:ContextBinding):Draft {const version=binding.currentVersion,first=version.selectors[0],config=first?.config??{},primary=config.typeKey??config.tagId??config.smartViewId??config.relationTypeId??config.packVersionId??config.retrievalRunId??config.start??first?.stableObjectId??"",secondary=first?.exactVersionId??"";return{bindingKey:binding.bindingKey,name:binding.name,description:binding.description,scopeKind:binding.scopeKind,scopeRef:binding.scopeRef??"",spaceId:binding.spaceId??"",inheritanceMode:version.inheritanceMode,activationRule:version.activationRule,slotKey:version.slotKey,priority:version.priority,contentRole:version.contentRole,tokenBudget:version.tokenBudget,trimStrategy:version.trimStrategy,dedupeStrategy:version.dedupeStrategy,selectors:version.selectors.map(({id:_,...selector})=>selector),selectorKind:first?.selectorKind??"card_type",selectorSourceType:first?.sourceType??"card_version",selectorPrimary:String(primary),selectorSecondary:String(secondary),selectorRangeEnd:Number(config.end??100)};}
function buildSelectors(draft:Draft):Array<Omit<ContextSourceSelector,"id">>{const common={sortOrder:0,stableObjectId:null,exactVersionId:null};switch(draft.selectorKind){case"explicit_source":return[{...common,selectorKind:"explicit_source",sourceType:draft.selectorSourceType,stableObjectId:draft.selectorPrimary,exactVersionId:draft.selectorSecondary,config:{}}];case"prompt_component":return[{...common,selectorKind:"prompt_component",sourceType:"prompt_component",stableObjectId:draft.selectorPrimary,exactVersionId:draft.selectorSecondary,config:{}}];case"tag":return[{...common,selectorKind:"tag",sourceType:"card_version",config:{tagId:draft.selectorPrimary}}];case"smart_view":return[{...common,selectorKind:"smart_view",sourceType:"card_version",config:{smartViewId:draft.selectorPrimary,limit:100}}];case"relation":return[{...common,selectorKind:"relation",sourceType:"card_version",config:{relationTypeId:draft.selectorPrimary}}];case"story_range":return[{...common,selectorKind:"story_range",sourceType:draft.selectorSourceType==="story_time"?"story_time":"card_version",config:{start:Number(draft.selectorPrimary),end:draft.selectorRangeEnd}}];case"research_pack":return[{...common,selectorKind:"research_pack",sourceType:"research_version",config:{packVersionId:draft.selectorPrimary}}];case"retrieval_trace":return[{...common,selectorKind:"retrieval_trace",sourceType:"retrieval_chunk",config:{retrievalRunId:draft.selectorPrimary,limit:20}}];default:return[{...common,selectorKind:"card_type",sourceType:"card_version",config:{typeKey:draft.selectorPrimary}}];}}
function versionInput(draft:Draft):ContextBindingVersionInput{return{inheritanceMode:draft.inheritanceMode,activationRule:draft.activationRule,slotKey:draft.slotKey,priority:draft.priority,contentRole:draft.contentRole,tokenBudget:draft.tokenBudget,trimStrategy:draft.contentRole==="required"?"none":draft.trimStrategy,dedupeStrategy:draft.dedupeStrategy,selectors:draft.inheritanceMode==="exclude"?[]:buildSelectors(draft),createdBy:"user"};}
function sourceHref(bookId:string,sourceType:string):string {if(sourceType==="research_version")return"/new-design/research/reference-packs";if(sourceType==="body_version")return`/new-design/books/${bookId}/views/chapters`;if(sourceType==="story_time")return`/new-design/books/${bookId}/views/events`;return`/new-design/books/${bookId}/cards`;}

const activationFieldLabels:Record<ContextActivationField,string>={task_key:"任务",task_group:"任务组",content_type:"内容类型",tag:"标签",material_status:"资料状态",canonical_status:"事实状态",volume:"卷",chapter:"章",scene:"场景",story_range:"故事范围",relation_exists:"关系存在",association_exists:"挂载存在",source_type:"来源类型",stale:"资料失效",manual_switch:"手动开关"};
const activationOperatorLabels:Record<ContextActivationOperator,string>={equals:"等于",not_equals:"不等于",in:"属于",not_in:"不属于",contains:"包含",exists:"存在",not_exists:"不存在",gte:"大于等于",lte:"小于等于",between:"介于",enabled:"已开启"};
const selectorLabels:Record<ContextSelectorKind,string>={explicit_source:"明确资料版本",card_type:"内容类型",tag:"标签",smart_view:"智能视图",relation:"资料关系",story_range:"故事范围",research_pack:"研究资料包",prompt_component:"提示词组件",retrieval_trace:"既有检索轨迹"};
const explicitSourceTypes:ContextSourceType[]=["card_version","body_version","canonical_fact","knowledge_state_change","state_change","story_time","story_event_relation","research_document_version","planning_version","research_version","research_pack_version"];
type ActivationCondition=Extract<ContextActivationRule,{kind:"condition"}>;
const ruleConditions=(rule:ContextActivationRule):ActivationCondition[]=>rule.kind==="group"?rule.items.filter((item):item is ActivationCondition=>item.kind==="condition"):[rule];
const parseRuleValue=(operator:ContextActivationOperator,value:string):ActivationCondition["value"]=>operator==="enabled"?value!=="false":operator==="in"||operator==="not_in"||operator==="between"?value.split(",").map(item=>item.trim()).filter(Boolean):value;

function ContextRuleControls({draft,setDraft}:{draft:Draft;setDraft:Dispatch<SetStateAction<Draft>>}){
  const conditions=ruleConditions(draft.activationRule),groupOperator=draft.activationRule.kind==="group"?draft.activationRule.operator:"and";
  const setConditions=(items:ActivationCondition[],operator=groupOperator)=>setDraft(value=>({...value,activationRule:{kind:"group",operator,items}}));
  const updateCondition=(index:number,patch:Partial<ActivationCondition>)=>setConditions(conditions.map((item,itemIndex)=>itemIndex===index?{...item,...patch}:item));
  const selectorNeedsExact=["explicit_source","prompt_component"].includes(draft.selectorKind),selectorNeedsUuid=["tag","smart_view","relation","research_pack","retrieval_trace"].includes(draft.selectorKind);
  return <details className="nd-context-condition" open>
    <summary>参考资料与激活条件</summary>
    <p>选择资料入口，再用受控条件缩小范围。这里不接受脚本、查询语句或提示词表达式。</p>
    <div className="nd-context-form-grid">
      <label><span>资料入口</span><select value={draft.selectorKind} onChange={event=>setDraft(value=>({...value,selectorKind:event.target.value as ContextSelectorKind,selectorPrimary:"",selectorSecondary:""}))}>{Object.entries(selectorLabels).map(([key,label])=><option value={key} key={key}>{label}</option>)}</select></label>
      {(draft.selectorKind==="explicit_source"||draft.selectorKind==="story_range")&&<label><span>来源类型</span><select value={draft.selectorSourceType} onChange={event=>setDraft(value=>({...value,selectorSourceType:event.target.value as ContextSourceType}))}>{(draft.selectorKind==="story_range"?["card_version","story_time"]:explicitSourceTypes).map(type=><option value={type} key={type}>{type}</option>)}</select></label>}
      <label><span>{draft.selectorKind==="card_type"?"内容类型标识":draft.selectorKind==="story_range"?"起始位置":selectorNeedsExact?"资料标识":selectorNeedsUuid?"所选对象":"来源参数"}</span><input value={draft.selectorPrimary} onChange={event=>setDraft(value=>({...value,selectorPrimary:event.target.value}))} required={draft.inheritanceMode!=="exclude"} placeholder={selectorNeedsUuid||selectorNeedsExact?"UUID":"例如 character"}/></label>
      {selectorNeedsExact&&<label><span>确切版本</span><input value={draft.selectorSecondary} onChange={event=>setDraft(value=>({...value,selectorSecondary:event.target.value}))} required={draft.inheritanceMode!=="exclude"} placeholder="版本 UUID"/></label>}
      {draft.selectorKind==="story_range"&&<label><span>结束位置</span><input type="number" value={draft.selectorRangeEnd} onChange={event=>setDraft(value=>({...value,selectorRangeEnd:Number(event.target.value)}))}/></label>}
      <label><span>组合关系</span><select value={groupOperator} onChange={event=>setConditions(conditions,event.target.value as "and"|"or")}><option value="and">全部满足</option><option value="or">任一满足</option></select></label>
      <label><span>参考资料超限</span><select value={draft.contentRole==="required"?"none":draft.trimStrategy} disabled={draft.contentRole==="required"} onChange={event=>setDraft(value=>({...value,trimStrategy:event.target.value as Draft["trimStrategy"]}))}><option value="none">不裁剪</option><option value="lowest_priority">低优先级先移出</option><option value="largest_first">较大资料先移出</option></select></label>
      <label><span>重复资料</span><select value={draft.dedupeStrategy} onChange={event=>setDraft(value=>({...value,dedupeStrategy:event.target.value as Draft["dedupeStrategy"]}))}><option value="stable_source">按资料正本去重</option><option value="exact_version">按确切版本去重</option></select></label>
    </div>
    <div className="nd-context-condition-list">
      {conditions.map((condition,index)=><div className="nd-context-condition-row" key={`${condition.field}-${index}`}>
        <select aria-label={`条件 ${index+1} 字段`} value={condition.field} onChange={event=>updateCondition(index,{field:event.target.value as ContextActivationField})}>{Object.entries(activationFieldLabels).map(([key,label])=><option value={key} key={key}>{label}</option>)}</select>
        <select aria-label={`条件 ${index+1} 判断`} value={condition.operator} onChange={event=>updateCondition(index,{operator:event.target.value as ContextActivationOperator})}>{Object.entries(activationOperatorLabels).map(([key,label])=><option value={key} key={key}>{label}</option>)}</select>
        <input aria-label={`条件 ${index+1} 值`} value={Array.isArray(condition.value)?condition.value.join(","):String(condition.value??"")} disabled={["exists","not_exists"].includes(condition.operator)} onChange={event=>updateCondition(index,{value:parseRuleValue(condition.operator,event.target.value)})}/>
        <button type="button" aria-label={`删除条件 ${index+1}`} onClick={()=>setConditions(conditions.filter((_,itemIndex)=>itemIndex!==index))}>×</button>
      </div>)}
      <button className="nd-button nd-button-secondary" type="button" disabled={conditions.length>=40} onClick={()=>setConditions([...conditions,{kind:"condition",field:"task_key",operator:"equals",value:""}])}>＋ 添加激活条件</button>
    </div>
  </details>;
}

export default function ContextManagementPage(){
  const [books,setBooks]=useState<BookSummary[]>([]),[bookId,setBookId]=useState(""),[contracts,setContracts]=useState<TaskContract[]>([]),[bindings,setBindings]=useState<ContextBinding[]>([]),[previews,setPreviews]=useState<ContextPreview[]>([]),[snapshots,setSnapshots]=useState<ContextSnapshotSummary[]>([]);
  const [selectedId,setSelectedId]=useState(""),[selectedPreviewId,setSelectedPreviewId]=useState(""),[selectedSnapshotId,setSelectedSnapshotId]=useState(""),[snapshotDetail,setSnapshotDetail]=useState<ContextManifest|null>(null),[snapshotSources,setSnapshotSources]=useState<ContextPreview["decisions"]>([]);
  const [tab,setTab]=useState<Tab>("rules"),[advanced,setAdvanced]=useState(false),[draft,setDraft]=useState<Draft>(emptyDraft),[taskContractId,setTaskContractId]=useState(""),[taskNodeKey,setTaskNodeKey]=useState("chapter.write"),[totalBudget,setTotalBudget]=useState(12000),[message,setMessage]=useState(""),[conflict,setConflict]=useState(""),[busy,setBusy]=useState(false);
  const selected=bindings.find(item=>item.id===selectedId),selectedPreview=previews.find(item=>item.id===selectedPreviewId)??null,selectedContract=contracts.find(item=>item.id===taskContractId)??contracts[0]??null;
  const loadBase=async()=>{const [nextBooks,nextContracts]=await Promise.all([newDesignApi.listBooks(),newDesignApi.listPublishedTaskContracts()]);setBooks(nextBooks);setContracts(nextContracts);setBookId(current=>current||nextBooks[0]?.id||"");setTaskContractId(current=>current||nextContracts[0]?.id||"");};
  const loadBook=async(id:string)=>{if(!id){setBindings([]);setPreviews([]);setSnapshots([]);return;}const [nextBindings,nextPreviews,nextSnapshots]=await Promise.all([newDesignApi.listContextBindings({bookId:id,includeGlobal:true}),newDesignApi.listContextPreviews(id),newDesignApi.listContextSnapshots(id)]);setBindings(nextBindings);setPreviews(nextPreviews);setSnapshots(nextSnapshots);setSelectedId(current=>nextBindings.some(item=>item.id===current)?current:nextBindings[0]?.id||"");setSelectedPreviewId(current=>nextPreviews.some(item=>item.id===current)?current:nextPreviews[0]?.id||"");};
  useEffect(()=>{void loadBase().catch(error=>setMessage(error instanceof Error?error.message:"上下文管理加载失败。"));},[]);
  useEffect(()=>{void loadBook(bookId).catch(error=>setMessage(error instanceof Error?error.message:"本书上下文加载失败。"));},[bookId]);
  useEffect(()=>{if(selected)setDraft(draftFrom(selected));},[selectedId,selected?.currentVersionId]);
  useEffect(()=>{if(!bookId||!selectedSnapshotId){setSnapshotDetail(null);setSnapshotSources([]);return;}void Promise.all([newDesignApi.getContextSnapshot(bookId,selectedSnapshotId),newDesignApi.listContextSnapshotSources(bookId,selectedSnapshotId)]).then(([detail,sources])=>{setSnapshotDetail(detail);setSnapshotSources(sources);}).catch(error=>setMessage(error instanceof Error?error.message:"运行快照加载失败。"));},[bookId,selectedSnapshotId]);
  const tree=useMemo(()=>Object.entries(scopeLabels).filter(([key])=>advanced||["system","task_group","task_node","book","volume","chapter","scene"].includes(key)).map(([key,label])=>({key:key as ContextScopeKind,label,count:bindings.filter(item=>item.scopeKind===key).length})),[advanced,bindings]);
  const submit=async()=>{if(!bookId)return;setBusy(true);setMessage("");setConflict("");try{if(selected){const saved=await newDesignApi.addContextBindingVersion(selected.id,{...versionInput(draft),bookId:selected.bookId?bookId:null,expectedRevision:selected.revision,baseVersionId:selected.currentVersionId,idempotencyKey:uuid()});setBindings(items=>items.map(item=>item.id===saved.id?saved:item));setSelectedId(saved.id);setMessage("新规则版本已保存。采用后才会影响后续装配预览。");}else{const saved=await newDesignApi.createContextBinding({...versionInput(draft),bindingKey:draft.bindingKey,name:draft.name,description:draft.description,scopeKind:draft.scopeKind,scopeRef:["system","public","book"].includes(draft.scopeKind)?null:draft.scopeRef,bookId:["book","volume","chapter","scene","one_time"].includes(draft.scopeKind)?bookId:null,spaceId:draft.scopeKind==="public"?draft.spaceId:null,idempotencyKey:uuid()});setBindings(items=>[...items,saved]);setSelectedId(saved.id);setMessage("上下文规则已创建并采用首个版本。");}}catch(error){if(error instanceof ApiError&&error.status===409)setConflict(`服务器修订 ${error.issues.serverRevision??"未知"}，本地填写内容仍保留。请对照后再次保存。`);setMessage(error instanceof Error?error.message:"上下文规则保存失败。");}finally{setBusy(false);}};
  const adopt=async(version:ContextBindingVersion)=>{if(!selected)return;setBusy(true);try{const saved=await newDesignApi.adoptContextBindingVersion(selected.id,{bookId:selected.bookId?bookId:null,versionId:version.id,expectedRevision:selected.revision,idempotencyKey:uuid(),actor:"user"});setBindings(items=>items.map(item=>item.id===saved.id?saved:item));setMessage(`已采用版本 v${version.version}，后续预览将按此规则装配。`);}catch(error){if(error instanceof ApiError&&error.status===409)setConflict(`服务器修订 ${error.issues.serverRevision??"未知"}，本地填写内容仍保留。`);setMessage(error instanceof Error?error.message:"规则采用失败。");}finally{setBusy(false);}};
  const archive=async()=>{if(!selected)return;setBusy(true);try{const saved=await newDesignApi.archiveContextBinding(selected.id,{bookId:selected.bookId?bookId:null,expectedRevision:selected.revision,idempotencyKey:uuid(),reason:"高级设置中归档",actor:"user"});setBindings(items=>items.filter(item=>item.id!==saved.id));setSelectedId("");setDraft(emptyDraft);setMessage("规则已归档；来源资料与历史运行快照保持不变。");}catch(error){setMessage(error instanceof Error?error.message:"规则归档失败。");}finally{setBusy(false);}};
  const preview=async()=>{if(!bookId||!selectedContract?.publishedVersion)return;setBusy(true);setMessage("");try{const next=await newDesignApi.createContextPreview({bookId,taskKey:selectedContract.taskKey,taskGroup:selectedContract.publishedVersion.taskGroup,taskNodeKey,totalBudget,taskContractVersionId:selectedContract.publishedVersion.id,manualSwitches:{},oneTimeOverrides:{excludeSourceKeys:[],manualSwitches:{}},createdBy:"user"});setPreviews(items=>[next,...items]);setSelectedPreviewId(next.id);setMessage(next.status==="invalid"?"必须事实的预算或来源需要调整，正式运行入口保持关闭。":"装配预览已生成，未调用模型，也未写入正式运行。");}catch(error){setMessage(error instanceof Error?error.message:"装配预览失败。");}finally{setBusy(false);}};
  return <StructureShell title="上下文管理" description="用表单决定创作任务可以参考哪些资料，并在运行前核对确切版本、预算和裁剪原因。">
    <div className="nd-context-toolbar"><label htmlFor="context-book">当前书籍</label><select id="context-book" value={bookId} onChange={event=>setBookId(event.target.value)}>{books.map(book=><option value={book.id} key={book.id}>{book.name}</option>)}</select><button className={`nd-button ${advanced?"nd-button-primary":"nd-button-secondary"}`} type="button" aria-pressed={advanced} onClick={()=>setAdvanced(value=>!value)}>{advanced?"收起高级编辑":"展开高级编辑"}</button></div>
    {message&&<p className={`nd-message ${message.includes("失败")||message.includes("超过")?"is-error":""}`} role="status">{message}</p>}{conflict&&<div className="nd-context-conflict" role="alert"><strong>发现并发修改</strong><span>{conflict}</span></div>}
    <div className="nd-context-tabs" role="tablist" aria-label="上下文管理内容">{([['rules','上下文规则'],['preview','装配预览'],['snapshots','运行快照']] as const).map(([key,label])=><button type="button" role="tab" aria-selected={tab===key} className={tab===key?"is-active":""} onClick={()=>setTab(key)} key={key}>{label}</button>)}</div>
    {tab==="rules"&&<div className="nd-context-layout">
      <aside className="nd-context-scope-tree" aria-label="上下文作用范围"><div className="nd-panel-title"><div><span>作用范围</span><strong>规则树</strong></div><button type="button" aria-label="新建上下文规则" onClick={()=>{setSelectedId("");setDraft(emptyDraft);setAdvanced(true);}}>＋</button></div><ol>{tree.map(item=><li key={item.key}><button type="button" onClick={()=>setAdvanced(true)}><span aria-hidden="true">{item.count?"▾":"›"}</span><b>{item.label}</b><small>{item.count}</small></button>{item.count>0&&<ul>{bindings.filter(binding=>binding.scopeKind===item.key).map(binding=><li key={binding.id}><button type="button" className={binding.id===selectedId?"is-selected":""} onClick={()=>setSelectedId(binding.id)}><span>{binding.name}</span><small>{binding.adoptedVersion?`v${binding.adoptedVersion.version}`:"未采用"}</small></button></li>)}</ul>}</li>)}</ol></aside>
      <section className="nd-context-rule-list"><div className="nd-panel-title"><div><span>本书与默认规则</span><strong>{bindings.length} 条规则</strong></div></div>{bindings.length?bindings.map(binding=><button type="button" className={binding.id===selectedId?"is-selected":""} onClick={()=>setSelectedId(binding.id)} key={binding.id}><span><b>{binding.name}</b><small>{scopeLabels[binding.scopeKind]} · {binding.currentVersion.contentRole==="required"?"必须事实":"参考资料"}</small></span><em>{binding.currentVersion.tokenBudget.toLocaleString()} tokens</em></button>):<div className="nd-empty"><strong>还没有上下文规则</strong><span>展开高级编辑后，可以为本书创建第一条参考资料规则。</span></div>}</section>
      <section className="nd-context-editor" aria-label="上下文规则编辑器"><div className="nd-panel-title"><div><span>{selected?`修订 ${selected.revision}`:"新规则"}</span><strong>{selected?.name||"配置参考资料"}</strong></div>{selected&&<span className="nd-context-version-pill">采用 v{selected.adoptedVersion?.version??"—"}</span>}</div>{advanced?<form onSubmit={event=>{event.preventDefault();void submit();}}><div className="nd-context-form-grid"><label><span>规则名称</span><input value={draft.name} onChange={event=>setDraft(value=>({...value,name:event.target.value}))} required maxLength={160}/></label><label><span>稳定标识</span><input value={draft.bindingKey} onChange={event=>setDraft(value=>({...value,bindingKey:event.target.value}))} required disabled={Boolean(selected)} pattern="[a-z][a-z0-9_.-]+"/></label><label><span>作用范围</span><select value={draft.scopeKind} onChange={event=>setDraft(value=>({...value,scopeKind:event.target.value as ContextScopeKind}))}>{Object.entries(scopeLabels).map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></label>{!["system","public","book"].includes(draft.scopeKind)&&<label><span>范围对象</span><input value={draft.scopeRef} onChange={event=>setDraft(value=>({...value,scopeRef:event.target.value}))} required aria-describedby="scope-help"/><small id="scope-help">填写任务键或所选卷、章、场景的内部对象。</small></label>}{draft.scopeKind==="public"&&<label><span>公共资源空间</span><input value={draft.spaceId} onChange={event=>setDraft(value=>({...value,spaceId:event.target.value}))} required placeholder="空间 UUID"/></label>}<label><span>继承方式</span><select value={draft.inheritanceMode} onChange={event=>setDraft(value=>({...value,inheritanceMode:event.target.value as Draft["inheritanceMode"]}))}><option value="inherit">继承并补充</option><option value="override">覆盖同名规则</option><option value="exclude">在此范围排除</option></select></label><label><span>装配位置</span><input value={draft.slotKey} onChange={event=>setDraft(value=>({...value,slotKey:event.target.value}))} required pattern="[a-z][a-z0-9_.-]+"/></label><label><span>资料角色</span><select value={draft.contentRole} onChange={event=>setDraft(value=>({...value,contentRole:event.target.value as Draft["contentRole"]}))}><option value="required">必须事实</option><option value="reference">参考资料</option></select></label><label><span>优先级</span><input type="number" min={-10000} max={10000} value={draft.priority} onChange={event=>setDraft(value=>({...value,priority:Number(event.target.value)}))}/></label><label><span>Token 预算</span><input type="number" min={0} max={1000000} value={draft.tokenBudget} onChange={event=>setDraft(value=>({...value,tokenBudget:Number(event.target.value)}))}/></label></div><label className="nd-context-wide"><span>说明</span><textarea value={draft.description} onChange={event=>setDraft(value=>({...value,description:event.target.value}))} rows={3} maxLength={1000}/></label><ContextRuleControls draft={draft} setDraft={setDraft}/><div className="nd-context-actions"><button className="nd-button nd-button-primary" type="submit" disabled={busy}>{selected?"保存新版本":"创建并采用"}</button>{selected&&selected.currentVersionId!==selected.adoptedVersionId&&<button className="nd-button nd-button-secondary" type="button" disabled={busy} onClick={()=>void adopt(selected.currentVersion)}>采用当前版本</button>}{selected&&<button className="nd-button nd-button-danger" type="button" disabled={busy} onClick={()=>void archive()}>归档规则</button>}</div></form>:<div className="nd-context-readonly">{selected?<><dl><div><dt>作用范围</dt><dd>{scopeLabels[selected.scopeKind]}</dd></div><div><dt>资料角色</dt><dd>{selected.adoptedVersion?.contentRole==="required"?"必须事实":"参考资料"}</dd></div><div><dt>装配位置</dt><dd>{selected.adoptedVersion?.slotKey}</dd></div><div><dt>预算</dt><dd>{selected.adoptedVersion?.tokenBudget.toLocaleString()} tokens</dd></div></dl><p>{selected.description||"未填写规则说明。"}</p></>:<div className="nd-empty"><strong>选择一条规则</strong><span>普通查看模式不会暴露内部版本和规则结构。</span></div>}</div>}
        {selected&&<details className="nd-context-history"><summary>版本历史</summary>{selected.versions.map(version=><article key={version.id}><span><b>v{version.version}</b><small>{version.id===selected.adoptedVersionId?"使用中":"候选版本"} · {new Date(version.createdAt).toLocaleString()}</small></span>{advanced&&version.id!==selected.adoptedVersionId&&<button type="button" disabled={busy} onClick={()=>void adopt(version)}>采用</button>}{advanced&&<code title="追溯哈希">{version.contentHash.slice(0,12)}</code>}</article>)}</details>}
      </section>
    </div>}
    {tab==="preview"&&<div className="nd-context-preview-layout"><section className="nd-context-preview-form"><div className="nd-panel-title"><div><span>运行前检查</span><strong>装配预览</strong></div></div><div className="nd-context-form-grid"><label><span>创作任务</span><select value={taskContractId} onChange={event=>setTaskContractId(event.target.value)}>{contracts.map(contract=><option value={contract.id} key={contract.id}>{contract.name}</option>)}</select></label><label><span>任务步骤</span><input value={taskNodeKey} onChange={event=>setTaskNodeKey(event.target.value)} aria-describedby="task-node-help"/><small id="task-node-help">例如章节写作或人物补全。</small></label><label><span>总预算</span><input type="number" min={1} max={1000000} value={totalBudget} onChange={event=>setTotalBudget(Number(event.target.value))}/></label></div><button className="nd-button nd-button-primary" type="button" disabled={busy||!selectedContract} onClick={()=>void preview()}>生成装配预览</button><p className="nd-context-note">预览只检查参考资料，不调用模型，也不会成为正式运行记录。</p><div className="nd-context-preview-history">{previews.map(item=><button type="button" className={item.id===selectedPreviewId?"is-selected":""} onClick={()=>setSelectedPreviewId(item.id)} key={item.id}><span><b>{item.taskNodeKey}</b><small>{new Date(item.createdAt).toLocaleString()}</small></span><em className={`is-${item.status}`}>{item.status==="complete"?"可运行":item.status==="invalid"?"需调整":item.status==="stale"?"需重新预览":"已超时"}</em></button>)}</div></section><section className="nd-context-preview-result">{selectedPreview?<><div className="nd-context-budget"><header><div><span>预算使用</span><strong>{selectedPreview.includedTokens.toLocaleString()} / {selectedPreview.totalBudget.toLocaleString()}</strong></div><em>{selectedPreview.remainingTokens>=0?`剩余 ${selectedPreview.remainingTokens.toLocaleString()}`:`超额 ${Math.abs(selectedPreview.remainingTokens).toLocaleString()}`}</em></header><progress max={selectedPreview.totalBudget} value={Math.min(selectedPreview.includedTokens,selectedPreview.totalBudget)}>预算使用</progress><dl><div><dt>必须事实</dt><dd>{selectedPreview.requiredTokens.toLocaleString()}</dd></div><div><dt>参考资料</dt><dd>{selectedPreview.referenceTokens.toLocaleString()}</dd></div><div><dt>候选</dt><dd>{selectedPreview.candidateCount}</dd></div><div><dt>选入</dt><dd>{selectedPreview.includedCount}</dd></div></dl></div>{selectedPreview.status==="invalid"&&<div className="nd-context-stop" role="alert"><strong>正式运行入口已关闭</strong><span>必须事实的预算或来源需要调整，系统不会静默删除关键资料。请查看逐项原因后重新预览。</span></div>}<div className="nd-context-decisions">{selectedPreview.decisions.map(decision=><article className={`is-${decision.decision}`} key={decision.id}><span className="nd-context-decision-state">{decisionLabels[decision.decision]}</span><div><h3>{decision.sourceLabel}</h3><p>{decision.reasonDetail}</p><small>{decision.layerLabel} · {decision.contentRole==="required"?"必须事实":"参考资料"} · {decision.tokenEstimate} tokens</small></div><a href={sourceHref(bookId,decision.sourceType)}>查看来源</a>{advanced&&<details><summary>追溯信息</summary><code>v {decision.sourceRevision} · {decision.sourceHash.slice(0,16)}</code>{decision.retrievalRunId&&<span>来自检索轨迹，排序 {decision.retrievalRank}</span>}</details>}</article>)}</div></>:<div className="nd-empty"><strong>还没有装配预览</strong><span>选择创作任务并生成预览，即可查看资料为什么被选入、排除、去重或裁剪。</span></div>}</section></div>}
    {tab==="snapshots"&&<div className="nd-context-snapshot-layout"><aside className="nd-context-snapshot-list"><div className="nd-panel-title"><div><span>不可变记录</span><strong>运行快照</strong></div></div>{snapshots.length?snapshots.map(item=><button type="button" className={item.id===selectedSnapshotId?"is-selected":""} onClick={()=>setSelectedSnapshotId(item.id)} key={item.id}><span><b>{item.nodeKey||"创作任务"}</b><small>{new Date(item.finalizedAt).toLocaleString()}</small></span><em>{item.includedCount} 项</em></button>):<div className="nd-empty"><strong>暂无运行快照</strong><span>装配预览不会出现在这里；只有受控运行正式开始前才会冻结快照。</span></div>}</aside><section className="nd-context-snapshot-detail">{snapshotDetail?<><div className="nd-panel-title"><div><span>运行时逐项小票</span><strong>{snapshotDetail.nodeKey||"上下文快照"}</strong></div><span className="nd-context-version-pill">只读</span></div><dl className="nd-context-snapshot-facts"><div><dt>资料版本</dt><dd>{snapshotSources.length}</dd></div><div><dt>装配位置</dt><dd>{snapshotDetail.slots.length}</dd></div><div><dt>检索轨迹</dt><dd>{snapshotDetail.retrievalTraces?.length??0}</dd></div></dl><div className="nd-context-decisions">{snapshotSources.map(source=><article key={source.id}><span className="nd-context-decision-state">已冻结</span><div><h3>{source.sourceLabel}</h3><p>{source.reasonDetail}</p><small>{source.layerLabel} · 版本 {source.sourceRevision}</small></div><a href={sourceHref(bookId,source.sourceType)}>查看来源</a></article>)}</div>{advanced&&<details className="nd-context-trace"><summary>版本与哈希</summary><code>快照 {snapshotDetail.manifestHash}</code><code>来源集合 {snapshotDetail.sourceSetHash}</code></details>}</>:<div className="nd-empty"><strong>选择运行快照</strong><span>这里展示一次正式运行实际冻结的资料版本，不会跟随来源后续修改。</span></div>}</section></div>}
  </StructureShell>;
}
