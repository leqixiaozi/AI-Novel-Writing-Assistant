import { useEffect, useMemo, useState } from "react";
import type { MarketAnalysisResult, MarketScanDetail, MarketSourceDefinition, ResearchRecordDetail, ResearchRecordSummary } from "../common/contracts";
import { newDesignApi } from "./api";
import ResearchShell from "./ResearchShell";

const statusLabel={queued:"等待开始",running:"采集中",completed:"已完成",partial:"部分完成",failed:"失败",cancelled:"已取消"} as const;
const analysisSections:[keyof Omit<MarketAnalysisResult,"evidenceBoundary"|"signals">,string][]=[["genre","题材"],["protagonistIdentities","主角身份"],["coreAdvantages","核心优势"],["openingPatterns","开局方式"],["relationshipHooks","关系钩子"],["titlePatterns","标题模式"],["readerPayoffs","读者满足"],["crowdedTropes","拥挤套路"],["differentiationOpportunities","差异化机会"]];

export default function MarketRadarPage(){
  const [sources,setSources]=useState<MarketSourceDefinition[]>([]),[sourceKeys,setSourceKeys]=useState<string[]>([]),[records,setRecords]=useState<ResearchRecordSummary[]>([]);
  const [scan,setScan]=useState<MarketScanDetail|null>(null),[selectedItems,setSelectedItems]=useState<string[]>([]),[analysis,setAnalysis]=useState<ResearchRecordDetail|null>(null);
  const [focus,setFocus]=useState("寻找仙侠、成长与悬疑结合的差异化机会"),[budget,setBudget]=useState(5000),[busy,setBusy]=useState(false),[message,setMessage]=useState("");
  const loadRecords=async()=>{const next=await newDesignApi.listResearchRecords({type:"market_scan"});setRecords(next);return next;};
  useEffect(()=>{void Promise.all([newDesignApi.listMarketSources(),loadRecords(),newDesignApi.listResearchRecords({type:"market_analysis"})]).then(([nextSources,nextRecords,analyses])=>{setSources(nextSources);setSourceKeys(nextSources.map((item)=>`${item.platform}:${item.listKey}`));if(nextRecords[0]){void newDesignApi.getMarketScan(nextRecords[0].id).then(setScan);const related=analyses.find((item)=>item.currentVersion.sourceScope.scanRecordId===nextRecords[0].id);if(related)void newDesignApi.getResearchRecord(related.id).then(setAnalysis);}}).catch((error)=>setMessage(error instanceof Error?error.message:"市场雷达加载失败。"));},[]);
  const scanRunning=scan&&["queued","running"].includes(scan.record.currentVersion.runStatus),analysisRunning=analysis&&["queued","running"].includes(analysis.currentVersion.runStatus);
  useEffect(()=>{if(!scanRunning&&!analysisRunning)return;const timer=window.setInterval(()=>{if(scanRunning&&scan)void newDesignApi.getMarketScan(scan.record.id).then(setScan);if(analysisRunning&&analysis)void newDesignApi.getResearchRecord(analysis.id).then(setAnalysis);void loadRecords();},1200);return()=>window.clearInterval(timer);},[scan?.record.id,scanRunning,analysis?.id,analysisRunning]);
  useEffect(()=>{if(scan&&!scanRunning&&!selectedItems.length)setSelectedItems(scan.snapshots.flatMap((source)=>source.items.slice(0,5).map((item)=>item.id)));},[scan?.record.currentVersion.id,scanRunning]);
  const grouped=useMemo(()=>sources.reduce<Record<string,MarketSourceDefinition[]>>((all,item)=>{(all[item.platformLabel]??=[]).push(item);return all;},{}),[sources]);
  const result=analysis?.currentVersion.structuredResult as Partial<MarketAnalysisResult>|undefined;
  const beginScan=async()=>{setBusy(true);setMessage("");setAnalysis(null);setSelectedItems([]);try{const run=await newDesignApi.startMarketScan(sourceKeys);const detail=await newDesignApi.getMarketScan(run.recordId);setScan(detail);await loadRecords();setMessage("扫描已开始；只采集公开榜单元数据，不会调用 AI。");}catch(error){setMessage(error instanceof Error?error.message:"扫描启动失败。");}finally{setBusy(false);}};
  const openScan=async(id:string)=>{setBusy(true);setAnalysis(null);setSelectedItems([]);try{setScan(await newDesignApi.getMarketScan(id));}catch(error){setMessage(error instanceof Error?error.message:"扫描记录加载失败。");}finally{setBusy(false);}};
  const retry=async()=>{if(!scan)return;setBusy(true);try{await newDesignApi.retryMarketScan(scan.record.id);setScan(await newDesignApi.getMarketScan(scan.record.id));setSelectedItems([]);setMessage("已新增扫描版本，旧快照保持不变。");}catch(error){setMessage(error instanceof Error?error.message:"重试失败。");}finally{setBusy(false);}};
  const cancel=async(versionId:string)=>{try{await newDesignApi.cancelResearchRun(versionId);setMessage("已请求取消，当前已保存的来源快照会保留。");}catch(error){setMessage(error instanceof Error?error.message:"取消失败。");}};
  const beginAnalysis=async()=>{if(!scan)return;setBusy(true);setMessage("");try{const run=analysis?await newDesignApi.retryMarketAnalysis(analysis.id):await newDesignApi.startMarketAnalysis({scanRecordId:scan.record.id,itemIds:selectedItems,focus,budgetTokens:budget});setAnalysis(await newDesignApi.getResearchRecord(run.recordId));setMessage(analysis?`已新增市场分析 v${run.version}，旧报告保持不变。`:"AI 只会分析你勾选的作品；结果先成为候选，不会自动建书或写入正式卡片。");}catch(error){setMessage(error instanceof Error?error.message:"市场分析启动失败。");}finally{setBusy(false);}};
  const adopt=async(id:string)=>{setBusy(true);try{const card=await newDesignApi.adoptMarketSignal(id);if(analysis)setAnalysis(await newDesignApi.getResearchRecord(analysis.id));setMessage(`“${card.title}”已由你确认保存为市场信号卡。`);}catch(error){setMessage(error instanceof Error?error.message:"市场信号保存失败。");}finally{setBusy(false);}};
  return <ResearchShell active="radar"><main className="nd-market-radar"><section className="nd-radar-source-panel"><div className="nd-section-heading"><div><p className="nd-kicker">第一步 · 只采集</p><h2>选择公开榜单</h2><p>进入页面不会扫描；点击后也只保存公开元数据和来源时间。</p></div><button className="nd-button nd-button-primary" disabled={busy||!sourceKeys.length} onClick={()=>void beginScan()} type="button">开始扫描</button></div><div className="nd-radar-sources">{Object.entries(grouped).map(([platform,items])=><fieldset key={platform}><legend>{platform}</legend>{items.map((item)=>{const key=`${item.platform}:${item.listKey}`;return <label key={key}><input checked={sourceKeys.includes(key)} onChange={(event)=>setSourceKeys((current)=>event.target.checked?[...current,key]:current.filter((value)=>value!==key))} type="checkbox"/><span><strong>{item.listLabel}</strong><small>{item.channel==="male"?"男频":item.channel==="female"?"女频":"综合"} · 公开页面</small></span></label>;})}</fieldset>)}</div></section><section className="nd-radar-history"><div className="nd-section-heading"><div><p className="nd-kicker">扫描历史</p><h2>每次运行都留版本</h2></div><b>{records.length}</b></div><div>{records.map((record)=><button className={scan?.record.id===record.id?"is-selected":""} key={record.id} onClick={()=>void openScan(record.id)} type="button"><span><strong>{record.title}</strong><small>v{record.currentVersion.version} · {statusLabel[record.currentVersion.runStatus]}</small></span><b>{record.currentVersion.progress}%</b></button>)}</div></section>{scan&&<section className="nd-radar-results"><div className="nd-radar-run-head"><div><p className="nd-kicker">扫描 v{scan.record.currentVersion.version}</p><h2>{statusLabel[scan.record.currentVersion.runStatus]} · {scan.snapshots.reduce((sum,item)=>sum+item.items.length,0)} 条</h2><p>{scan.record.currentVersion.report||scan.record.currentVersion.lastError||"正在逐个读取所选公开榜单。"}</p></div><div>{scanRunning&&<button className="nd-button nd-button-secondary" onClick={()=>void cancel(scan.record.currentVersion.id)} type="button">取消扫描</button>}{!scanRunning&&<button className="nd-button nd-button-secondary" onClick={()=>void retry()} type="button">重试并新增版本</button>}</div></div><progress max="100" value={scan.record.currentVersion.progress}/><div className="nd-radar-snapshots">{scan.snapshots.map((snapshot)=><details open={snapshot.status==="succeeded"} key={snapshot.id}><summary><span>{snapshot.platform}／{snapshot.listLabel}</span><b className={`nd-state is-${snapshot.status}`}>{snapshot.status==="succeeded"?`${snapshot.items.length} 条`:snapshot.status==="failed"?"失败":"旧快照"}</b></summary>{snapshot.error&&<p className="nd-message is-error">{snapshot.error}；其他来源与既有记录不受影响。</p>}<div>{snapshot.items.map((item)=><label key={item.id}><input checked={selectedItems.includes(item.id)} onChange={(event)=>setSelectedItems((current)=>event.target.checked?[...current,item.id]:current.filter((id)=>id!==item.id))} type="checkbox"/><span className="nd-rank">{item.rank}</span><span><strong>{item.title}</strong><small>{[item.author,item.category,item.heatLabel].filter(Boolean).join(" · ")||"公开榜单条目"}</small></span><a href={item.sourceUrl} rel="noreferrer" target="_blank">来源</a></label>)}</div></details>)}</div></section>}{scan&&!scanRunning&&scan.snapshots.some((item)=>item.items.length)&&<section className="nd-radar-analysis"><div className="nd-section-heading"><div><p className="nd-kicker">第二步 · 明确调用 AI</p><h2>分析已选的 {selectedItems.length} 条作品</h2><p>未勾选的数据不会进入分析；扫描和 AI 分析是两次独立运行。</p></div><button className="nd-button nd-button-primary" disabled={busy||!selectedItems.length||Boolean(analysisRunning)} onClick={()=>void beginAnalysis()} type="button">开始 AI 分析</button></div><div className="nd-radar-analysis-controls"><label className="nd-control"><span>这次重点看什么</span><input value={focus} onChange={(event)=>setFocus(event.target.value)}/></label><label className="nd-control"><span>最大输出预算</span><select value={budget} onChange={(event)=>setBudget(Number(event.target.value))}><option value={3000}>快速 · 3,000 tokens</option><option value={5000}>标准 · 5,000 tokens</option><option value={8000}>完整 · 8,000 tokens</option></select></label></div>{analysis&&<div className="nd-analysis-output"><div className="nd-radar-run-head"><div><h3>{statusLabel[analysis.currentVersion.runStatus]}</h3><p>{analysis.currentVersion.lastError||`已使用 ${analysis.currentVersion.usedTokens} tokens`}</p></div>{analysisRunning&&<button className="nd-button nd-button-secondary" onClick={()=>void cancel(analysis.currentVersion.id)} type="button">取消分析</button>}</div><progress max="100" value={analysis.currentVersion.progress}/>{analysis.currentVersion.runStatus==="completed"&&<><div className="nd-analysis-grid">{analysisSections.map(([key,label])=><article key={key}><strong>{label}</strong><p>{(result?.[key]??[]).join("、")||"没有足够证据"}</p></article>)}</div><aside><strong>证据边界</strong><p>{result?.evidenceBoundary}</p></aside><div className="nd-signal-candidates"><div><p className="nd-kicker">待你确认</p><h3>市场信号候选</h3></div>{analysis.candidates.map((candidate)=><article key={candidate.id}><div><small>{String(candidate.values.signal_type)}</small><h4>{candidate.title}</h4><p>{String(candidate.values.summary??"")}</p><em>来源：{String(candidate.values.source_refs??"")}</em></div><button className="nd-button nd-button-secondary" disabled={busy||candidate.status==="adopted"} onClick={()=>void adopt(candidate.id)} type="button">{candidate.status==="adopted"?"已保存":"确认保存为卡片"}</button></article>)}</div></>}</div>}</section>}{message&&<p className="nd-message">{message}</p>}</main></ResearchShell>;
}
