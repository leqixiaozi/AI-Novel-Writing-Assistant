export const DRAMA_TABS=[{key:'source',label:'来源'},{key:'strategy',label:'策略'},{key:'characters',label:'人物'},{key:'episodes',label:'分集'},{key:'production',label:'制作'},{key:'quality',label:'质量'},{key:'export',label:'导出'}] as const;export type DramaTab=typeof DRAMA_TABS[number]['key'];export function resolveDramaTab(value:string|null):{valid:boolean;tab:DramaTab|null}{const tab=value??'source',match=DRAMA_TABS.find(item=>item.key===tab);return{valid:Boolean(match),tab:match?.key??null};}
