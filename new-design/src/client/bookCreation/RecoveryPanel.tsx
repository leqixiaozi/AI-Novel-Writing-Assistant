import type {BookCreationSession} from "../../common/contracts";
/** Actions live in the unified review form; this projection never starts a new model request. */
export default function CreationRecoveryPanel({session,busy,onRead}:{session:BookCreationSession;busy:boolean;onRead:()=>Promise<void>}){return <section className="nd-creation-recovery" aria-label="开书失败与恢复"><h2>开书步骤需要核对</h2><p>{session.errorMessage??"原请求执行结果尚未确认。"}</p><p>开书起点、已保存候选和表单填写保留。回执未知时只读核对；已保存结果在同一表单审阅采用，不重新生成。</p><button className="nd-button nd-button-secondary" type="button" disabled={busy} onClick={()=>void onRead()}>只读核对原请求结果</button><a href={`/new-design/books/new?session=${session.id}`}>返回原开书表单</a><a href="/new-design/structure/models">打开新设计模型设置</a></section>;}
