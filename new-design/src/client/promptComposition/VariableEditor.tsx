import type {CompositionVariable} from "../../common/promptComposition";
export function VariableEditor({variables,values,onVariables,onValues,disabled}:{variables:CompositionVariable[];values:Record<string,string|number|boolean>;onVariables(next:CompositionVariable[]):void;onValues(next:Record<string,string|number|boolean>):void;disabled:boolean}){
  const revise=(index:number,patch:Partial<CompositionVariable>)=>onVariables(variables.map((item,position)=>position===index?{...item,...patch}:item));
  return <fieldset disabled={disabled} className="nd-composition-variables"><div className="nd-composition-section-heading"><h3>中文补充参数</h3><button type="button" className="nd-button" onClick={()=>onVariables([...variables,{key:`variable_${crypto.randomUUID().replaceAll("-","")}`,label:"",type:"text",options:[],defaultValue:""}])}>＋ 添加参数</button></div><p>补充要求随本次输入交给模型，不替代系统任务或输出规格。</p>{variables.map((variable,index)=><section key={variable.key} className="nd-composition-variable"><div className="nd-composition-form-grid"><label className="nd-control">参数名称<input value={variable.label} onChange={event=>revise(index,{label:event.target.value})}/></label><label className="nd-control">填写方式<select value={variable.type} onChange={event=>{const type=event.target.value as CompositionVariable["type"];revise(index,{type,defaultValue:type==="boolean"?false:type==="number"?0:""});onValues({...values,[variable.key]:type==="boolean"?false:type==="number"?0:""});}}><option value="text">文字</option><option value="number">数字</option><option value="boolean">是／否</option><option value="select">下拉选择</option></select></label></div>{variable.type==="select"&&<label className="nd-control">可选值（每行一个）<textarea rows={3} value={variable.options.join("\n")} onChange={event=>revise(index,{options:event.target.value.split("\n").filter(Boolean)})}/></label>}<label className="nd-control">默认值{variable.type==="boolean"?<select value={String(variable.defaultValue)} onChange={event=>revise(index,{defaultValue:event.target.value==="true"})}><option value="false">否</option><option value="true">是</option></select>:variable.type==="select"?<select value={String(variable.defaultValue)} onChange={event=>revise(index,{defaultValue:event.target.value})}><option value="">请选择</option>{variable.options.map(option=><option key={option}>{option}</option>)}</select>:<input type={variable.type==="number"?"number":"text"} value={String(variable.defaultValue)} onChange={event=>revise(index,{defaultValue:variable.type==="number"?Number(event.target.value):event.target.value})}/>}</label><label className="nd-control">本次填写{variable.type==="boolean"?<select value={String(values[variable.key]??variable.defaultValue)} onChange={event=>onValues({...values,[variable.key]:event.target.value==="true"})}><option value="false">否</option><option value="true">是</option></select>:variable.type==="select"?<select value={String(values[variable.key]??variable.defaultValue)} onChange={event=>onValues({...values,[variable.key]:event.target.value})}><option value="">请选择</option>{variable.options.map(option=><option key={option}>{option}</option>)}</select>:<input type={variable.type==="number"?"number":"text"} value={String(values[variable.key]??variable.defaultValue)} onChange={event=>onValues({...values,[variable.key]:variable.type==="number"?Number(event.target.value):event.target.value})}/>}</label><button type="button" className="nd-button" onClick={()=>onVariables(variables.filter((_,position)=>position!==index))}>移除此参数</button><details><summary>稳定标识</summary><code>{variable.key}</code></details></section>)}</fieldset>;
}
