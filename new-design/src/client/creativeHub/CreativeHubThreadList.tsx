import type {CreativeHubThread} from '../../common/creativeHub';
export function CreativeHubThreadList({threads,selectedId,busy,onSelect,onCreate}:{threads:CreativeHubThread[];selectedId:string|null;busy:boolean;onSelect(thread:CreativeHubThread):void;onCreate():void}){return <aside className="nd-hub-thread-panel"><header><div><p className="nd-eyebrow">持续会话</p><h2>诊断记录</h2></div><button type="button" className="nd-button" disabled={busy} onClick={onCreate}>新会话</button></header>{threads.length?<ul>{threads.map(thread=><li key={thread.id}><button type="button" className={thread.id===selectedId?'is-selected':''} aria-pressed={thread.id===selectedId} onClick={()=>onSelect(thread)}><strong>{thread.title}</strong><small>{thread.binding.bookId?'已绑定作品':'未绑定作品'} · 版本 {thread.revision}</small></button></li>)}</ul>:<div className="nd-empty-state"><p>尚无创作中枢会话。</p><p>新建会话后，可绑定作品并持续保留诊断历史。</p></div>}</aside>;}
