# 公共世界包HTTP边界

固定包能力与已发布版本GET、完整原请求POST只读核对放在书架写入守卫之前，归档书仍能核对既有回执，能力停用不销毁历史。普通启动未安装084时能力GET返回installed=false、operational=false，不创建表或激活服务。

预览只读，公共候选准备／首次明确发布、完整资料及关系安装、pull／push／候选明确发布／同步设置分别由来源页操作。完整原请求核对不执行写入或重发命令。任何写入须手动安装并启用完整数据库保护；后续完整链路仍待统一功能验证，不把基础安装检查或编译当作D验收。模块不调用AI、复制书籍状态或启动后台恢复。
