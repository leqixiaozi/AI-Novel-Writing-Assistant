/** Exact persisted supplement and revision records; no view or runtime SQL rewrite. */
export {canonicalFactEvidenceRows,chapterAdoptionPreparationRows,chapterAdoptionSessionRows,chapterRevisionExecutionRows,chapterRevisionReviewFlagRows,chapterStableCheckpointRows,currentKnowledgeStateProjectionRows,currentStateProjectionRows,entityInitialStateRows,knowledgeStateChangeRows,knowledgeStateProposalVersionRows,knowledgeStateProposalRows,planningObjectRows,planningVersionReferenceRows,planningVersionRows} from '../../chapterProduction/persistence';
export const canonicalFactRows = `(SELECT data.id,data.book_id,data.subject_card_id,data.predicate,data.value_kind,record_version.values->'value_json' AS value_json,data.value_hash,data.object_card_id,data.valid_story_start,data.valid_story_end,data.status,data.confidence,data.source_method,data.supersedes_fact_id,data.superseded_by_fact_id,data.revision,data.created_by,data.created_at,data.updated_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,subject_card_id uuid,predicate text,value_kind text,value_json jsonb,value_hash char(64),object_card_id uuid,valid_story_start numeric,valid_story_end numeric,status text,confidence numeric(5,4),source_method text,supersedes_fact_id uuid,superseded_by_fact_id uuid,revision integer,created_by text,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='canonical_fact'))`;
export const chapterResourceSupplementRows = `(SELECT data.session_id,data.book_id,data.base_checkpoint_id,data.request_key,record_version.values->'full_input' AS full_input,data.input_hash,record_version.values->'source_snapshot' AS source_snapshot,data.source_hash,record_version.values->'original_receipt' AS original_receipt,data.actor,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(session_id uuid,book_id uuid,base_checkpoint_id uuid,request_key text,full_input jsonb,input_hash char(64),source_snapshot jsonb,source_hash char(64),original_receipt jsonb,actor text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_resource_supplement'))`;
export const chapterRevisionEventRows = `(SELECT data.id,data.preview_id,data.plan_id,data.execution_id,data.event_kind,data.actor,data.idempotency_key,record_version.values->'detail' AS detail,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,preview_id uuid,plan_id uuid,execution_id uuid,event_kind text,actor text,idempotency_key text,detail jsonb,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_event'))`;
export const chapterRevisionImpactRows = `(SELECT data.id,data.preview_id,data.sequence,data.category,data.target_kind,data.target_id,data.target_version_id,data.target_chapter_document_id,data.target_chapter_order,data.source_mode,data.risk_level,record_version.values->'reason_chain' AS reason_chain,data.recommended_action,data.allowed_actions,data.active_source_count,data.manual_protected,record_version.values->'detail' AS detail,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,preview_id uuid,sequence integer,category text,target_kind text,target_id uuid,target_version_id uuid,target_chapter_document_id uuid,target_chapter_order integer,source_mode text,risk_level text,reason_chain jsonb,recommended_action text,allowed_actions text[],active_source_count integer,manual_protected boolean,detail jsonb,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_impact'))`;
export const chapterRevisionPlanItemRows = `(SELECT data.id,data.plan_id,data.impact_id,data.selected_action,data.decision_source,data.note,data.revision,data.decided_by,data.created_at,data.updated_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,plan_id uuid,impact_id uuid,selected_action text,decision_source text,note text,revision integer,decided_by text,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_plan_item'))`;
export const chapterRevisionPlanRows = `(SELECT data.id,data.preview_id,data.book_id,data.expected_preview_revision,data.expected_document_revision,data.reason,data.status,record_version.values->'summary' AS summary,data.revision,data.idempotency_key,data.created_by,data.confirmed_by,data.confirmed_at,data.created_at,data.updated_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,preview_id uuid,book_id uuid,expected_preview_revision integer,expected_document_revision integer,reason text,status text,summary jsonb,revision integer,idempotency_key text,created_by text,confirmed_by text,confirmed_at timestamptz,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_plan'))`;
export const chapterRevisionPreviewRows = `(SELECT data.id,data.book_id,data.session_id,data.chapter_document_id,data.old_body_version_id,data.new_body_version_id,data.old_settlement_id,data.old_checkpoint_id,data.restart_checkpoint_id,data.expected_document_revision,record_version.values->'dependency_generation' AS dependency_generation,record_version.values->'body_diff_summary' AS body_diff_summary,record_version.values->'impact_summary' AS impact_summary,data.snapshot_hash,data.status,data.revision,data.idempotency_key,data.created_by,data.created_at,data.updated_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,session_id uuid,chapter_document_id uuid,old_body_version_id uuid,new_body_version_id uuid,old_settlement_id uuid,old_checkpoint_id uuid,restart_checkpoint_id uuid,expected_document_revision integer,dependency_generation jsonb,body_diff_summary jsonb,impact_summary jsonb,snapshot_hash char(64),status text,revision integer,idempotency_key text,created_by text,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_preview'))`;
export const chapterRevisionProtectionMarkRows = `(SELECT data.id,data.book_id,data.target_kind,data.target_id,data.protection_kind,data.source_execution_id,data.reason,data.actor,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,target_kind text,target_id uuid,protection_kind text,source_execution_id uuid,reason text,actor text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_protection_mark'))`;
export const chapterRevisionRecomputeStepRows = `(SELECT data.id,data.execution_id,data.sequence,data.predecessor_step_id,data.impact_id,data.chapter_document_id,data.chapter_order,data.step_kind,data.selected_action,data.dependency_request_id,data.background_job_id,data.status,record_version.values->'result' AS result,data.error_summary,data.idempotency_key,data.created_at,data.updated_at,data.completed_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,execution_id uuid,sequence integer,predecessor_step_id uuid,impact_id uuid,chapter_document_id uuid,chapter_order integer,step_kind text,selected_action text,dependency_request_id uuid,background_job_id uuid,status text,result jsonb,error_summary text,idempotency_key text,created_at timestamptz,updated_at timestamptz,completed_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_recompute_step'))`;
export const dependencyInvalidationEventRows = `(SELECT data.id,data.space_id,data.book_id,data.old_resource_id,data.new_resource_id,data.change_preview_id,data.book_change_set_id,data.reason,data.trigger_source,data.requested_state,data.trigger_id,data.idempotency_key,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,space_id uuid,book_id uuid,old_resource_id uuid,new_resource_id uuid,change_preview_id uuid,book_change_set_id uuid,reason text,trigger_source text,requested_state text,trigger_id uuid,idempotency_key text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='dependency_invalidation_event'))`;
export const dependencyRecomputeRequestRows = `(SELECT data.id,data.book_id,data.target_resource_id,data.invalidation_event_id,record_version.values->'required_upstream_versions' AS required_upstream_versions,data.priority,data.status,data.reason,data.strategy_key,data.task_contract_version_id,data.idempotency_key,data.created_at,data.started_at,data.completed_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,target_resource_id uuid,invalidation_event_id uuid,required_upstream_versions jsonb,priority integer,status text,reason text,strategy_key text,task_contract_version_id uuid,idempotency_key text,created_at timestamptz,started_at timestamptz,completed_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='dependency_recompute_request'))`;
export const dependencyResourceStateRows = `(SELECT data.resource_id,data.book_id,data.state,data.revision,data.last_event_id,data.updated_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(resource_id uuid,book_id uuid,state text,revision bigint,last_event_id uuid,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='dependency_resource_state'))`;
export const entityInitialStateVersionRows = `(SELECT data.id,data.initial_state_id,data.version,record_version.values->'value_json' AS value_json,data.value_hash,data.source_fact_id,data.actor,data.note,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,initial_state_id uuid,version integer,value_json jsonb,value_hash char(64),source_fact_id uuid,actor text,note text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='entity_initial_state_version'))`;
export const resourceSupplementCorrectionOriginRows = `(SELECT data.session_id,data.book_id,data.issue_id,data.canonical_input,data.canonical_source,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(session_id uuid,book_id uuid,issue_id uuid,canonical_input text,canonical_source text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='resource_supplement_correction_origin'))`;
export const resourceSupplementFormalCommitRows = `(SELECT data.book_id,data.session_id,data.settlement_id,data.request_key,record_version.values->'full_input' AS full_input,data.input_hash,data.canonical_input,record_version.values->'original_receipt' AS original_receipt,data.receipt_hash,data.canonical_receipt,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(book_id uuid,session_id uuid,settlement_id uuid,request_key uuid,full_input jsonb,input_hash char(64),canonical_input text,original_receipt jsonb,receipt_hash char(64),canonical_receipt text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='resource_supplement_formal_commit'))`;
export const resourceSupplementImpactReviewRows = `(SELECT data.review_id,data.book_id,data.session_id,data.request_key,data.session_revision,record_version.values->'full_input' AS full_input,data.input_hash,data.canonical_input,record_version.values->'impact_snapshot' AS impact_snapshot,data.impact_hash,data.canonical_impact,record_version.values->'original_receipt' AS original_receipt,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(review_id uuid,book_id uuid,session_id uuid,request_key uuid,session_revision integer,full_input jsonb,input_hash char(64),canonical_input text,impact_snapshot jsonb,impact_hash char(64),canonical_impact text,original_receipt jsonb,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='resource_supplement_impact_review'))`;
export const resourceSupplementIntegrityIssueRows = `(SELECT data.issue_id,data.settlement_id,data.book_id,data.chapter_document_id,data.body_version_id,data.state_change_id,data.subject_kind,data.subject_id,data.state_key,record_version.values->'impact' AS impact,data.source_route,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(issue_id uuid,settlement_id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,state_change_id uuid,subject_kind text,subject_id uuid,state_key text,impact jsonb,source_route text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='resource_supplement_integrity_issue'))`;
export const resourceSupplementIntegrityJournalRows = `(SELECT data.settlement_id,data.book_id,data.review_id,data.checkpoint_id,record_version.values->'merged_write' AS merged_write,data.merged_hash,data.canonical_merged,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(settlement_id uuid,book_id uuid,review_id uuid,checkpoint_id uuid,merged_write jsonb,merged_hash char(64),canonical_merged text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='resource_supplement_integrity_journal'))`;
export const resourceSupplementIntegrityResolutionRows = `(SELECT data.resolution_id,data.issue_id,data.book_id,data.correction_checkpoint_id,data.request_key,record_version.values->'full_proof' AS full_proof,data.proof_hash,record_version.values->'original_receipt' AS original_receipt,data.created_at,data.canonical_proof FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(resolution_id uuid,issue_id uuid,book_id uuid,correction_checkpoint_id uuid,request_key uuid,full_proof jsonb,proof_hash char(64),original_receipt jsonb,created_at timestamptz,canonical_proof text) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='resource_supplement_integrity_resolution'))`;
export const stateChangeProposalRows = `(SELECT data.id,data.book_id,data.chapter_document_id,data.body_version_id,data.text_anchor_id,data.cause_event_card_id,data.subject_kind,data.subject_id,data.state_key,data.before_json,record_version.values->'after_json' AS after_json,data.delta_json,data.reason,data.effective_story_order,data.source,data.status,data.confirmed_state_change_id,data.revision,data.created_at,data.updated_at,data.before_known FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,text_anchor_id uuid,cause_event_card_id uuid,subject_kind text,subject_id uuid,state_key text,before_json jsonb,after_json jsonb,delta_json jsonb,reason text,effective_story_order numeric,source text,status text,confirmed_state_change_id uuid,revision integer,created_at timestamptz,updated_at timestamptz,before_known boolean) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='state_change_proposal'))`;
export const stateChangeRows = `(SELECT data.id,data.sequence,data.book_id,data.settlement_id,data.proposal_id,data.chapter_document_id,data.body_version_id,data.text_anchor_id,data.cause_event_card_id,data.subject_kind,data.subject_id,data.state_key,data.before_json,record_version.values->'after_json' AS after_json,data.delta_json,data.reason,data.effective_story_order,data.status,data.created_at FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,sequence bigint,book_id uuid,settlement_id uuid,proposal_id uuid,chapter_document_id uuid,body_version_id uuid,text_anchor_id uuid,cause_event_card_id uuid,subject_kind text,subject_id uuid,state_key text,before_json jsonb,after_json jsonb,delta_json jsonb,reason text,effective_story_order numeric,status text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='state_change'))`;
