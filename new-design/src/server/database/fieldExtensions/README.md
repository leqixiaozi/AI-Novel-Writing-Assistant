# 补充字段与原请求核对

本书同类字段、当前卡片字段和当前卡片字段修订／归档复用 field_scope_adoptions 不可变事件，在 impact 中保存完整原输入、哈希及当次字段快照，不查询最新版本代替原成功结果。同请求锁先于修订检查，跨书／不同输入／旧不完整回执保持 unknown；只有进入 COMMIT 前并收到物理回滚确认才允许 not_written。

作者补充信息弹窗在发送前保留原填写与凭证；响应中断后锁定新增并只读核对完整冻结输入。确认写入后的目录刷新失败也不释放请求重新创建。关联局部字段仍归 associations 原合同，本阶段只保护未知提交不重发，其专门回执随关联阶段补齐。

字段标记 stateSettlement=none 时，安装实际非结算策略；禁止覆盖未发布类型草稿。数据正本仍为原规格、卡片及本地字段版本表，无第二套档案。
