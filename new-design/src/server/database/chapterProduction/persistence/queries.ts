/** Typed projections for the exact production, quality and follow-up record contracts.
 * SQL predicates continue to compare business identities stored in the immutable current version. */
export const assetRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='asset'))`;
export const canonicalFactConflictRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,fact_a_id uuid,fact_b_id uuid,predicate text,reason text,status text,resolution_fact_id uuid,revision integer,created_at timestamptz,resolved_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='canonical_fact_conflict'))`;
export const canonicalFactEvidenceRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,fact_id uuid,chapter_text_anchor_id uuid,card_version_id uuid,research_evidence_id uuid,extraction_method text,note text,stale_at timestamptz,stale_reason text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='canonical_fact_evidence'))`;
export const canonicalFactRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,subject_card_id uuid,predicate text,value_kind text,value_json jsonb,value_hash char(64),object_card_id uuid,valid_story_start numeric,valid_story_end numeric,status text,confidence numeric(5,4),source_method text,supersedes_fact_id uuid,superseded_by_fact_id uuid,revision integer,created_by text,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='canonical_fact'))`;
export const chapterAdoptionPreparationRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,expected_document_revision integer,planning_object_id uuid,planning_version_id uuid,planning_content_hash char(64),context_manifest_id uuid,dependency_snapshot jsonb,dependency_hash char(64),status text,idempotency_key text,created_by text,created_at timestamptz,supplement_base_checkpoint_id uuid) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_adoption_preparation'))`;
export const chapterAdoptionSessionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,preparation_id uuid,prior_body_version_id uuid,adoption_id uuid,settlement_id uuid,policy_version_id uuid,planning_object_id uuid,planning_version_id uuid,context_manifest_id uuid,dependency_hash char(64),adoption_kind text,status text,revision integer,idempotency_key text,created_by text,error_summary text,created_at timestamptz,updated_at timestamptz,supplement_base_checkpoint_id uuid) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_adoption_session'))`;
export const chapterBodyOperationRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,chapter_document_id uuid,operation_kind text,expected_revision integer,result_revision integer,result_body_version_id uuid,request_hash char(64),idempotency_key text,actor text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_body_operation'))`;
export const chapterQualityRequestRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,request_key uuid,request_hash text,input_payload jsonb,frozen_snapshot jsonb,ai_task_id uuid,step_id uuid,attempt_id uuid,report_id uuid,status text,model_request_state text,generated_output jsonb,generated_execution jsonb,failure text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_quality_request'))`;
export const chapterRevisionExecutionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,plan_id uuid,preview_id uuid,book_id uuid,chapter_document_id uuid,old_body_version_id uuid,new_body_version_id uuid,old_checkpoint_id uuid,restart_checkpoint_id uuid,adoption_id uuid,invalidation_event_id uuid,status text,current_step integer,total_steps integer,revision integer,idempotency_key text,actor text,error_summary text,created_at timestamptz,updated_at timestamptz,completed_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_execution'))`;
export const chapterRevisionReviewFlagRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,execution_id uuid,changed_chapter_document_id uuid,target_chapter_document_id uuid,target_body_version_id uuid,impact_id uuid,status text,reason text,source_route text,manual_protected boolean,resolution_note text,revision integer,created_at timestamptz,updated_at timestamptz,resolved_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_revision_review_flag'))`;
export const chapterStableCheckpointRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,session_id uuid,settlement_id uuid,previous_checkpoint_id uuid,chapter_order integer,summary jsonb,dependency_hash char(64),status text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_stable_checkpoint'))`;
export const chapterWritingRequestRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,chapter_document_id uuid,operation_kind text,planning_object_id uuid,planning_version_id uuid,planning_content_hash char(64),context_manifest_id uuid,task_contract_version_id uuid,prompt_recipe_version_id uuid,model_route_snapshot_id uuid,input_body_version_id uuid,input_body_hash char(64),selection_start integer,selection_end integer,instruction text,expected_document_revision integer,ai_task_id uuid,result_body_version_id uuid,status text,error_summary text,request_hash char(64),idempotency_key text,created_by text,created_at timestamptz,updated_at timestamptz,controlled_snapshot jsonb,controlled_output jsonb,controlled_execution jsonb) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='chapter_writing_request'))`;
export const currentKnowledgeStateProjectionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(book_id uuid,holder_kind text,holder_key text,holder_card_id uuid,claim_id uuid,source_change_id uuid,stance text,confidence numeric,effective_story_order numeric,effective_narrative_order numeric,projection_revision bigint,rebuilt_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='current_knowledge_state_projection'))`;
export const currentStateProjectionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(book_id uuid,subject_kind text,subject_id uuid,state_key text,value_json jsonb,source_initial_version_id uuid,source_state_change_id uuid,projection_revision bigint,is_stale boolean,rebuilt_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='current_state_projection'))`;
export const entityInitialStateVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,initial_state_id uuid,version integer,value_json jsonb,value_hash char(64),source_fact_id uuid,actor text,note text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='entity_initial_state_version'))`;
export const entityInitialStateRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,subject_kind text,subject_id uuid,state_key text,current_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='entity_initial_state'))`;
export const epistemicClaimRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,subject_card_id uuid,predicate text,value_kind text,value_json jsonb,object_card_id uuid,value_hash char(64),truth_fact_id uuid,created_by text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='epistemic_claim'))`;
export const knowledgeStateChangeRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,sequence bigint,book_id uuid,proposal_id uuid,proposal_version_id uuid,claim_id uuid,holder_kind text,holder_key text,holder_card_id uuid,stance text,confidence numeric,effective_story_order numeric,effective_narrative_order numeric,status text,confirmed_by text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='knowledge_state_change'))`;
export const knowledgeStateProposalVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,proposal_id uuid,version integer,stance text,confidence numeric,acquisition_method text,source_character_card_id uuid,source_event_card_id uuid,chapter_document_id uuid,body_version_id uuid,text_anchor_id uuid,effective_story_order numeric,effective_narrative_order numeric,reason text,editor text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='knowledge_state_proposal_version'))`;
export const knowledgeStateProposalRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,claim_id uuid,holder_kind text,holder_key text,holder_card_id uuid,current_version_id uuid,source text,status text,confirmed_change_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='knowledge_state_proposal'))`;
export const planningObjectRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='planning_object'))`;
export const planningVersionReferenceRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,planning_version_id uuid,planning_object_id uuid,book_id uuid,reference_role text,card_id uuid,card_version_id uuid,action_key text,note text,sort_order integer,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='planning_version_reference'))`;
export const planningVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,object_id uuid,book_id uuid,version integer,base_version_id uuid,based_on_parent_version_id uuid,source text,status text,content jsonb,content_hash char(64),source_body_version_id uuid,created_by text,stale_at timestamptz,stale_reason text,created_at timestamptz,execution_mode text) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='planning_version'))`;
export const productionDirectorChapterRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(run_id uuid,book_id uuid,sort_order integer,planning_object_id uuid,planning_version_id uuid,expected_planning_revision integer,chapter_card_id uuid,title text,current_request_id uuid,current_request_key uuid,boundary_completed boolean) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='production_director_chapter'))`;
export const productionDirectorCommandRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(book_id uuid,request_key uuid,run_id uuid,request_hash char(64),action text,accepted_revision integer,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='production_director_command'))`;
export const productionDirectorRunRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,request_key uuid,request_hash char(64),issue_policy text,instruction text,knowledge_sources jsonb,status text,revision integer,pause_requested boolean,lease_token uuid,lease_expires_at timestamptz,failure jsonb,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='production_director_run'))`;
export const qualityAuditReportRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,scope_kind text,scope_id uuid,task_id uuid,step_id uuid,attempt_id uuid,task_contract_version_id uuid,prompt_recipe_version_id uuid,context_manifest_id uuid,model_route_snapshot_id uuid,rule_set_key text,rule_set_version text,input_hash char(64),policy_mode text,policy_decision text,execution_effect text,summary text,idempotency_key text,request_hash char(64),created_by text,created_at timestamptz,stale_at timestamptz,stale_reason text) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_audit_report'))`;
export const qualityFixAdoptionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,candidate_id uuid,candidate_version_id uuid,chapter_body_adoption_id uuid,adopted_body_version_id uuid,idempotency_key text,actor text,created_at timestamptz,adopted_card_version_id uuid,author_write_request_key uuid,world_repair_request_hash char(64),world_repair_input jsonb) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_fix_adoption'))`;
export const qualityFixCandidateEventRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,candidate_id uuid,candidate_version_id uuid,from_status text,to_status text,action text,actor text,reason text,candidate_revision integer,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_fix_candidate_event'))`;
export const qualityFixCandidateVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,candidate_id uuid,version integer,base_version_id uuid,target_chapter_document_id uuid,target_body_version_id uuid,target_anchor_id uuid,patch jsonb,content_hash char(64),source text,created_by text,created_at timestamptz,target_card_id uuid,target_card_version_id uuid,target_field_key text,target_field_spec_version_id uuid,target_field_spec_hash char(64),target_before_hash char(64)) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_fix_candidate_version'))`;
export const qualityFixCandidateRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,issue_id uuid,status text,current_version_id uuid,accepted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_fix_candidate'))`;
export const qualityIssueEventRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,issue_id uuid,issue_version_id uuid,from_status text,to_status text,action text,actor_kind text,actor text,reason text,issue_revision integer,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_issue_event'))`;
export const qualityIssueEvidenceRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,issue_version_id uuid,evidence_kind text,text_anchor_id uuid,fact_id uuid,state_change_id uuid,story_timing_id uuid,story_relation_id uuid,planning_version_id uuid,rule_key text,rule_version text,note text,is_unverified_observation boolean,created_at timestamptz,material_kind text,material_id uuid,card_version_id uuid,relation_version_id uuid,field_key text,field_spec_version_id uuid,field_spec_hash char(64),observed_value_hash char(64)) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_issue_evidence'))`;
export const qualityIssueVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,issue_id uuid,version integer,base_version_id uuid,category_key text,severity text,confidence numeric(5,4),title text,description text,detection_source text,impact_scope jsonb,suggested_action text,target_value jsonb,observed_value jsonb,scale_version text,interpretation text,created_by text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_issue_version'))`;
export const qualityIssueRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,report_id uuid,stable_key text,current_version_id uuid,current_status text,is_quality_debt boolean,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_issue'))`;
export const qualityRecheckRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,issue_id uuid,fix_candidate_id uuid,source_report_id uuid,recheck_report_id uuid,checked_body_version_id uuid,outcome text,evidence_summary text,status text,idempotency_key text,request_hash char(64),actor text,created_at timestamptz,stale_at timestamptz,stale_reason text,checked_material_versions jsonb) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_recheck'))`;
export const qualityReportBodyVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(report_id uuid,chapter_document_id uuid,body_version_id uuid) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_report_body_version'))`;
export const qualityReportFactRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(report_id uuid,fact_id uuid) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_report_fact'))`;
export const qualityReportMaterialVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(report_id uuid,subject_kind text,subject_id uuid,card_version_id uuid,relation_version_id uuid,snapshot_hash char(64)) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_report_material_version'))`;
export const qualityReportPlanningVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(report_id uuid,planning_object_id uuid,planning_version_id uuid) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='quality_report_planning_version'))`;
export const researchEvidenceRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,research_version_id uuid,source_document_version_id uuid,field_path text,excerpt text,start_offset integer,end_offset integer,certainty text,note text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='research_evidence'))`;
export const researchRecordVersionRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,record_id uuid,version integer,parent_version_id uuid,source_scope jsonb,template_key text,template_version integer,run_status text,progress integer,budget_tokens integer,used_tokens integer,prompt_snapshot jsonb,model_snapshot jsonb,input_snapshot jsonb,structured_result jsonb,report text,last_error text,cancel_requested boolean,run_hash text,created_at timestamptz,completed_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='research_record_version'))`;
export const researchRecordRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,record_type text,title text,source_document_version_id uuid,current_version_id uuid,tags jsonb,favorite boolean,notes text,status text,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='research_record'))`;
export const stateChangeProposalRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,text_anchor_id uuid,cause_event_card_id uuid,subject_kind text,subject_id uuid,state_key text,before_json jsonb,after_json jsonb,delta_json jsonb,reason text,effective_story_order numeric,source text,status text,confirmed_state_change_id uuid,revision integer,created_at timestamptz,updated_at timestamptz,before_known boolean) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='state_change_proposal'))`;
export const stateChangeRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,sequence bigint,book_id uuid,settlement_id uuid,proposal_id uuid,chapter_document_id uuid,body_version_id uuid,text_anchor_id uuid,cause_event_card_id uuid,subject_kind text,subject_id uuid,state_key text,before_json jsonb,after_json jsonb,delta_json jsonb,reason text,effective_story_order numeric,status text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='state_change'))`;
export const storyEventRelationRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,sequence bigint,book_id uuid,proposal_id uuid,proposal_version_id uuid,relation_family text,relation_type text,source_event_card_id uuid,target_event_card_id uuid,confidence numeric,status text,confirmed_by text,reason text,created_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='story_event_relation'))`;
export const storyEventTimingRows = `(SELECT data.* FROM new_design.cards record_card JOIN new_design.card_versions record_version ON record_version.id=record_card.current_version_id AND record_version.card_id=record_card.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS data(id uuid,sequence bigint,book_id uuid,event_card_id uuid,proposal_id uuid,proposal_version_id uuid,lifecycle text,status text,time_mode text,start_certainty text,end_certainty text,start_instant timestamptz,end_instant timestamptz,timezone_name text,calendar_key text,start_label text,end_label text,normalized_start numeric,normalized_end numeric,duration_value numeric,duration_unit text,relative_to_event_card_id uuid,relative_relation text,relative_offset numeric,evidence_kind text,chapter_document_id uuid,body_version_id uuid,text_anchor_id uuid,fact_id uuid,plan_version_id uuid,state_proposal_id uuid,replaces_timing_id uuid,confirmed_by text,reason text,confirmed_at timestamptz) WHERE record_card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types record_type WHERE record_type.id=record_card.card_type_id AND record_type.type_key='story_event_timing'))`;
