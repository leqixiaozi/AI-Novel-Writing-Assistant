import { createRecordCard } from "../recordCards";
import {DEFAULT_SPACE_ID} from "../store";
import {randomUUID} from "node:crypto";
import type {CreateKnowledgeProfileInput,KnowledgeProfileReceipt} from "../../../common/knowledgeIndex";
import type {PoolClient} from "pg";
import {readManagedEmbeddingConnectionVersion} from "../modelManagement";
import {stableHash} from "../aiContracts";
import { NewDesignError } from "../../domain/errors";
import {indexTransaction,indexOriginalRead,OriginalKnowledgeRequestConflict} from "./repository";
import {createKnowledgeProfileSchema} from "./policy";
async function receipt(client:PoolClient,bookId:string,key:string,repeated=true):Promise<KnowledgeProfileReceipt|null>{const rows=(await client.query("SELECT * FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='embedding_profile_version' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,profile_id uuid,version integer,provider_key text,model_key text,dimensions integer,distance_metric text,normalize boolean,chunker_key text,chunker_version text,max_chunk_chars integer,overlap_chars integer,allowed_source_kinds text[],content_hash text,created_by text,created_at timestamptz,connection_version_id uuid,knowledge_profile_key uuid,knowledge_profile_hash text,knowledge_profile_book_id uuid) WHERE record.status='active') embedding_profile_version_record WHERE knowledge_profile_key=$1 AND knowledge_profile_book_id=$2",[key,bookId])).rows;if(rows.length>1)throw new NewDesignError("原嵌入规格请求出现重复来源，请保留原标识核对。",409);const row=rows[0];return row?{requestKey:key,inputHash:String(row.knowledge_profile_hash),profileId:String(row.profile_id),profileVersionId:String(row.id),connectionVersionId:String(row.connection_version_id),repeated,mutationOutcome:"committed"}:null;}
export async function getKnowledgeProfileByKey(bookId:string,key:string){return indexOriginalRead(bookId,key,async client=>{await client.query("SELECT pg_advisory_xact_lock(hashtextextended($1,0))",[`knowledge_profile:${key}`]);return receipt(client,bookId,key);});}
export async function createKnowledgeProfile(bookId:string,value:CreateKnowledgeProfileInput){const input=createKnowledgeProfileSchema.parse(value),hash=stableHash({contract:"knowledge_profile_v1",bookId,input});return indexTransaction(bookId,input.requestKey,"保存知识嵌入规格","not_sent",async(client,markAbsent)=>{await client.query("SELECT pg_advisory_xact_lock(hashtextextended($1,0))",[`knowledge_profile:${input.requestKey}`]);const prior=await receipt(client,bookId,input.requestKey);if(prior){if(prior.inputHash!==hash)throw new OriginalKnowledgeRequestConflict(bookId,"同一原请求不能保存不同嵌入规格或用于另一部书。");return prior;}if((await client.query("SELECT id FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='embedding_profile_version' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,profile_id uuid,version integer,provider_key text,model_key text,dimensions integer,distance_metric text,normalize boolean,chunker_key text,chunker_version text,max_chunk_chars integer,overlap_chars integer,allowed_source_kinds text[],content_hash text,created_by text,created_at timestamptz,connection_version_id uuid,knowledge_profile_key uuid,knowledge_profile_hash text,knowledge_profile_book_id uuid) WHERE record.status='active') embedding_profile_version_record WHERE knowledge_profile_key=$1",[input.requestKey])).rowCount)throw new OriginalKnowledgeRequestConflict(bookId,"原规格请求属于另一部书，请核对原来源。");markAbsent();if(!(await client.query("SELECT id FROM new_design.books WHERE id=$1 AND status='active'",[bookId])).rowCount)throw new NewDesignError("此书不存在或已归档。",409);const connection=await readManagedEmbeddingConnectionVersion(input.connectionVersionId,{client});if(connection.maxRetries!==0)throw new NewDesignError("首次知识嵌入仅支持单次请求，请在原向量配置明确设为不自动重试。",422);if(connection.model.length>160)throw new NewDesignError("模型标识超过原嵌入规格支持的长度，请明确配置兼容模型版本。",422);const id=randomUUID(),versionId=randomUUID();await client.query("INSERT INTO new_design.embedding_profiles(id,profile_key,name,purpose,created_by) VALUES($1,$2,$3,'semantic_retrieval','knowledge-index-user')",[id,`knowledge.${id}`,input.name]);await (async()=>{const values={normalize:input.normalize,created_by:"knowledge-index-user",created_at:new Date().toISOString(),connection_version_id:connection.id,knowledge_profile_key:input.requestKey,knowledge_profile_hash:hash,knowledge_profile_book_id:bookId,id:versionId,profile_id:id,version:1,provider_key:connection.provider,model_key:connection.model,dimensions:input.dimensions,distance_metric:input.distanceMetric,chunker_key:"knowledge.characters",chunker_version:"v1",max_chunk_chars:input.maxChunkChars,overlap_chars:input.overlapChars,allowed_source_kinds:["asset_parsed_text"],content_hash:stableHash({...input,provider:connection.provider,model:connection.model,connectionHash:connection.connectionHash})};if(!Number.isInteger(values.dimensions)||values.dimensions<1||values.dimensions>2000||values.max_chunk_chars<128||values.max_chunk_chars>50000||values.overlap_chars<0||values.overlap_chars>=values.max_chunk_chars||!["cosine","l2","inner_product"].includes(values.distance_metric)||!values.allowed_source_kinds.length)throw new NewDesignError("嵌入规格的维数、距离或分块范围无效。",422);const row=await createRecordCard(client,{id:String(values.id),spaceId:DEFAULT_SPACE_ID,typeKey:"embedding_profile_version",title:"embedding_profile_version",values});return{rows:[row],rowCount:1};})();await client.query("UPDATE new_design.embedding_profiles SET current_version_id=$2 WHERE id=$1",[id,versionId]);return{requestKey:input.requestKey,inputHash:hash,profileId:id,profileVersionId:versionId,connectionVersionId:connection.id,repeated:false,mutationOutcome:"committed" as const};});}
