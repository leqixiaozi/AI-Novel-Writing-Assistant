import {storyRecordCtes,insertStoryRecords,updateStoryRecords,insertStoryReviewActions,deleteStoryRecords,lockedStoryRecordQuery} from './persistence';
import { randomUUID } from "node:crypto";
import type { PoolClient } from "pg";
import type {AuthorTimelineCommand} from "../../../common/bookComposition/timeline";
import type {
  StoryEventLifecycle,
  StoryEventRelation,
  StoryEventTiming,
  StoryEvidenceKind,
  StoryNarrativeOccurrence,
  StoryRelationFamily,
  StoryRelationProposal,
  StoryRelationProposalVersion,
  StoryRelationReviewAction,
  StoryRelationType,
  StoryTimeCertainty,
  StoryTimeMode,
  StoryTimeProposal,
  StoryTimeProposalSource,
  StoryTimeProposalVersion,
  StoryTimeReviewAction,
} from "../../../common/contracts";
import { NewDesignError, assertFound } from "../../domain/errors";
import { getNewDesignPool } from "../runtime";

type EvidenceInput={
  evidenceKind:Exclude<StoryEvidenceKind,"migration">;
  chapterDocumentId?:string|null;
  bodyVersionId?:string|null;
  textAnchorId?:string|null;
  factId?:string|null;
  planVersionId?:string|null;
  stateProposalId?:string|null;
};

export type StoryTimeVersionInput=EvidenceInput&{
  lifecycle:StoryEventLifecycle;
  timeMode:StoryTimeMode;
  startCertainty:StoryTimeCertainty;
  endCertainty:StoryTimeCertainty;
  startInstant?:string|null;
  endInstant?:string|null;
  timezoneName?:string|null;
  calendarKey?:string|null;
  startLabel?:string|null;
  endLabel?:string|null;
  normalizedStart?:number|null;
  normalizedEnd?:number|null;
  durationValue?:number|null;
  durationUnit?:string|null;
  relativeToEventCardId?:string|null;
  relativeRelation?:"before"|"after"|"simultaneous"|null;
  relativeOffset?:number|null;
  replacesTimingId?:string|null;
  reason:string;
  editor?:string;
};

export type StoryRelationVersionInput=EvidenceInput&{
  relationFamily:StoryRelationFamily;
  relationType:StoryRelationType;
  sourceEventCardId:string;
  targetEventCardId:string;
  confidence?:number|null;
  reason:string;
  editor?:string;
};

function asDate(value:unknown):string{return value instanceof Date?value.toISOString():new Date(String(value)).toISOString();}
function asNumber(value:unknown):number|null{return value===null||value===undefined?null:Number(value);}
function asText(value:unknown):string|null{return value===null||value===undefined?null:String(value);}

function mapTimeFields(row:Record<string,unknown>){return{
  lifecycle:row.lifecycle as StoryEventLifecycle,timeMode:row.time_mode as StoryTimeMode,startCertainty:row.start_certainty as StoryTimeCertainty,endCertainty:row.end_certainty as StoryTimeCertainty,
  startInstant:row.start_instant?asDate(row.start_instant):null,endInstant:row.end_instant?asDate(row.end_instant):null,timezoneName:asText(row.timezone_name),calendarKey:asText(row.calendar_key),startLabel:asText(row.start_label),endLabel:asText(row.end_label),
  normalizedStart:asNumber(row.normalized_start),normalizedEnd:asNumber(row.normalized_end),durationValue:asNumber(row.duration_value),durationUnit:asText(row.duration_unit),relativeToEventCardId:asText(row.relative_to_event_card_id),relativeRelation:row.relative_relation as "before"|"after"|"simultaneous"|null,relativeOffset:asNumber(row.relative_offset),
  evidenceKind:row.evidence_kind as StoryEvidenceKind,chapterDocumentId:asText(row.chapter_document_id),bodyVersionId:asText(row.body_version_id),textAnchorId:asText(row.text_anchor_id),factId:asText(row.fact_id),planVersionId:asText(row.plan_version_id),stateProposalId:asText(row.state_proposal_id),replacesTimingId:asText(row.replaces_timing_id),reason:String(row.reason),
};}
function mapTimeVersion(row:Record<string,unknown>):StoryTimeProposalVersion{return{id:String(row.id),proposalId:String(row.proposal_id),version:Number(row.version),...mapTimeFields(row),editor:String(row.editor),createdAt:asDate(row.created_at)};}
function mapTimeReview(row:Record<string,unknown>):StoryTimeReviewAction{return{id:String(row.id),proposalId:String(row.proposal_id),proposalVersionId:String(row.proposal_version_id),action:row.action as StoryTimeReviewAction["action"],actor:String(row.actor),note:String(row.note??""),idempotencyKey:asText(row.idempotency_key),createdAt:asDate(row.created_at)};}
function mapTiming(row:Record<string,unknown>):StoryEventTiming{return{id:String(row.id),sequence:Number(row.sequence),bookId:String(row.book_id),eventCardId:String(row.event_card_id),proposalId:asText(row.proposal_id),proposalVersionId:asText(row.proposal_version_id),...mapTimeFields(row),status:row.status as StoryEventTiming["status"],confirmedBy:String(row.confirmed_by),confirmedAt:asDate(row.confirmed_at)};}
function mapOccurrence(row:Record<string,unknown>):StoryNarrativeOccurrence{return{id:String(row.id),bookId:String(row.book_id),eventCardId:String(row.event_card_id),chapterCardId:String(row.chapter_card_id),sceneCardId:asText(row.scene_card_id),chapterDocumentId:asText(row.chapter_document_id),bodyVersionId:asText(row.body_version_id),textAnchorId:asText(row.text_anchor_id),sourceTimeProposalVersionId:asText(row.source_time_proposal_version_id),role:row.role as StoryNarrativeOccurrence["role"],narrativeOrder:asNumber(row.narrative_order),sourceKind:row.source_kind as StoryNarrativeOccurrence["sourceKind"],note:String(row.note??""),status:row.status as StoryNarrativeOccurrence["status"],revision:Number(row.revision),createdAt:asDate(row.created_at),updatedAt:asDate(row.updated_at)};}
function mapRelationFields(row:Record<string,unknown>){return{relationFamily:row.relation_family as StoryRelationFamily,relationType:row.relation_type as StoryRelationType,sourceEventCardId:String(row.source_event_card_id),targetEventCardId:String(row.target_event_card_id),evidenceKind:row.evidence_kind as Exclude<StoryEvidenceKind,"migration">,chapterDocumentId:asText(row.chapter_document_id),bodyVersionId:asText(row.body_version_id),textAnchorId:asText(row.text_anchor_id),factId:asText(row.fact_id),planVersionId:asText(row.plan_version_id),stateProposalId:asText(row.state_proposal_id),confidence:asNumber(row.confidence),reason:String(row.reason)};}
function mapRelationVersion(row:Record<string,unknown>):StoryRelationProposalVersion{return{id:String(row.id),proposalId:String(row.proposal_id),version:Number(row.version),...mapRelationFields(row),editor:String(row.editor),createdAt:asDate(row.created_at)};}
function mapRelationReview(row:Record<string,unknown>):StoryRelationReviewAction{return{id:String(row.id),proposalId:String(row.proposal_id),proposalVersionId:String(row.proposal_version_id),action:row.action as StoryRelationReviewAction["action"],actor:String(row.actor),note:String(row.note??""),idempotencyKey:asText(row.idempotency_key),createdAt:asDate(row.created_at)};}
function mapRelation(row:Record<string,unknown>):StoryEventRelation{return{id:String(row.id),sequence:Number(row.sequence),bookId:String(row.book_id),proposalId:String(row.proposal_id),proposalVersionId:String(row.proposal_version_id),relationFamily:row.relation_family as StoryRelationFamily,relationType:row.relation_type as StoryEventRelation["relationType"],sourceEventCardId:String(row.source_event_card_id),targetEventCardId:String(row.target_event_card_id),confidence:asNumber(row.confidence),status:row.status as StoryEventRelation["status"],confirmedBy:String(row.confirmed_by),reason:String(row.reason),createdAt:asDate(row.created_at)};}

async function requireBook(client:PoolClient,bookId:string){return assertFound((await client.query("SELECT * FROM new_design.books WHERE id=$1 AND status='active'",[bookId])).rows[0],"书籍不存在或已归档。");}
async function requireEvent(client:PoolClient,bookId:string,eventCardId:string,label="事件"){const row=await client.query("SELECT card.id,card.values FROM new_design.books book JOIN new_design.cards card ON card.space_id=book.space_id JOIN new_design.card_types type ON type.id=card.card_type_id WHERE book.id=$1 AND card.id=$2 AND card.status='active' AND type.type_key='event'",[bookId,eventCardId]);if(!row.rowCount)throw new NewDesignError(`${label}不属于该书或不是有效事件。`,422);return row.rows[0];}
async function requireCardType(client:PoolClient,bookId:string,cardId:string,typeKey:string,label:string){const row=await client.query("SELECT 1 FROM new_design.books book JOIN new_design.cards card ON card.space_id=book.space_id JOIN new_design.card_types type ON type.id=card.card_type_id WHERE book.id=$1 AND card.id=$2 AND card.status='active' AND type.type_key=$3",[bookId,cardId,typeKey]);if(!row.rowCount)throw new NewDesignError(`${label}不属于该书或类型不正确。`,422);}

function validateTimeValue(eventCardId:string,input:StoryTimeVersionInput){
  const start=input.normalizedStart??null,end=input.normalizedEnd??null;
  if(start!==null&&end!==null&&end<start)throw new NewDesignError("故事结束时间不能早于开始时间。",422);
  if(input.startInstant&&input.endInstant&&new Date(input.endInstant).getTime()<new Date(input.startInstant).getTime())throw new NewDesignError("故事结束时刻不能早于开始时刻。",422);
  if(input.durationValue!==undefined&&input.durationValue!==null&&input.durationValue<=0)throw new NewDesignError("时长必须大于零。",422);
  if(input.relativeToEventCardId===eventCardId)throw new NewDesignError("事件不能把自己作为相对时间参照。",422);
  if(input.timeMode==="absolute"&&!input.startInstant)throw new NewDesignError("绝对时间必须提供开始时刻。",422);
  if(input.timeMode==="custom_calendar"&&(!input.calendarKey||!input.startLabel))throw new NewDesignError("自定义历法必须提供历法口径和开始标签。",422);
  if(input.timeMode==="relative"&&(!input.relativeToEventCardId||!input.relativeRelation))throw new NewDesignError("相对时间必须选择参照事件和前后关系。",422);
  if(input.timeMode==="partial"&&!input.startLabel&&!input.endLabel&&!input.calendarKey)throw new NewDesignError("部分未知时间至少需要一个可读标签或历法口径。",422);
  if(input.timeMode==="unknown"&&(input.startInstant||input.endInstant||start!==null||end!==null||input.relativeToEventCardId))throw new NewDesignError("未知时间不能伪装成确定时间坐标。",422);
}

async function validateEvidence(client:PoolClient,bookId:string,input:EvidenceInput,requireAdopted=false){
  const refs=[input.chapterDocumentId,input.bodyVersionId,input.textAnchorId,input.factId,input.planVersionId,input.stateProposalId].filter(Boolean);
  if(input.evidenceKind==="manual"){if(refs.length)throw new NewDesignError("人工来源不能混入其他来源引用。",422);return;}
  if(input.evidenceKind==="body"){
    if(!input.chapterDocumentId||!input.bodyVersionId||!input.textAnchorId)throw new NewDesignError("正文来源必须同时指定章节档案、正文版本和精确锚点。",422);
    const row=await client.query(`SELECT document.adopted_version_id FROM new_design.chapter_documents document JOIN new_design.chapter_body_versions version ON version.chapter_document_id=document.id JOIN new_design.text_anchors anchor ON anchor.body_version_id=version.id WHERE document.id=$1 AND version.id=$2 AND anchor.id=$3 AND document.book_id=$4`,[input.chapterDocumentId,input.bodyVersionId,input.textAnchorId,bookId]);
    if(!row.rowCount)throw new NewDesignError("正文时间证据不属于该书或锚点不匹配。",422);
    if(requireAdopted&&String(row.rows[0].adopted_version_id??"")!==input.bodyVersionId)throw new NewDesignError("只能确认当前采用正文中的故事时间或关系。",409);
    if(refs.length!==3)throw new NewDesignError("正文来源不能混入其他来源引用。",422);
    return;
  }
  if(input.evidenceKind==="fact"){
    if(!input.factId||refs.length!==1)throw new NewDesignError("事实来源必须且只能引用一条已确认事实。",422);
    const row=await client.query(`WITH ${storyRecordCtes.canonical_facts}
SELECT 1 FROM canonical_facts WHERE id=$1 AND book_id=$2 AND status='confirmed'`,[input.factId,bookId]);if(!row.rowCount)throw new NewDesignError("时间来源事实不存在或尚未确认。",422);return;
  }
  if(input.evidenceKind==="plan_version"){if(!input.planVersionId||refs.length!==1)throw new NewDesignError("计划来源必须且只能记录上游计划版本 ID。",422);const row=await client.query(`WITH ${storyRecordCtes.planning_versions},
${storyRecordCtes.planning_objects}
SELECT 1 FROM planning_versions version JOIN planning_objects object ON object.id=version.object_id WHERE version.id=$1 AND version.book_id=$2 AND object.adopted_version_id=version.id AND version.status='adopted' AND version.stale_at IS NULL`,[input.planVersionId,bookId]);if(!row.rowCount)throw new NewDesignError("计划版本不属于该书、尚未采用或正在等待复核。",422);return;}
  if(!input.stateProposalId||refs.length!==1)throw new NewDesignError("状态来源必须且只能引用一条状态提案。",422);
  const row=await client.query(`WITH ${storyRecordCtes.state_change_proposals}
SELECT 1 FROM state_change_proposals WHERE id=$1 AND book_id=$2 AND status IN ('proposed','confirmed')`,[input.stateProposalId,bookId]);if(!row.rowCount)throw new NewDesignError("状态提案来源不存在或已经失效。",422);
}

async function insertTimeVersion(client:PoolClient,proposalId:string,version:number,input:StoryTimeVersionInput):Promise<StoryTimeProposalVersion>{const row=(await insertStoryRecords(client,"story_time_proposal_version",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::integer AS version,($4)::text AS lifecycle,($5)::text AS time_mode,($6)::text AS start_certainty,($7)::text AS end_certainty,($8)::timestamptz AS start_instant,($9)::timestamptz AS end_instant,($10)::text AS timezone_name,($11)::text AS calendar_key,($12)::text AS start_label,($13)::text AS end_label,($14)::numeric AS normalized_start,($15)::numeric AS normalized_end,($16)::numeric AS duration_value,($17)::text AS duration_unit,($18)::uuid AS relative_to_event_card_id,($19)::text AS relative_relation,($20)::numeric AS relative_offset,($21)::text AS evidence_kind,($22)::uuid AS chapter_document_id,($23)::uuid AS body_version_id,($24)::uuid AS text_anchor_id,($25)::uuid AS fact_id,($26)::uuid AS plan_version_id,($27)::uuid AS state_proposal_id,($28)::uuid AS replaces_timing_id,($29)::text AS reason,($30)::text AS editor`,
  [randomUUID(),proposalId,version,input.lifecycle,input.timeMode,input.startCertainty,input.endCertainty,input.startInstant??null,input.endInstant??null,input.timezoneName??null,input.calendarKey??null,input.startLabel??null,input.endLabel??null,input.normalizedStart??null,input.normalizedEnd??null,input.durationValue??null,input.durationUnit??null,input.relativeToEventCardId??null,input.relativeRelation??null,input.relativeOffset??null,input.evidenceKind,input.chapterDocumentId??null,input.bodyVersionId??null,input.textAnchorId??null,input.factId??null,input.planVersionId??null,input.stateProposalId??null,input.replacesTimingId??null,input.reason,input.editor??"system"])).rows[0];return mapTimeVersion(row);}

export async function getStoryTimeProposal(id:string,transaction?:PoolClient):Promise<StoryTimeProposal>{const pool=transaction??await getNewDesignPool(),proposal=assertFound((await pool.query(`WITH ${storyRecordCtes.story_time_proposals}
SELECT * FROM story_time_proposals WHERE id=$1`,[id])).rows[0],"故事时间提案不存在。"),[versions,reviews]=await Promise.all([pool.query(`WITH ${storyRecordCtes.story_time_proposal_versions}
SELECT * FROM story_time_proposal_versions WHERE proposal_id=$1 ORDER BY version DESC`,[id]),pool.query(`WITH ${storyRecordCtes.story_time_review_actions}
SELECT * FROM story_time_review_actions WHERE proposal_id=$1 ORDER BY created_at,id`,[id])]),mapped=versions.rows.map(mapTimeVersion);return{id:String(proposal.id),bookId:String(proposal.book_id),eventCardId:String(proposal.event_card_id),proposalSource:proposal.proposal_source,status:proposal.status,currentVersionId:String(proposal.current_version_id),confirmedTimingId:asText(proposal.confirmed_timing_id),revision:Number(proposal.revision),currentVersion:assertFound(mapped.find((item)=>item.id===String(proposal.current_version_id)),"故事时间提案当前版本不存在。"),versions:mapped,reviewActions:reviews.rows.map(mapTimeReview),createdAt:asDate(proposal.created_at),updatedAt:asDate(proposal.updated_at)};}
export async function listStoryTimeProposals(bookId:string,status?:StoryTimeProposal["status"]):Promise<StoryTimeProposal[]>{const rows=await(await getNewDesignPool()).query(`WITH ${storyRecordCtes.story_time_proposals}
SELECT id FROM story_time_proposals WHERE book_id=$1 AND ($2::text IS NULL OR status=$2) ORDER BY created_at,id`,[bookId,status??null]);return Promise.all(rows.rows.map((row)=>getStoryTimeProposal(String(row.id))));}

export async function proposeStoryTime(input:{bookId:string;eventCardId:string;proposalSource:StoryTimeProposalSource}&StoryTimeVersionInput,transaction?:PoolClient):Promise<StoryTimeProposal>{validateTimeValue(input.eventCardId,input);const pool=await getNewDesignPool(),client=transaction??await pool.connect();try{if(!transaction)await client.query("BEGIN");await requireBook(client,input.bookId);await requireEvent(client,input.bookId,input.eventCardId);if(input.relativeToEventCardId)await requireEvent(client,input.bookId,input.relativeToEventCardId,"相对时间参照事件");await validateEvidence(client,input.bookId,input);if(input.proposalSource==="ai"&&input.evidenceKind==="manual")throw new NewDesignError("AI 故事时间提案必须记录可追溯来源。",422);const id=randomUUID();await insertStoryRecords(client,"story_time_proposal",`SELECT ($1)::uuid AS id,($2)::uuid AS book_id,($3)::uuid AS event_card_id,($4)::text AS proposal_source`,[id,input.bookId,input.eventCardId,input.proposalSource]);const version=await insertTimeVersion(client,id,1,input);await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals}
SELECT story_time_proposals.*,($2)::uuid AS current_version_id FROM story_time_proposals WHERE id=$1`,[id,version.id]);await insertStoryReviewActions(client,"story_time",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('propose')::text AS action,($4)::text AS actor,($5)::text AS note`,[randomUUID(),id,version.id,input.editor??input.proposalSource,input.reason]);const saved=await getStoryTimeProposal(id,client);if(!transaction)await client.query("COMMIT");return saved;}catch(error){if(!transaction)await client.query("ROLLBACK");throw error;}finally{if(!transaction)client.release();}}

export async function editStoryTimeProposal(id:string,input:StoryTimeVersionInput&{expectedRevision:number;actor?:string;note?:string},transaction?:PoolClient):Promise<StoryTimeProposal>{const pool=await getNewDesignPool(),client=transaction??await pool.connect();try{if(!transaction)await client.query("BEGIN");const proposal=assertFound((await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_time_proposals}
SELECT * FROM story_time_proposals WHERE id=$1`,[id])).rows[0],"故事时间提案不存在。");if(!["proposed","stale"].includes(String(proposal.status)))throw new NewDesignError("只有待确认或待复核故事时间可以修改。",409);if(Number(proposal.revision)!==input.expectedRevision)throw new NewDesignError("故事时间提案已更新，请刷新后重试。",409);validateTimeValue(String(proposal.event_card_id),input);if(input.relativeToEventCardId)await requireEvent(client,String(proposal.book_id),input.relativeToEventCardId,"相对时间参照事件");await validateEvidence(client,String(proposal.book_id),input);const next=Number((await client.query(`WITH ${storyRecordCtes.story_time_proposal_versions}
SELECT COALESCE(max(version),0)+1 AS version FROM story_time_proposal_versions WHERE proposal_id=$1`,[id])).rows[0].version),version=await insertTimeVersion(client,id,next,{...input,editor:input.actor??input.editor??"user"});await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals}
SELECT story_time_proposals.*,($2)::uuid AS current_version_id,('proposed')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_time_proposals WHERE id=$1`,[id,version.id]);await insertStoryReviewActions(client,"story_time",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('edit')::text AS action,($4)::text AS actor,($5)::text AS note`,[randomUUID(),id,version.id,input.actor??"user",input.note??""]);const saved=await getStoryTimeProposal(id,client);if(!transaction)await client.query("COMMIT");return saved;}catch(error){if(!transaction)await client.query("ROLLBACK");throw error;}finally{if(!transaction)client.release();}}

async function syncLegacyTimeProjection(client:PoolClient,timing:Record<string,unknown>){const book=await client.query("SELECT space_id FROM new_design.books WHERE id=$1",[timing.book_id]);if(!book.rowCount)return;if(!["planned","occurred"].includes(String(timing.lifecycle))){await deleteStoryRecords(client,"story_time_position",`WITH ${storyRecordCtes.story_time_positions}
SELECT story_time_positions.* FROM story_time_positions WHERE space_id=$1 AND card_id=$2`,[book.rows[0].space_id,timing.event_card_id]);return;}await insertStoryRecords(client,"story_time_position",`SELECT ($1)::uuid AS id,($2)::uuid AS space_id,($3)::uuid AS card_id,($4)::numeric(14,3) AS start_order,($5)::numeric(14,3) AS end_order,($6)::text AS start_label,($7)::text AS end_label,($8)::text AS uncertainty,($9)::uuid AS canonical_timing_id`,
  [randomUUID(),book.rows[0].space_id,timing.event_card_id,timing.normalized_start,timing.normalized_end,timing.start_label??"",timing.end_label??"",timing.start_certainty==="known"&&timing.end_certainty==="known"?"":`${timing.start_certainty}/${timing.end_certainty}`,timing.id],{keys:["space_id","card_id"],update:(previous,incoming)=>({start_order:incoming.start_order,end_order:incoming.end_order,start_label:incoming.start_label,end_label:incoming.end_label,uncertainty:incoming.uncertainty,canonical_timing_id:incoming.canonical_timing_id,revision:Number(previous.revision)+1,updated_at:new Date().toISOString()})});}

async function insertOccurrenceFromBody(client:PoolClient,bookId:string,eventCardId:string,version:Record<string,unknown>){if(!version.chapter_document_id||!version.body_version_id||!version.text_anchor_id)return;const document=await client.query("SELECT chapter_card_id,logical_order FROM new_design.chapter_documents WHERE id=$1",[version.chapter_document_id]);if(!document.rowCount)return;await insertStoryRecords(client,"story_event_narrative_occurrence",`SELECT ($1)::uuid AS id,($2)::uuid AS book_id,($3)::uuid AS event_card_id,($4)::uuid AS chapter_card_id,($5)::uuid AS chapter_document_id,($6)::uuid AS body_version_id,($7)::uuid AS text_anchor_id,($8)::uuid AS source_time_proposal_version_id,('mention')::text AS role,($9)::numeric AS narrative_order,('body')::text AS source_kind,($10)::text AS note`,
  [randomUUID(),bookId,eventCardId,document.rows[0].chapter_card_id,version.chapter_document_id,version.body_version_id,version.text_anchor_id,version.id,document.rows[0].logical_order,"由已确认故事时间的正文证据建立。"],{ignore:true});}

export async function reviewStoryTimeProposal(id:string,input:{action:"confirm"|"reject";expectedRevision:number;idempotencyKey:string;actor?:string;note?:string},transaction?:PoolClient):Promise<StoryTimeProposal>{const pool=await getNewDesignPool(),repeated=await(transaction??pool).query(`WITH ${storyRecordCtes.story_time_review_actions}
SELECT proposal_id FROM story_time_review_actions WHERE idempotency_key=$1`,[input.idempotencyKey]);if(repeated.rows[0]){if(String(repeated.rows[0].proposal_id)!==id)throw new NewDesignError("幂等键已用于其他故事时间提案。",409);return getStoryTimeProposal(id,transaction);}const client=transaction??await pool.connect();try{if(!transaction)await client.query("BEGIN");const proposal=assertFound((await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_time_proposals}
SELECT * FROM story_time_proposals WHERE id=$1`,[id])).rows[0],"故事时间提案不存在。");if(proposal.status!=="proposed")throw new NewDesignError("故事时间提案已经处理，请刷新后重试。",409);if(Number(proposal.revision)!==input.expectedRevision)throw new NewDesignError("故事时间提案已更新，请刷新后重试。",409);const version=assertFound((await client.query(`WITH ${storyRecordCtes.story_time_proposal_versions}
SELECT * FROM story_time_proposal_versions WHERE id=$1`,[proposal.current_version_id])).rows[0],"故事时间提案当前版本不存在。");if(input.action==="confirm"){
    await validateEvidence(client,String(proposal.book_id),{evidenceKind:version.evidence_kind,chapterDocumentId:version.chapter_document_id,bodyVersionId:version.body_version_id,textAnchorId:version.text_anchor_id,factId:version.fact_id,planVersionId:version.plan_version_id,stateProposalId:version.state_proposal_id},true);
    let prior:Record<string,unknown>|undefined;
    if(version.replaces_timing_id){prior=assertFound((await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE id=$1 AND book_id=$2 AND event_card_id=$3 AND status='active'`,[version.replaces_timing_id,proposal.book_id,proposal.event_card_id])).rows[0],"待替换的故事时间不存在或已经失效。");if(version.lifecycle==="occurred"&&String(prior?.lifecycle)!=="planned")throw new NewDesignError("只有计划发生的事件可以转为实际发生。",422);}
    else{const active=await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_event_timings}
SELECT 1 FROM story_event_timings WHERE book_id=$1 AND event_card_id=$2 AND status='active'`,[proposal.book_id,proposal.event_card_id]);if(active.rowCount)throw new NewDesignError("事件已有有效故事时间，请通过替换关系提交修订。",409);}
    if(prior)await updateStoryRecords(client,"story_event_timing",`WITH ${storyRecordCtes.story_event_timings}
SELECT story_event_timings.*,(CASE WHEN $2 IN ('cancelled','invalidated') THEN 'invalidated' ELSE 'superseded' END)::text AS status FROM story_event_timings WHERE id=$1`,[prior.id,version.lifecycle]);
    const timingId=randomUUID(),timing=(await insertStoryRecords(client,"story_event_timing",`WITH ${storyRecordCtes.story_time_proposals},
${storyRecordCtes.story_time_proposal_versions}
SELECT ($1)::uuid AS id,(proposal.book_id)::uuid AS book_id,(proposal.event_card_id)::uuid AS event_card_id,(proposal.id)::uuid AS proposal_id,(version.id)::uuid AS proposal_version_id,(version.lifecycle)::text AS lifecycle,(version.time_mode)::text AS time_mode,(version.start_certainty)::text AS start_certainty,(version.end_certainty)::text AS end_certainty,(version.start_instant)::timestamptz AS start_instant,(version.end_instant)::timestamptz AS end_instant,(version.timezone_name)::text AS timezone_name,(version.calendar_key)::text AS calendar_key,(version.start_label)::text AS start_label,(version.end_label)::text AS end_label,(version.normalized_start)::numeric AS normalized_start,(version.normalized_end)::numeric AS normalized_end,(version.duration_value)::numeric AS duration_value,(version.duration_unit)::text AS duration_unit,(version.relative_to_event_card_id)::uuid AS relative_to_event_card_id,(version.relative_relation)::text AS relative_relation,(version.relative_offset)::numeric AS relative_offset,(version.evidence_kind)::text AS evidence_kind,(version.chapter_document_id)::uuid AS chapter_document_id,(version.body_version_id)::uuid AS body_version_id,(version.text_anchor_id)::uuid AS text_anchor_id,(version.fact_id)::uuid AS fact_id,(version.plan_version_id)::uuid AS plan_version_id,(version.state_proposal_id)::uuid AS state_proposal_id,(version.replaces_timing_id)::uuid AS replaces_timing_id,($2)::text AS confirmed_by,(version.reason)::text AS reason
       FROM story_time_proposals proposal JOIN story_time_proposal_versions version ON version.id=proposal.current_version_id WHERE proposal.id=$3`,
      [timingId,input.actor??"user",id])).rows[0];
    await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals}
SELECT story_time_proposals.*,('confirmed')::text AS status,($2)::uuid AS confirmed_timing_id,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_time_proposals WHERE id=$1`,[id,timingId]);await syncLegacyTimeProjection(client,timing);await insertOccurrenceFromBody(client,String(proposal.book_id),String(proposal.event_card_id),version);
  }else await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals}
SELECT story_time_proposals.*,('rejected')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_time_proposals WHERE id=$1`,[id]);
  await insertStoryReviewActions(client,"story_time",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,($4)::text AS action,($5)::text AS actor,($6)::text AS note,($7)::text AS idempotency_key`,[randomUUID(),id,version.id,input.action,input.actor??"user",input.note??"",input.idempotencyKey]);const saved=await getStoryTimeProposal(id,client);if(!transaction)await client.query("COMMIT");return saved;}catch(error){if(transaction)throw error;await client.query("ROLLBACK");if((error as {code?:string}).code==="23505"){const found=await pool.query(`WITH ${storyRecordCtes.story_time_review_actions}
SELECT proposal_id FROM story_time_review_actions WHERE idempotency_key=$1`,[input.idempotencyKey]);if(String(found.rows[0]?.proposal_id)===id)return getStoryTimeProposal(id);}throw error;}finally{if(!transaction)client.release();}}

export async function saveLegacyStoryTimeFromView(client:PoolClient,bookId:string,input:{eventCardId:string;startOrder:number|null;endOrder:number|null;startLabel:string;endLabel:string;uncertainty:string;actor?:string}):Promise<void>{
  const event=await requireEvent(client,bookId,input.eventCardId),active=(await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE book_id=$1 AND event_card_id=$2 AND status='active'`,[bookId,input.eventCardId])).rows[0],proposalId=randomUUID();
  await insertStoryRecords(client,"story_time_proposal",`SELECT ($1)::uuid AS id,($2)::uuid AS book_id,($3)::uuid AS event_card_id,('manual')::text AS proposal_source`,[proposalId,bookId,input.eventCardId]);
  const values=(event.values??{}) as Record<string,unknown>,lifecycle:StoryEventLifecycle=values.event_status==="completed"?"occurred":"planned",hasOrder=input.startOrder!==null||input.endOrder!==null,hasLabel=Boolean(input.startLabel||input.endLabel),version=await insertTimeVersion(client,proposalId,1,{lifecycle,timeMode:hasOrder?"custom_calendar":hasLabel?"partial":"unknown",startCertainty:input.startOrder!==null?"known":input.startLabel?"partial":"unknown",endCertainty:input.endOrder!==null?"known":input.endLabel?"partial":"unknown",calendarKey:hasOrder?"legacy-order":null,startLabel:input.startLabel||null,endLabel:input.endLabel||null,normalizedStart:input.startOrder,normalizedEnd:input.endOrder,evidenceKind:"manual",replacesTimingId:active?String(active.id):null,reason:input.uncertainty||"用户从事件视图确认故事时间。",editor:input.actor??"user"});
  await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals}
SELECT story_time_proposals.*,($2)::uuid AS current_version_id FROM story_time_proposals WHERE id=$1`,[proposalId,version.id]);if(active)await updateStoryRecords(client,"story_event_timing",`WITH ${storyRecordCtes.story_event_timings}
SELECT story_event_timings.*,('superseded')::text AS status FROM story_event_timings WHERE id=$1`,[active.id]);
  const timingId=randomUUID(),timing=(await insertStoryRecords(client,"story_event_timing",`SELECT ($1)::uuid AS id,($2)::uuid AS book_id,($3)::uuid AS event_card_id,($4)::uuid AS proposal_id,($5)::uuid AS proposal_version_id,($6)::text AS lifecycle,($7)::text AS time_mode,($8)::text AS start_certainty,($9)::text AS end_certainty,($10)::text AS calendar_key,($11)::text AS start_label,($12)::text AS end_label,($13)::numeric AS normalized_start,($14)::numeric AS normalized_end,('manual')::text AS evidence_kind,($15)::uuid AS replaces_timing_id,($16)::text AS confirmed_by,($17)::text AS reason`,[timingId,bookId,input.eventCardId,proposalId,version.id,lifecycle,version.timeMode,version.startCertainty,version.endCertainty,version.calendarKey,version.startLabel,version.endLabel,version.normalizedStart,version.normalizedEnd,active?.id??null,input.actor??"user",version.reason])).rows[0];
  await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals}
SELECT story_time_proposals.*,('confirmed')::text AS status,($2)::uuid AS confirmed_timing_id,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_time_proposals WHERE id=$1`,[proposalId,timingId]);await insertStoryReviewActions(client,"story_time",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('propose')::text AS action,($4)::text AS actor,($5)::text AS note
UNION ALL
SELECT ($6)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('confirm')::text AS action,($4)::text AS actor,($5)::text AS note`,[randomUUID(),proposalId,version.id,input.actor??"user",version.reason,randomUUID()]);await syncLegacyTimeProjection(client,timing);
}

export async function listCurrentStoryTimings(bookId:string):Promise<StoryEventTiming[]>{const rows=await(await getNewDesignPool()).query(`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE book_id=$1 AND status='active' ORDER BY normalized_start NULLS LAST,start_instant NULLS LAST,sequence`,[bookId]);return rows.rows.map(mapTiming);}
export async function listStoryTimingsInRange(bookId:string,input:{normalizedStart?:number;normalizedEnd?:number;startInstant?:string;endInstant?:string}):Promise<StoryEventTiming[]>{const hasNormalized=input.normalizedStart!==undefined||input.normalizedEnd!==undefined,hasAbsolute=Boolean(input.startInstant||input.endInstant);if(hasNormalized===hasAbsolute)throw new NewDesignError("故事时间范围必须且只能使用一种时间口径。",422);const pool=await getNewDesignPool();let rows;if(hasNormalized){const from=input.normalizedStart??input.normalizedEnd,to=input.normalizedEnd??input.normalizedStart;if(from===undefined||to===undefined||to<from)throw new NewDesignError("故事时间范围无效。",422);rows=await pool.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE book_id=$1 AND status='active' AND lifecycle IN ('planned','occurred') AND normalized_start IS NOT NULL AND COALESCE(normalized_end,normalized_start)>=$2 AND normalized_start<=$3 ORDER BY normalized_start,sequence`,[bookId,from,to]);}else{const from=input.startInstant??input.endInstant,to=input.endInstant??input.startInstant;if(!from||!to||Number.isNaN(Date.parse(from))||Number.isNaN(Date.parse(to))||new Date(to).getTime()<new Date(from).getTime())throw new NewDesignError("绝对时间范围无效。",422);rows=await pool.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE book_id=$1 AND status='active' AND lifecycle IN ('planned','occurred') AND start_instant IS NOT NULL AND COALESCE(end_instant,start_instant)>=$2::timestamptz AND start_instant<=$3::timestamptz ORDER BY start_instant,sequence`,[bookId,from,to]);}return rows.rows.map(mapTiming);}

export async function saveStoryNarrativeOccurrence(input:{id?:string;bookId:string;eventCardId:string;chapterCardId:string;sceneCardId?:string|null;chapterDocumentId?:string|null;bodyVersionId?:string|null;textAnchorId?:string|null;role:StoryNarrativeOccurrence["role"];narrativeOrder?:number|null;sourceKind:StoryNarrativeOccurrence["sourceKind"];note?:string;expectedRevision?:number},transaction?:PoolClient):Promise<StoryNarrativeOccurrence>{const pool=await getNewDesignPool(),client=transaction??await pool.connect();try{if(!transaction)await client.query("BEGIN");await requireEvent(client,input.bookId,input.eventCardId);await requireCardType(client,input.bookId,input.chapterCardId,"chapter","叙事章节");if(input.sceneCardId)await requireCardType(client,input.bookId,input.sceneCardId,"scene","叙事场景");if(input.sourceKind==="body")await validateEvidence(client,input.bookId,{evidenceKind:"body",chapterDocumentId:input.chapterDocumentId,bodyVersionId:input.bodyVersionId,textAnchorId:input.textAnchorId},false);else if(input.chapterDocumentId||input.bodyVersionId||input.textAnchorId)throw new NewDesignError("只有正文来源可以绑定正文版本和锚点。",422);let row;if(input.id){const current=assertFound((await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_event_narrative_occurrences}
SELECT * FROM story_event_narrative_occurrences WHERE id=$1 AND book_id=$2`,[input.id,input.bookId])).rows[0],"叙事出现记录不存在。");if(Number(current.revision)!==input.expectedRevision)throw new NewDesignError("叙事出现记录已更新，请刷新后重试。",409);row=(await updateStoryRecords(client,"story_event_narrative_occurrence",`WITH ${storyRecordCtes.story_event_narrative_occurrences}
SELECT story_event_narrative_occurrences.*,($2)::uuid AS event_card_id,($3)::uuid AS chapter_card_id,($4)::uuid AS scene_card_id,($5)::uuid AS chapter_document_id,($6)::uuid AS body_version_id,($7)::uuid AS text_anchor_id,($8)::text AS role,($9)::numeric AS narrative_order,($10)::text AS source_kind,($11)::text AS note,('active')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_event_narrative_occurrences WHERE id=$1`,[input.id,input.eventCardId,input.chapterCardId,input.sceneCardId??null,input.chapterDocumentId??null,input.bodyVersionId??null,input.textAnchorId??null,input.role,input.narrativeOrder??null,input.sourceKind,input.note??""])).rows[0];}else row=(await insertStoryRecords(client,"story_event_narrative_occurrence",`SELECT ($1)::uuid AS id,($2)::uuid AS book_id,($3)::uuid AS event_card_id,($4)::uuid AS chapter_card_id,($5)::uuid AS scene_card_id,($6)::uuid AS chapter_document_id,($7)::uuid AS body_version_id,($8)::uuid AS text_anchor_id,($9)::text AS role,($10)::numeric AS narrative_order,($11)::text AS source_kind,($12)::text AS note`,[randomUUID(),input.bookId,input.eventCardId,input.chapterCardId,input.sceneCardId??null,input.chapterDocumentId??null,input.bodyVersionId??null,input.textAnchorId??null,input.role,input.narrativeOrder??null,input.sourceKind,input.note??""])).rows[0];const saved=mapOccurrence(row);if(!transaction)await client.query("COMMIT");return saved;}catch(error){if(transaction)throw error;await client.query("ROLLBACK");if((error as {code?:string}).code==="23505")throw new NewDesignError("同一事件在该章节已有相同叙事出现记录。",409);throw error;}finally{if(!transaction)client.release();}}
export async function listStoryOccurrencesByChapter(bookId:string,chapterCardId:string):Promise<StoryNarrativeOccurrence[]>{const rows=await(await getNewDesignPool()).query(`WITH ${storyRecordCtes.story_event_narrative_occurrences}
SELECT * FROM story_event_narrative_occurrences WHERE book_id=$1 AND chapter_card_id=$2 AND status='active' ORDER BY narrative_order NULLS LAST,created_at,id`,[bookId,chapterCardId]);return rows.rows.map(mapOccurrence);}

function normalizeRelation(input:StoryRelationVersionInput):StoryRelationVersionInput{if(input.relationType==="after")return{...input,relationType:"before",sourceEventCardId:input.targetEventCardId,targetEventCardId:input.sourceEventCardId};if(["simultaneous","overlaps"].includes(input.relationType)&&input.sourceEventCardId>input.targetEventCardId)return{...input,sourceEventCardId:input.targetEventCardId,targetEventCardId:input.sourceEventCardId};return input;}
function validateRelation(input:StoryRelationVersionInput){if(input.sourceEventCardId===input.targetEventCardId)throw new NewDesignError("事件不能与自己建立时间或因果关系。",422);const temporal=["before","after","simultaneous","overlaps","contains"].includes(input.relationType);if((input.relationFamily==="temporal")!==temporal)throw new NewDesignError("关系类型与时间／因果分类不一致。",422);if(input.confidence!==undefined&&input.confidence!==null&&(input.confidence<0||input.confidence>1))throw new NewDesignError("关系置信度必须在 0 到 1 之间。",422);}
async function insertRelationVersion(client:PoolClient,proposalId:string,version:number,input:StoryRelationVersionInput):Promise<StoryRelationProposalVersion>{const normalized=normalizeRelation(input),row=(await insertStoryRecords(client,"story_relation_proposal_version",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::integer AS version,($4)::text AS relation_family,($5)::text AS relation_type,($6)::uuid AS source_event_card_id,($7)::uuid AS target_event_card_id,($8)::text AS evidence_kind,($9)::uuid AS chapter_document_id,($10)::uuid AS body_version_id,($11)::uuid AS text_anchor_id,($12)::uuid AS fact_id,($13)::uuid AS plan_version_id,($14)::uuid AS state_proposal_id,($15)::numeric AS confidence,($16)::text AS reason,($17)::text AS editor`,[randomUUID(),proposalId,version,normalized.relationFamily,normalized.relationType,normalized.sourceEventCardId,normalized.targetEventCardId,normalized.evidenceKind,normalized.chapterDocumentId??null,normalized.bodyVersionId??null,normalized.textAnchorId??null,normalized.factId??null,normalized.planVersionId??null,normalized.stateProposalId??null,normalized.confidence??null,normalized.reason,normalized.editor??"system"])).rows[0];return mapRelationVersion(row);}
export async function getStoryRelationProposal(id:string,transaction?:PoolClient):Promise<StoryRelationProposal>{const pool=transaction??await getNewDesignPool(),proposal=assertFound((await pool.query(`WITH ${storyRecordCtes.story_relation_proposals}
SELECT * FROM story_relation_proposals WHERE id=$1`,[id])).rows[0],"事件关系提案不存在。"),[versions,reviews]=await Promise.all([pool.query(`WITH ${storyRecordCtes.story_relation_proposal_versions}
SELECT * FROM story_relation_proposal_versions WHERE proposal_id=$1 ORDER BY version DESC`,[id]),pool.query(`WITH ${storyRecordCtes.story_relation_review_actions}
SELECT * FROM story_relation_review_actions WHERE proposal_id=$1 ORDER BY created_at,id`,[id])]),mapped=versions.rows.map(mapRelationVersion);return{id:String(proposal.id),bookId:String(proposal.book_id),proposalSource:proposal.proposal_source,status:proposal.status,currentVersionId:String(proposal.current_version_id),confirmedRelationId:asText(proposal.confirmed_relation_id),revision:Number(proposal.revision),currentVersion:assertFound(mapped.find((item)=>item.id===String(proposal.current_version_id)),"事件关系提案当前版本不存在。"),versions:mapped,reviewActions:reviews.rows.map(mapRelationReview),createdAt:asDate(proposal.created_at),updatedAt:asDate(proposal.updated_at)};}
export async function listStoryRelationProposals(bookId:string,status?:StoryRelationProposal["status"]):Promise<StoryRelationProposal[]>{const rows=await(await getNewDesignPool()).query(`WITH ${storyRecordCtes.story_relation_proposals}
SELECT id FROM story_relation_proposals WHERE book_id=$1 AND ($2::text IS NULL OR status=$2) ORDER BY created_at,id`,[bookId,status??null]);return Promise.all(rows.rows.map((row)=>getStoryRelationProposal(String(row.id))));}
export async function proposeStoryRelation(input:{bookId:string;proposalSource:StoryTimeProposalSource}&StoryRelationVersionInput,transaction?:PoolClient):Promise<StoryRelationProposal>{validateRelation(input);const normalized=normalizeRelation(input),pool=await getNewDesignPool(),client=transaction??await pool.connect();try{if(!transaction)await client.query("BEGIN");await requireBook(client,input.bookId);await requireEvent(client,input.bookId,normalized.sourceEventCardId,"起点事件");await requireEvent(client,input.bookId,normalized.targetEventCardId,"目标事件");await validateEvidence(client,input.bookId,normalized);if(input.proposalSource==="ai"&&input.evidenceKind==="manual")throw new NewDesignError("AI 事件关系提案必须记录可追溯来源。",422);const id=randomUUID();await insertStoryRecords(client,"story_relation_proposal",`SELECT ($1)::uuid AS id,($2)::uuid AS book_id,($3)::text AS proposal_source`,[id,input.bookId,input.proposalSource]);const version=await insertRelationVersion(client,id,1,normalized);await updateStoryRecords(client,"story_relation_proposal",`WITH ${storyRecordCtes.story_relation_proposals}
SELECT story_relation_proposals.*,($2)::uuid AS current_version_id FROM story_relation_proposals WHERE id=$1`,[id,version.id]);await insertStoryReviewActions(client,"story_relation",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('propose')::text AS action,($4)::text AS actor,($5)::text AS note`,[randomUUID(),id,version.id,input.editor??input.proposalSource,input.reason]);const saved=await getStoryRelationProposal(id,client);if(!transaction)await client.query("COMMIT");return saved;}catch(error){if(!transaction)await client.query("ROLLBACK");throw error;}finally{if(!transaction)client.release();}}
export async function editStoryRelationProposal(id:string,input:StoryRelationVersionInput&{expectedRevision:number;actor?:string;note?:string},transaction?:PoolClient):Promise<StoryRelationProposal>{validateRelation(input);const normalized=normalizeRelation(input),pool=await getNewDesignPool(),client=transaction??await pool.connect();try{if(!transaction)await client.query("BEGIN");const proposal=assertFound((await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_relation_proposals}
SELECT * FROM story_relation_proposals WHERE id=$1`,[id])).rows[0],"事件关系提案不存在。");if(!["proposed","stale"].includes(String(proposal.status)))throw new NewDesignError("只有待确认或待复核事件关系可以修改。",409);if(Number(proposal.revision)!==input.expectedRevision)throw new NewDesignError("事件关系提案已更新，请刷新后重试。",409);await requireEvent(client,String(proposal.book_id),normalized.sourceEventCardId,"起点事件");await requireEvent(client,String(proposal.book_id),normalized.targetEventCardId,"目标事件");await validateEvidence(client,String(proposal.book_id),normalized);const next=Number((await client.query(`WITH ${storyRecordCtes.story_relation_proposal_versions}
SELECT COALESCE(max(version),0)+1 AS version FROM story_relation_proposal_versions WHERE proposal_id=$1`,[id])).rows[0].version),version=await insertRelationVersion(client,id,next,{...normalized,editor:input.actor??input.editor??"user"});await updateStoryRecords(client,"story_relation_proposal",`WITH ${storyRecordCtes.story_relation_proposals}
SELECT story_relation_proposals.*,($2)::uuid AS current_version_id,('proposed')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_relation_proposals WHERE id=$1`,[id,version.id]);await insertStoryReviewActions(client,"story_relation",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('edit')::text AS action,($4)::text AS actor,($5)::text AS note`,[randomUUID(),id,version.id,input.actor??"user",input.note??""]);const saved=await getStoryRelationProposal(id,client);if(!transaction)await client.query("COMMIT");return saved;}catch(error){if(!transaction)await client.query("ROLLBACK");throw error;}finally{if(!transaction)client.release();}}
export async function reviewStoryRelationProposal(id:string,input:{action:"confirm"|"reject";expectedRevision:number;idempotencyKey:string;actor?:string;note?:string},transaction?:PoolClient):Promise<StoryRelationProposal>{const pool=await getNewDesignPool(),repeated=await(transaction??pool).query(`WITH ${storyRecordCtes.story_relation_review_actions}
SELECT proposal_id FROM story_relation_review_actions WHERE idempotency_key=$1`,[input.idempotencyKey]);if(repeated.rows[0]){if(String(repeated.rows[0].proposal_id)!==id)throw new NewDesignError("幂等键已用于其他事件关系提案。",409);return getStoryRelationProposal(id,transaction);}const client=transaction??await pool.connect();try{if(!transaction)await client.query("BEGIN");const proposal=assertFound((await lockedStoryRecordQuery(client,`WITH ${storyRecordCtes.story_relation_proposals}
SELECT * FROM story_relation_proposals WHERE id=$1`,[id])).rows[0],"事件关系提案不存在。");if(proposal.status!=="proposed")throw new NewDesignError("事件关系提案已经处理，请刷新后重试。",409);if(Number(proposal.revision)!==input.expectedRevision)throw new NewDesignError("事件关系提案已更新，请刷新后重试。",409);const version=assertFound((await client.query(`WITH ${storyRecordCtes.story_relation_proposal_versions}
SELECT * FROM story_relation_proposal_versions WHERE id=$1`,[proposal.current_version_id])).rows[0],"事件关系提案当前版本不存在。");if(input.action==="confirm"){await validateEvidence(client,String(proposal.book_id),{evidenceKind:version.evidence_kind,chapterDocumentId:version.chapter_document_id,bodyVersionId:version.body_version_id,textAnchorId:version.text_anchor_id,factId:version.fact_id,planVersionId:version.plan_version_id,stateProposalId:version.state_proposal_id},true);const relationId=randomUUID();await insertStoryRecords(client,"story_event_relation",`SELECT ($1)::uuid AS id,($2)::uuid AS book_id,($3)::uuid AS proposal_id,($4)::uuid AS proposal_version_id,($5)::text AS relation_family,($6)::text AS relation_type,($7)::uuid AS source_event_card_id,($8)::uuid AS target_event_card_id,($9)::numeric AS confidence,($10)::text AS confirmed_by,($11)::text AS reason`,[relationId,proposal.book_id,id,version.id,version.relation_family,version.relation_type,version.source_event_card_id,version.target_event_card_id,version.confidence,input.actor??"user",version.reason]);await updateStoryRecords(client,"story_relation_proposal",`WITH ${storyRecordCtes.story_relation_proposals}
SELECT story_relation_proposals.*,('confirmed')::text AS status,($2)::uuid AS confirmed_relation_id,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_relation_proposals WHERE id=$1`,[id,relationId]);}else await updateStoryRecords(client,"story_relation_proposal",`WITH ${storyRecordCtes.story_relation_proposals}
SELECT story_relation_proposals.*,('rejected')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_relation_proposals WHERE id=$1`,[id]);await insertStoryReviewActions(client,"story_relation",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,($4)::text AS action,($5)::text AS actor,($6)::text AS note,($7)::text AS idempotency_key`,[randomUUID(),id,version.id,input.action,input.actor??"user",input.note??"",input.idempotencyKey]);const saved=await getStoryRelationProposal(id,client);if(!transaction)await client.query("COMMIT");return saved;}catch(error){if(transaction)throw error;await client.query("ROLLBACK");if((error as {code?:string}).code==="23505"){const found=await pool.query(`WITH ${storyRecordCtes.story_relation_review_actions}
SELECT proposal_id FROM story_relation_review_actions WHERE idempotency_key=$1`,[input.idempotencyKey]);if(String(found.rows[0]?.proposal_id)===id)return getStoryRelationProposal(id);throw new NewDesignError("同一事件关系已经生效。",409);}throw error;}finally{if(!transaction)client.release();}}
export async function listStoryEventRelations(bookId:string,family?:StoryRelationFamily):Promise<StoryEventRelation[]>{const rows=await(await getNewDesignPool()).query(`WITH ${storyRecordCtes.story_event_relations}
SELECT * FROM story_event_relations WHERE book_id=$1 AND status='active' AND ($2::text IS NULL OR relation_family=$2) ORDER BY sequence`,[bookId,family??null]);return rows.rows.map(mapRelation);}
export async function listTemporalNeighbors(bookId:string,eventCardId:string):Promise<StoryEventRelation[]>{const rows=await(await getNewDesignPool()).query(`WITH ${storyRecordCtes.story_event_relations}
SELECT * FROM story_event_relations WHERE book_id=$1 AND status='active' AND relation_family='temporal' AND (source_event_card_id=$2 OR target_event_card_id=$2) ORDER BY sequence`,[bookId,eventCardId]);return rows.rows.map(mapRelation);}
export async function listConcurrentEvents(bookId:string,eventCardId:string):Promise<StoryEventTiming[]>{const pool=await getNewDesignPool(),timing=assertFound((await pool.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE book_id=$1 AND event_card_id=$2 AND status='active' AND lifecycle IN ('planned','occurred')`,[bookId,eventCardId])).rows[0],"事件没有可参与并发查询的有效故事时间。"),ids=new Set<string>();if(timing.normalized_start!==null){const rows=await pool.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT event_card_id FROM story_event_timings WHERE book_id=$1 AND status='active' AND lifecycle IN ('planned','occurred') AND event_card_id<>$2 AND normalized_start IS NOT NULL AND COALESCE(normalized_end,normalized_start)>=$3 AND normalized_start<=COALESCE($4,$3)`,[bookId,eventCardId,timing.normalized_start,timing.normalized_end]);for(const row of rows.rows)ids.add(String(row.event_card_id));}const explicit=await pool.query(`WITH ${storyRecordCtes.story_event_relations}
SELECT source_event_card_id,target_event_card_id FROM story_event_relations WHERE book_id=$1 AND status='active' AND relation_type IN ('simultaneous','overlaps') AND (source_event_card_id=$2 OR target_event_card_id=$2)`,[bookId,eventCardId]);for(const row of explicit.rows)ids.add(String(row.source_event_card_id)===eventCardId?String(row.target_event_card_id):String(row.source_event_card_id));if(!ids.size)return[];const rows=await pool.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE book_id=$1 AND event_card_id=ANY($2::uuid[]) AND status='active' AND lifecycle IN ('planned','occurred') ORDER BY normalized_start NULLS LAST,sequence`,[bookId,[...ids]]);return rows.rows.map(mapTiming);}
export async function listCausalGraph(bookId:string,eventCardId:string,direction:"upstream"|"downstream",maxDepth=10):Promise<StoryEventRelation[]>{if(maxDepth<1||maxDepth>50)throw new NewDesignError("因果查询深度必须在 1 到 50 之间。",422);const relations=await listStoryEventRelations(bookId,"causal"),byFrom=new Map<string,StoryEventRelation[]>(),byTo=new Map<string,StoryEventRelation[]>();for(const relation of relations){const from=relation.relationType==="depends_on"?relation.targetEventCardId:relation.sourceEventCardId,to=relation.relationType==="depends_on"?relation.sourceEventCardId:relation.targetEventCardId;byFrom.set(from,[...(byFrom.get(from)??[]),relation]);byTo.set(to,[...(byTo.get(to)??[]),relation]);}const queue:[string,number][]=[[eventCardId,0]],seenEvents=new Set([eventCardId]),seenRelations=new Map<string,StoryEventRelation>();while(queue.length){const[current,depth]=queue.shift()!;if(depth>=maxDepth)continue;for(const relation of (direction==="downstream"?byFrom:byTo).get(current)??[]){seenRelations.set(relation.id,relation);const from=relation.relationType==="depends_on"?relation.targetEventCardId:relation.sourceEventCardId,to=relation.relationType==="depends_on"?relation.sourceEventCardId:relation.targetEventCardId,next=direction==="downstream"?to:from;if(!seenEvents.has(next)){seenEvents.add(next);queue.push([next,depth+1]);}}}return[...seenRelations.values()];}

/** Author preview reuses the owning domain's exact validation without creating proposals. */
export async function validateAuthorTimelineCommandInTransaction(client:PoolClient,bookId:string,command:AuthorTimelineCommand):Promise<StoryTimeProposal|StoryRelationProposal|StoryNarrativeOccurrence|null>{
 await requireBook(client,bookId);let current:StoryTimeProposal|StoryRelationProposal|StoryNarrativeOccurrence|null=null;
 if("proposalId" in command){if(command.operation.startsWith("time_"))current=await getStoryTimeProposal(command.proposalId,client);else current=await getStoryRelationProposal(command.proposalId,client);if(current.bookId!==bookId)throw new NewDesignError("所选提案不属于本书。",422,{proposalId:"请从本书列表选择提案。"});if(current.revision!==command.expectedRevision)throw new NewDesignError("正式提案修订已变化，草稿保留，请重新读取后明确重新准备。",409,{expectedRevision:"来源修订已变化。"});if(command.operation.endsWith("review")&&current.status!=="proposed")throw new NewDesignError("只能明确审核尚未处理的提案。",409);if(command.operation.endsWith("edit")&&!["proposed","stale"].includes(current.status))throw new NewDesignError("只能修改待确认或待复核提案。",409);}
 if(command.operation==="time_create"||command.operation==="time_edit"){const eventId=command.operation==="time_create"?command.eventCardId:(current as StoryTimeProposal).eventCardId;await requireEvent(client,bookId,eventId);validateTimeValue(eventId,command.value);if(command.value.relativeToEventCardId)await requireEvent(client,bookId,command.value.relativeToEventCardId,"参照事件");await validateEvidence(client,bookId,command.value);if(command.value.replacesTimingId){const timing=await client.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT id FROM story_event_timings WHERE id=$1 AND book_id=$2 AND event_card_id=$3 AND status='active'`,[command.value.replacesTimingId,bookId,eventId]);if(!timing.rowCount)throw new NewDesignError("所选替换时间不是本事件当前正式时间。",422);}}
 if(command.operation==="relation_create"||command.operation==="relation_edit"){validateRelation(command.value);await requireEvent(client,bookId,command.value.sourceEventCardId,"起点事件");await requireEvent(client,bookId,command.value.targetEventCardId,"目标事件");await validateEvidence(client,bookId,command.value);}
 if((command.operation==="time_review"||command.operation==="relation_review")&&command.action==="confirm"){
  if(!current||!("currentVersion" in current))throw new NewDesignError("原提案版本未找到，请保留填写并只读核对来源。",409);
  const {evidenceKind,...references}=current.currentVersion;
  if(evidenceKind==="migration")throw new NewDesignError("迁入记录仅作为已存在的正式故事时间来源，不能作为新提案确认依据；请明确填写人工或已确认来源。",422,{"command.value.evidenceKind":"请选择可追溯的提案来源，不使用迁入记录。"});
  await validateEvidence(client,bookId,{...references,evidenceKind},true);
 }
 if(command.operation==="time_review"&&command.action==="confirm"){const proposal=current as StoryTimeProposal,value=proposal.currentVersion;if(value.replacesTimingId){const prior=assertFound((await client.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT lifecycle FROM story_event_timings WHERE id=$1 AND book_id=$2 AND event_card_id=$3 AND status='active'`,[value.replacesTimingId,bookId,proposal.eventCardId])).rows[0],"原正式时间已变化，请保留提案后重新准备替换来源。");if(value.lifecycle==="occurred"&&prior.lifecycle!=="planned")throw new NewDesignError("只有计划发生的事件可以转为实际发生。",422,{"command.value.lifecycle":"请核对原正式发生状态。"});}else if((await client.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT 1 FROM story_event_timings WHERE book_id=$1 AND event_card_id=$2 AND status='active'`,[bookId,proposal.eventCardId])).rowCount)throw new NewDesignError("事件已有正式时间，先在新提案中明确选择替换来源。",409,{"command.value.replacesTimingId":"请明确选择本事件原正式时间。"});}
 if(command.operation==="occurrence_create"||command.operation==="occurrence_edit"){const value=command.value;await requireEvent(client,bookId,value.eventCardId);await requireCardType(client,bookId,value.chapterCardId,"chapter","叙事章节");if(value.sceneCardId){await requireCardType(client,bookId,value.sceneCardId,"scene","叙事场景");if(!(await client.query(`WITH ${storyRecordCtes.planning_objects}
SELECT 1 FROM planning_objects scene JOIN planning_objects chapter ON chapter.id=scene.parent_object_id WHERE scene.book_id=$1 AND scene.card_id=$2 AND chapter.card_id=$3 AND scene.status='active' AND chapter.status='active'`,[bookId,value.sceneCardId,value.chapterCardId])).rowCount)throw new NewDesignError("所选场景不属于叙事章节的正式目录。",422,{sceneCardId:"请重新选择本章场景。"});}if(value.sourceKind==="body")await validateEvidence(client,bookId,{...value,evidenceKind:"body"});else if(value.chapterDocumentId||value.bodyVersionId||value.textAnchorId)throw new NewDesignError("人工位置不能混入正文证据。",422);if(command.operation==="occurrence_edit"){const row=assertFound((await client.query(`WITH ${storyRecordCtes.story_event_narrative_occurrences}
SELECT * FROM story_event_narrative_occurrences WHERE id=$1 AND book_id=$2`,[command.occurrenceId,bookId])).rows[0],"本书叙事位置未找到。");current=mapOccurrence(row);if(current.revision!==command.expectedRevision)throw new NewDesignError("原叙事位置已变化，草稿保留，请明确重新准备。",409);}}
 return current;
}
export async function listAuthorStoryOccurrencesInTransaction(client:PoolClient,bookId:string):Promise<StoryNarrativeOccurrence[]>{return (await client.query(`WITH ${storyRecordCtes.story_event_narrative_occurrences}
SELECT * FROM story_event_narrative_occurrences WHERE book_id=$1 ORDER BY chapter_card_id,narrative_order NULLS LAST,id`,[bookId])).rows.map(mapOccurrence);}
export async function readAuthorFormalStoryTimelineInTransaction(client:PoolClient,bookId:string){const timings=(await client.query(`WITH ${storyRecordCtes.story_event_timings}
SELECT * FROM story_event_timings WHERE book_id=$1 AND status='active' ORDER BY id`,[bookId])).rows.map(mapTiming),eventRelations=(await client.query(`WITH ${storyRecordCtes.story_event_relations}
SELECT * FROM story_event_relations WHERE book_id=$1 AND status='active' ORDER BY id`,[bookId])).rows.map(mapRelation);return {timings,eventRelations};}

export async function invalidateStoryTimelineForBodySwitch(client:PoolClient,chapterDocumentId:string,adoptedBodyVersionId:string):Promise<void>{
  const timings=await updateStoryRecords(client,"story_event_timing",`WITH ${storyRecordCtes.story_event_timings},
${storyRecordCtes.story_time_proposal_versions}
SELECT timing.*,('stale')::text AS status FROM story_event_timings timing, story_time_proposal_versions version WHERE timing.proposal_version_id=version.id AND version.chapter_document_id=$1 AND version.body_version_id<>$2 AND timing.status='active'`,[chapterDocumentId,adoptedBodyVersionId]);
  for(const timing of timings.rows){await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals}
SELECT story_time_proposals.*,('stale')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_time_proposals WHERE id=$1`,[timing.proposal_id]);await insertStoryReviewActions(client,"story_time",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('mark_stale')::text AS action,('system')::text AS actor,($4)::text AS note`,[randomUUID(),timing.proposal_id,timing.proposal_version_id,"章节采用了其他正文版本，原故事时间等待重新核对。"]);await deleteStoryRecords(client,"story_time_position",`WITH ${storyRecordCtes.story_time_positions}
SELECT story_time_positions.* FROM story_time_positions WHERE canonical_timing_id=$1`,[timing.id]);}
  const pendingTimes=await updateStoryRecords(client,"story_time_proposal",`WITH ${storyRecordCtes.story_time_proposals},
${storyRecordCtes.story_time_proposal_versions}
SELECT proposal.*,('stale')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_time_proposals proposal, story_time_proposal_versions version WHERE proposal.current_version_id=version.id AND version.chapter_document_id=$1 AND version.body_version_id<>$2 AND proposal.status='proposed'`,[chapterDocumentId,adoptedBodyVersionId]);for(const row of pendingTimes.rows)await insertStoryReviewActions(client,"story_time",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('mark_stale')::text AS action,('system')::text AS actor,($4)::text AS note`,[randomUUID(),row.id,row.current_version_id,"章节采用了其他正文版本，故事时间草案等待修改来源。"]);
  const relations=await updateStoryRecords(client,"story_event_relation",`WITH ${storyRecordCtes.story_event_relations},
${storyRecordCtes.story_relation_proposal_versions}
SELECT relation.*,('stale')::text AS status FROM story_event_relations relation, story_relation_proposal_versions version WHERE relation.proposal_version_id=version.id AND version.chapter_document_id=$1 AND version.body_version_id<>$2 AND relation.status='active'`,[chapterDocumentId,adoptedBodyVersionId]);for(const relation of relations.rows){await updateStoryRecords(client,"story_relation_proposal",`WITH ${storyRecordCtes.story_relation_proposals}
SELECT story_relation_proposals.*,('stale')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_relation_proposals WHERE id=$1`,[relation.proposal_id]);await insertStoryReviewActions(client,"story_relation",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('mark_stale')::text AS action,('system')::text AS actor,($4)::text AS note`,[randomUUID(),relation.proposal_id,relation.proposal_version_id,"章节采用了其他正文版本，原事件关系等待重新核对。"]);}
  const pendingRelations=await updateStoryRecords(client,"story_relation_proposal",`WITH ${storyRecordCtes.story_relation_proposals},
${storyRecordCtes.story_relation_proposal_versions}
SELECT proposal.*,('stale')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_relation_proposals proposal, story_relation_proposal_versions version WHERE proposal.current_version_id=version.id AND version.chapter_document_id=$1 AND version.body_version_id<>$2 AND proposal.status='proposed'`,[chapterDocumentId,adoptedBodyVersionId]);for(const row of pendingRelations.rows)await insertStoryReviewActions(client,"story_relation",`SELECT ($1)::uuid AS id,($2)::uuid AS proposal_id,($3)::uuid AS proposal_version_id,('mark_stale')::text AS action,('system')::text AS actor,($4)::text AS note`,[randomUUID(),row.id,row.current_version_id,"章节采用了其他正文版本，事件关系草案等待修改来源。"]);
  await updateStoryRecords(client,"story_event_narrative_occurrence",`WITH ${storyRecordCtes.story_event_narrative_occurrences}
SELECT story_event_narrative_occurrences.*,('stale')::text AS status,(revision+1)::integer AS revision,(now())::timestamptz AS updated_at FROM story_event_narrative_occurrences WHERE chapter_document_id=$1 AND body_version_id<>$2 AND status='active'`,[chapterDocumentId,adoptedBodyVersionId]);
}
