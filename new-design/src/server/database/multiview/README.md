# 多维读取模块边界

本模块只把章节、事实、状态变化、质量证据和任务账本组合成书籍多维读取模型，并计算 F8 可复用的连续稳定章节范围。

- PostgreSQL 业务表始终是正本；本模块不保存视图副本。
- AGE 与 pgvector 可以提供查询加速，但不能成为读取合同的必需依赖，也不能承接业务写入。
- 编辑继续进入资料表单、影响预览、章节采用结算或质量问题状态接口。
- AI 修订继续进入统一运行预览，只产生候选，不在本模块内改正文。
- 对外只从 `index.ts` 导出稳定入口，HTTP 层不得深层引用内部读取函数。
