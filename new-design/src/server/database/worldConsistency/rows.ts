export {qualityFixCandidateRows,qualityFixCandidateVersionRows,qualityFixAdoptionRows,qualityFixCandidateEventRows,qualityIssueRows,qualityIssueEventRows,qualityAuditReportRows,qualityIssueVersionRows,qualityIssueEvidenceRows,planningObjectRows,planningVersionRows,assetRows} from '../chapterProduction/persistence';
export const dictionaryDefinitionRows = `(SELECT data.id,data.dictionary_key,data.name,data.description,data.scope,data.owner_space_id,data.status,data.revision,data.created_at,data.updated_at,data.source_dictionary_id,data.read_only FROM new_design.cards card JOIN new_design.card_versions version ON version.id=card.current_version_id AND version.card_id=card.id CROSS JOIN LATERAL jsonb_to_record(version.values) AS data(id uuid,dictionary_key text,name text,description text,scope text,owner_space_id uuid,status text,revision integer,created_at timestamptz,updated_at timestamptz,source_dictionary_id uuid,read_only boolean) WHERE card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types type WHERE type.id=card.card_type_id AND type.type_key='dictionary_definition'))`;
export const dictionaryItemRows = `(SELECT data.id,data.dictionary_id,data.item_key,data.label,version.values->'value' AS value,data.sort_order,data.status,data.created_at,data.updated_at,data.parent_id,data.description,data.revision,data.current_version_id,data.source_item_id FROM new_design.cards card JOIN new_design.card_versions version ON version.id=card.current_version_id AND version.card_id=card.id CROSS JOIN LATERAL jsonb_to_record(version.values) AS data(id uuid,dictionary_id uuid,item_key text,label text,value jsonb,sort_order integer,status text,created_at timestamptz,updated_at timestamptz,parent_id uuid,description text,revision integer,current_version_id uuid,source_item_id uuid) WHERE card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types type WHERE type.id=card.card_type_id AND type.type_key='dictionary_item'))`;
export const dictionaryItemVersionRows = `(SELECT data.id,data.item_id,data.version,data.label,data.description,data.parent_id,data.sort_order,version.values->'value' AS value,data.status,data.path_node_ids,data.path_labels,data.created_by,data.created_at FROM new_design.cards card JOIN new_design.card_versions version ON version.id=card.current_version_id AND version.card_id=card.id CROSS JOIN LATERAL jsonb_to_record(version.values) AS data(id uuid,item_id uuid,version integer,label text,description text,parent_id uuid,sort_order integer,value jsonb,status text,path_node_ids uuid[],path_labels text[],created_by text,created_at timestamptz) WHERE card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types type WHERE type.id=card.card_type_id AND type.type_key='dictionary_item_version'))`;
export const worldConsistencyRequestRows = `(SELECT data.id,data.book_id,data.request_key,data.request_hash,data.input_hash,version.values->'input_payload' AS input_payload,version.values->'frozen_plan' AS frozen_plan,data.ai_task_id,data.step_id,data.attempt_id,data.report_id,data.status,data.model_request_state,data.generated_output,data.generated_execution,data.failure,data.created_at,data.updated_at FROM new_design.cards card JOIN new_design.card_versions version ON version.id=card.current_version_id AND version.card_id=card.id CROSS JOIN LATERAL jsonb_to_record(version.values) AS data(id uuid,book_id uuid,request_key uuid,request_hash char(64),input_hash char(64),input_payload jsonb,frozen_plan jsonb,ai_task_id uuid,step_id uuid,attempt_id uuid,report_id uuid,status text,model_request_state text,generated_output jsonb,generated_execution jsonb,failure jsonb,created_at timestamptz,updated_at timestamptz) WHERE card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types type WHERE type.id=card.card_type_id AND type.type_key='world_consistency_request'))`;
export const imagePromptPreparationRows = `(SELECT data.id,data.request_key,data.request_hash,data.scope_id,version.values->'input_payload' AS input_payload,version.values->'source_snapshot' AS source_snapshot,version.values->'frozen_plan' AS frozen_plan,data.step_id,data.attempt_id,data.status,data.reply,data.output,data.summary,data.created_at FROM new_design.cards card JOIN new_design.card_versions version ON version.id=card.current_version_id AND version.card_id=card.id CROSS JOIN LATERAL jsonb_to_record(version.values) AS data(id uuid,request_key uuid,request_hash char(64),scope_id uuid,input_payload jsonb,source_snapshot jsonb,frozen_plan jsonb,step_id uuid,attempt_id uuid,status text,reply jsonb,output jsonb,summary text,created_at timestamptz) WHERE card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types type WHERE type.id=card.card_type_id AND type.type_key='image_prompt_preparation'))`;
export const cardVersionLocalValueRows = `(SELECT data.card_version_id,data.field_definition_id,data.field_definition_version_id,version.values->'value' AS value,data.created_at FROM new_design.cards card JOIN new_design.card_versions version ON version.id=card.current_version_id AND version.card_id=card.id CROSS JOIN LATERAL jsonb_to_record(version.values) AS data(card_version_id uuid,field_definition_id uuid,field_definition_version_id uuid,value jsonb,created_at timestamptz) WHERE card.status='active' AND EXISTS(SELECT 1 FROM new_design.card_types type WHERE type.id=card.card_type_id AND type.type_key='card_version_local_value'))`;
