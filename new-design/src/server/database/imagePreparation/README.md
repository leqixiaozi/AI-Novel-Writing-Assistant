# 图片画面优化的原请求边界

门面只提供完整资料读取、原键单次领取、已有回复续存、明确结束过期未知领取和发送选择核对。`sources.ts` 读取确切公共角色版本，或原书籍、完整活动卡片／局部字段、实际采用故事计划；超限拒绝，不截断。`freeze.ts` 冻结独立 `new_design.image.prompt_preparation@v1`、原 form_assist 路线及全部来源版本。模型执行归 application，图片执行仍归原图片链。

手动098依赖097，默认关闭、不注册普通启动；保护原 Task／Step／Attempt、合同、配方、模型快照和完整清单，技术回执不复制角色、书籍、图片或正式事实。原回复先写安全不可替换的受控凭证，再完成原账本及实际用量。恢复不重调模型，未知用量保持未知；首次新请求只有原键不存在、未开始提交且回滚确认才返回未写入证明。

原文／中文／英文选择附原优化回执和来源哈希，发送时重核原范围、原选择及当前完整资料；不能把结果应用到另一书或新版角色。原文超图片合同上限须明确编辑，不能静默截短。

双端编译通过，schema／选择单元检查通过。实际098安装及优化／恢复 PG 被公共试用夹具的前置路由保护挡住，仍待验；未安装作者库或调用真实模型。统一证据及失败见 docs/legacy-page-replication-progress.md。
