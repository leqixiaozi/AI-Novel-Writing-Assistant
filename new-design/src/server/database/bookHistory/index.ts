import {createHash} from 'node:crypto';
import {createRecordCard} from '../recordCards';
import {recordWorkflowAction} from '../cardWorkflow';
import type {PoolClient} from 'pg';
import {bookHistoryPayloadSchema,bookHistorySummarySchema,bookHistorySnapshotSchema,bookHistorySaveSchema,bookHistoryRestoreSchema,bookHistoryReceiptSchema,bookHistoryWorkspaceSchema,type BookHistoryReceipt} from '../../../common/bookHistory';
import {getNewDesignPool} from '../runtime';
import {stableHash} from '../aiContracts';
import {NewDesignError,assertFound} from '../../domain/errors';
import {addPlanningVersionInTransaction,adoptPlanningVersionInTransaction} from '../planning/store';
import {saveChapterCandidateInTransaction} from '../chapterWriting/store';
export class BookHistoryError extends NewDesignError{readonly recovery;constructor(bookId:string,message:string,status=503,outcome:'not_written'|'unknown'='unknown'){super(message,status);this.recovery={summary:message,mutationOutcome:outcome,sourceRoute:`/new-design/books/${bookId}/history`,actionLabel:'返回整书历史',savedResult:'原历史、当前稿件与恢复原键保留；只读核对不调用模型。'};}}
async function transaction<T>(bookId:string,key:string|undefined,work:(client:PoolClient,absent:()=>void)=>Promise<T>){const client=await(await getNewDesignPool()).connect();let committing=false,missing=false;try{await client.query(key?'BEGIN':'BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY');if(key)await client.query('SELECT pg_advisory_xact_lock(hashtextextended($1,0))',[`book-history:${bookId}:${key}`]);const result=await work(client,()=>{missing=true;});committing=true;await client.query('COMMIT');return result;}catch(error){const rolledBack=await client.query('ROLLBACK').then(()=>true,()=>false);throw new BookHistoryError(bookId,error instanceof NewDesignError?error.message:'原历史操作未确认，请保留原键只读核对。',error instanceof NewDesignError?error.status:503,missing&&!committing&&rolledBack?'not_written':'unknown');}finally{client.release();}}
export async function bookHistoryCapability(client:PoolClient){
 const row=(await client.query(`SELECT capability.installed,capability.operational,
 (SELECT count(DISTINCT type_key) FROM new_design.card_types WHERE type_key=ANY($1::text[]) AND status='published' AND is_internal) type_count,
 EXISTS(SELECT 1 FROM pg_trigger WHERE tgrelid='new_design.card_version_actions'::regclass AND tgname='card_version_actions_immutable' AND tgenabled IN ('O','A')) immutable
 FROM new_design.system_capabilities capability WHERE capability.capability_key='book_content_history_v1'`,[["book_content_history_snapshot","book_content_history_restore"]])).rows[0];
 const installed=row?.installed===true&&Number(row.type_count)===2;return{installed,operational:installed&&row?.operational===true&&row.immutable===true};
}
async function currentState(client:PoolClient,bookId:string){
 const row=assertFound((await client.query(`SELECT jsonb_build_object('bookId',book.id,'plans',COALESCE((SELECT jsonb_agg(jsonb_build_object('id',object.id,'versionId',version.id,'parentObjectId',object.parent_object_id,'cardId',object.card_id,'level',object.level,'title',object.title,'sortOrder',object.sort_order,'content',version.content,'executionMode',version.execution_mode,'references',COALESCE((SELECT jsonb_agg(jsonb_build_object('role',ref.reference_role,'cardId',ref.card_id,'cardVersionId',ref.card_version_id,'action',ref.action_key,'note',ref.note,'sortOrder',ref.sort_order) ORDER BY ref.sort_order,ref.id) FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='planning_version_reference' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,planning_version_id uuid,planning_object_id uuid,book_id uuid,reference_role text,card_id uuid,card_version_id uuid,action_key text,note text,sort_order integer,created_at timestamptz) WHERE record.status='active') ref WHERE ref.planning_version_id=version.id),'[]'::jsonb)) ORDER BY object.level,object.sort_order,object.id) FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='planning_object' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status='active') object JOIN (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='planning_version' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,object_id uuid,book_id uuid,version integer,base_version_id uuid,based_on_parent_version_id uuid,source text,status text,content jsonb,content_hash char(64),source_body_version_id uuid,created_by text,stale_at timestamptz,stale_reason text,created_at timestamptz,execution_mode text) WHERE record.status='active') version ON version.id=object.adopted_version_id AND version.object_id=object.id WHERE object.book_id=book.id AND object.status='active'),'[]'::jsonb),'chapters',COALESCE((SELECT jsonb_agg(jsonb_build_object('id',document.id,'cardId',document.chapter_card_id,'bodyVersionId',version.id,'logicalOrder',document.logical_order,'title',COALESCE((SELECT title FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='planning_object' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status='active') object WHERE object.book_id=book.id AND object.card_id=document.chapter_card_id AND object.level='chapter' AND object.status='active'),document.title),'content',version.content,'contentHash',version.content_hash) ORDER BY document.logical_order,document.id) FROM new_design.chapter_documents document JOIN new_design.chapter_body_versions version ON version.id=document.adopted_version_id AND version.chapter_document_id=document.id WHERE document.book_id=book.id AND document.status='active'),'[]'::jsonb)) payload,
 jsonb_build_object('bookRevision',book.revision,'plans',COALESCE((SELECT jsonb_agg(jsonb_build_object('id',id,'revision',revision,'status',status,'adoptedVersionId',adopted_version_id,'currentVersionId',current_version_id) ORDER BY id) FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='planning_object' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status='active') planning_object_record WHERE book_id=book.id),'[]'::jsonb),'documents',COALESCE((SELECT jsonb_agg(jsonb_build_object('id',id,'revision',revision,'status',status,'adoptedVersionId',adopted_version_id) ORDER BY id) FROM new_design.chapter_documents WHERE book_id=book.id),'[]'::jsonb)) heads FROM new_design.books book WHERE book.id=$1 AND book.status='active'`,[bookId])).rows[0],'本书不存在或已归档。');
 const payload=bookHistoryPayloadSchema.parse(row.payload);return{payload,fingerprint:stableHash({payload,heads:row.heads}),heads:row.heads as {plans:Array<{id:string;revision:number;status:string;adoptedVersionId:string|null;currentVersionId:string|null}>;documents:Array<{id:string;revision:number;status:string;adoptedVersionId:string|null}>}};
}
function summary(row:Record<string,unknown>){const payload=row.payload as {plans:unknown[];chapters:unknown[]};if(!payload||!Array.isArray(payload.plans)||!Array.isArray(payload.chapters))throw new NewDesignError('历史来源元数据不完整。',409);return bookHistorySummarySchema.parse({id:row.id,bookId:row.book_id,kind:row.kind,label:row.label,sourceId:row.source_id,requestKey:row.request_key,inputHash:row.request_hash,sourceHash:row.source_hash,createdAt:new Date(String(row.created_at)).toISOString(),planCount:payload.plans.length,chapterCount:payload.chapters.length});}
async function snapshot(client:PoolClient,row:Record<string,unknown>){
 const action=(await client.query("SELECT action.payload,action.receipt FROM new_design.card_version_actions action WHERE action.card_id=$1 AND action.action_key='book_history.capture' ORDER BY action.created_at,action.id LIMIT 1",[row.id])).rows[0];if(!action||action.payload?.sourceHash!==row.source_hash||action.receipt?.snapshotId!==row.id)throw new NewDesignError('历史冻结来源凭证不匹配。',409);
 const frozen=row.payload as {bookId:string;plans:unknown[];chapters:Array<Record<string,unknown>>};
 if(!frozen||frozen.bookId!==row.book_id||!Array.isArray(frozen.chapters))throw new NewDesignError('历史所属小说或来源元数据不完整。',409);
 const versions=(await client.query("SELECT body.id,body.chapter_document_id,body.content,body.content_hash FROM new_design.chapter_body_versions body JOIN new_design.chapter_documents document ON document.id=body.chapter_document_id WHERE document.book_id=$1 AND body.id=ANY($2::uuid[])",[row.book_id,frozen.chapters.map(item=>item.bodyVersionId)])).rows,byId=new Map(versions.map(item=>[String(item.id),item]));
 const chapters=frozen.chapters.map(item=>{const body=byId.get(String(item.bodyVersionId));if(!body||String(body.chapter_document_id)!==item.id||body.content_hash!==item.contentHash||createHash('sha256').update(String(body.content),'utf8').digest('hex')!==item.contentHash)throw new NewDesignError('历史正文的精确来源缺失或哈希不匹配，未替换为当前正文。',409);return{...item,content:String(body.content)};});
 const payload=bookHistoryPayloadSchema.parse({...frozen,chapters});if(stableHash(payload)!==row.source_hash)throw new NewDesignError('历史内容哈希不匹配，不能恢复。',409);
 return bookHistorySnapshotSchema.parse({...summary(row),payload});
}
async function readSnapshot(client:PoolClient,bookId:string,id:string){return snapshot(client,assertFound((await client.query('SELECT * FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_snapshot\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,request_key uuid,request_hash char(64),kind text,label text,source_id uuid,source_hash char(64),payload jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_snapshot_record WHERE book_id=$1 AND id=$2',[bookId,id])).rows[0],'原整书历史不存在。'));}
export async function captureBookHistoryInTransaction(client:PoolClient,input:{bookId:string;requestKey:string;kind:'manual'|'before_restore'|'auto_milestone'|'before_pipeline';label:string;sourceId?:string|null;requestInput?:unknown}){
 if(!(await bookHistoryCapability(client)).operational)return null;
 await client.query("SELECT pg_advisory_xact_lock(hashtextextended($1,0))",["book-history:"+input.bookId+":"+input.requestKey]);
 const prior=(await client.query('SELECT * FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_snapshot\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,request_key uuid,request_hash char(64),kind text,label text,source_id uuid,source_hash char(64),payload jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_snapshot_record WHERE book_id=$1 AND request_key=$2',[input.bookId,input.requestKey])).rows[0];if(prior){if(prior.kind!==input.kind||prior.label!==input.label||prior.source_id!==(input.sourceId??null)||prior.request_hash!==stableHash(input.requestInput??input))throw new NewDesignError('原历史保存键不能改动保存用途或来源。',409);return summary(prior);}
 const current=await currentState(client,input.bookId),payload=current.payload;if(Buffer.byteLength(JSON.stringify(payload),'utf8')>50*1024*1024)throw new NewDesignError('当前正文超出单次历史保存容量，未截断内容。',422);
 const result=(await (async()=>{const values={book_id:input.bookId,request_key:input.requestKey,request_hash:stableHash(input.requestInput??input),kind:input.kind,label:input.label,source_id:input.sourceId??null,source_hash:stableHash(payload),payload:{...payload,chapters:payload.chapters.map(({content,...chapter})=>{void content;return chapter;})}};const book=assertFound((await client.query("SELECT space_id FROM new_design.books WHERE id=$1",[input.bookId])).rows[0],"小说不存在。");const row=await createRecordCard(client,{spaceId:String(book.space_id),typeKey:"book_content_history_snapshot",title:input.label,values});await recordWorkflowAction(client,{cardId:row.recordCardId,actionKey:"book_history.capture",requestKey:"book-history:capture:"+input.bookId+":"+input.requestKey,inputHash:values.request_hash,payload:{kind:input.kind,sourceHash:values.source_hash},receipt:{snapshotId:row.id}});return{rows:[row]};})()).rows[0];return summary(result);
}
export const saveBookHistory=(bookId:string,raw:unknown)=>{const input=bookHistorySaveSchema.parse(raw);return transaction(bookId,input.requestKey,async(client,absent)=>{if(!(await bookHistoryCapability(client)).installed){absent();throw new NewDesignError('本书历史保存尚未启用。',503);}const prior=(await client.query('SELECT * FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_snapshot\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,request_key uuid,request_hash char(64),kind text,label text,source_id uuid,source_hash char(64),payload jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_snapshot_record WHERE book_id=$1 AND request_key=$2',[bookId,input.requestKey])).rows[0];if(prior){if(prior.kind!=='manual'||prior.label!==input.label||prior.request_hash!==stableHash(input))throw new NewDesignError('原历史保存键不能改变保存说明。',409);return summary(prior);}absent();await client.query('SELECT id FROM new_design.books WHERE id=$1 FOR UPDATE',[bookId]);await client.query('SELECT id FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'planning_object\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status=\'active\') planning_object_record WHERE book_id=$1 ORDER BY id FOR UPDATE',[bookId]);await client.query('SELECT id FROM new_design.chapter_documents WHERE book_id=$1 ORDER BY id FOR UPDATE',[bookId]);const current=await currentState(client,bookId);if(current.fingerprint!==input.expectedFingerprint)throw new NewDesignError('当前规划或正文已变化，请只读刷新后明确保存。',409);return assertFound(await captureBookHistoryInTransaction(client,{bookId,requestKey:input.requestKey,kind:'manual',label:input.label,requestInput:input}),'本书历史保存尚未启用。');});};
export const readBookHistorySave=(bookId:string,key:string)=>transaction(bookId,undefined,async client=>{if(!(await bookHistoryCapability(client)).installed)return null;const row=(await client.query('SELECT * FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_snapshot\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,request_key uuid,request_hash char(64),kind text,label text,source_id uuid,source_hash char(64),payload jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_snapshot_record WHERE book_id=$1 AND request_key=$2',[bookId,key])).rows[0];return row?summary(row):null;});
export const previewBookHistory=(bookId:string,id:string)=>transaction(bookId,undefined,async client=>{const saved=await readSnapshot(client,bookId,id),current=await currentState(client,bookId),items=[];
 for(const plan of saved.payload.plans){const head=current.heads.plans.find(item=>item.id===plan.id&&item.status==='active'),now=current.payload.plans.find(item=>item.id===plan.id),before=JSON.stringify(now?.content??{},null,2),after=JSON.stringify(plan.content,null,2);items.push({kind:'plan' as const,id:plan.id,title:plan.title,sourceVersionId:plan.versionId,targetRevision:head?.revision??null,status:!head?'missing' as const:now&&stableHash({content:now.content,references:now.references,executionMode:now.executionMode})===stableHash({content:plan.content,references:plan.references,executionMode:plan.executionMode})?'same' as const:'changed' as const,before,after});}
 for(const chapter of saved.payload.chapters){const head=current.heads.documents.find(item=>item.id===chapter.id&&item.status==='active'),now=current.payload.chapters.find(item=>item.id===chapter.id);items.push({kind:'body' as const,id:chapter.id,title:chapter.title,sourceVersionId:chapter.bodyVersionId,targetRevision:head?.revision??null,status:!head?'missing' as const:now?.contentHash===chapter.contentHash&&now.title===chapter.title&&now.logicalOrder===chapter.logicalOrder?'same' as const:'changed' as const,before:now?`第 ${now.logicalOrder} 章：${now.title}\n\n${now.content}`:'',after:`第 ${chapter.logicalOrder} 章：${chapter.title}\n\n${chapter.content}`});}
 return{snapshot:saved,currentFingerprint:current.fingerprint,items};});
async function presentReceipt(client:PoolClient,raw:unknown){const receipt=bookHistoryReceiptSchema.parse(raw),items=[];for(const item of receipt.items){if(item.kind==='plan'){const row=(await client.query('SELECT adopted_version_id FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'planning_object\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status=\'active\') planning_object_record WHERE book_id=$1 AND id=$2',[receipt.bookId,item.id])).rows[0];const adopted=row?.adopted_version_id===item.resultVersionId;items.push({...item,status:adopted?'adopted' as const:'prepared' as const,summary:adopted?'恢复规划已按原采用流程生效。':'恢复规划未作为当前采用版本，请返回原规划核对。'});}else{const row=(await client.query("SELECT document.adopted_version_id,document.title,document.logical_order,EXISTS(SELECT 1 FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='chapter_stable_checkpoint' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,session_id uuid,settlement_id uuid,previous_checkpoint_id uuid,chapter_order integer,summary jsonb,dependency_hash char(64),status text,created_at timestamptz) WHERE record.status='active') checkpoint JOIN (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='chapter_adoption_session' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,chapter_document_id uuid,body_version_id uuid,preparation_id uuid,prior_body_version_id uuid,adoption_id uuid,settlement_id uuid,policy_version_id uuid,planning_object_id uuid,planning_version_id uuid,context_manifest_id uuid,dependency_hash char(64),adoption_kind text,status text,revision integer,idempotency_key text,created_by text,error_summary text,created_at timestamptz,updated_at timestamptz) WHERE record.status='active') session ON session.id=checkpoint.session_id WHERE checkpoint.chapter_document_id=document.id AND checkpoint.body_version_id=$3 AND checkpoint.status='stable' AND checkpoint.chapter_order=document.logical_order AND session.status='stable') settled FROM new_design.chapter_documents document WHERE document.book_id=$1 AND document.id=$2",[receipt.bookId,item.id,item.resultVersionId])).rows[0];const adopted=row?.adopted_version_id===item.resultVersionId,directoryReady=row?.title===item.targetTitle&&Number(row?.logical_order)===item.targetOrder;items.push({...item,directoryReady,status:!directoryReady?'directory_pending' as const:adopted&&row.settled?'settled' as const:adopted?'adopted' as const:'prepared' as const,summary:!directoryReady?`恢复目录待完成：需为第 ${item.targetOrder} 章《${item.targetTitle}》，请继续原编排与采用结算。`:adopted&&row.settled?'恢复正文已明确采用并按原流程结算。':adopted?'恢复正文已采用；继续原结算，未复制历史状态。':'恢复正文候选已保存；继续原采用与结算。'});}}
 return bookHistoryReceiptSchema.parse({...receipt,items,completed:items.every(item=>item.kind==='plan'?item.status==='adopted':item.status==='settled')});
}
export const readBookHistoryRestore=(bookId:string,key:string)=>transaction(bookId,undefined,async client=>{if(!(await bookHistoryCapability(client)).installed)return null;const row=(await client.query('SELECT receipt FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_restore\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(book_id uuid,request_key uuid,input_hash char(64),input_payload jsonb,before_snapshot_id uuid,receipt jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_restore_record WHERE book_id=$1 AND request_key=$2',[bookId,key])).rows[0];return row?presentReceipt(client,row.receipt):null;});
export const prepareBookHistoryRestore=(bookId:string,raw:unknown)=>{const input=bookHistoryRestoreSchema.parse(raw),hash=stableHash(input);return transaction(bookId,input.requestKey,async(client,absent)=>{
 if(!(await bookHistoryCapability(client)).installed){absent();throw new NewDesignError('整书历史恢复尚未启用。',503);}const prior=(await client.query('SELECT input_hash,receipt FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_restore\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(book_id uuid,request_key uuid,input_hash char(64),input_payload jsonb,before_snapshot_id uuid,receipt jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_restore_record WHERE book_id=$1 AND request_key=$2',[bookId,input.requestKey])).rows[0];if(prior){if(prior.input_hash!==hash)throw new NewDesignError('原恢复键不能改变历史或恢复选择。',409);return presentReceipt(client,prior.receipt);}absent();if(!(await bookHistoryCapability(client)).operational)throw new NewDesignError('整书历史恢复尚未启用。',503);
 await client.query('SELECT id FROM new_design.books WHERE id=$1 FOR UPDATE',[bookId]);await client.query('SELECT id FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'planning_object\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status=\'active\') planning_object_record WHERE book_id=$1 ORDER BY id FOR UPDATE',[bookId]);await client.query('SELECT id FROM new_design.chapter_documents WHERE book_id=$1 ORDER BY id FOR UPDATE',[bookId]);
 const saved=await readSnapshot(client,bookId,input.snapshotId),current=await currentState(client,bookId);if(saved.sourceHash!==input.sourceHash||current.fingerprint!==input.currentFingerprint)throw new NewDesignError('历史确切来源或当前规划／正文已变化；未自动套用新的来源。',409);
 for(const selected of input.items){const source=selected.kind==='plan'?saved.payload.plans.find(item=>item.id===selected.id):saved.payload.chapters.find(item=>item.id===selected.id),head=(selected.kind==='plan'?current.heads.plans:current.heads.documents).find(item=>item.id===selected.id&&item.status==='active');if(!source||!head||head.revision!==selected.expectedRevision)throw new NewDesignError('原恢复项不存在、已归档或修订变化；未部分覆盖。',409);}
 const before=assertFound(await captureBookHistoryInTransaction(client,{bookId,requestKey:input.requestKey,kind:'before_restore',label:'恢复前的当前规划与正文',sourceId:saved.id,requestInput:input}),'恢复前历史未保存。'),items:BookHistoryReceipt['items']=[],levels={story:0,volume:1,chapter:2,scene:3};
 const plans=saved.payload.plans.filter(item=>input.items.some(selected=>selected.kind==='plan'&&selected.id===item.id)).sort((a,b)=>levels[a.level]-levels[b.level]||a.sortOrder-b.sortOrder||a.id.localeCompare(b.id));
 for(const plan of plans){const head=assertFound((await client.query('SELECT revision FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'planning_object\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status=\'active\') planning_object_record WHERE book_id=$1 AND id=$2',[bookId,plan.id])).rows[0],'原规划不存在。'),parent=plan.parentObjectId?(await client.query('SELECT adopted_version_id FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'planning_object\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status=\'active\') planning_object_record WHERE book_id=$1 AND id=$2',[bookId,plan.parentObjectId])).rows[0]?.adopted_version_id:null;
  const created=await addPlanningVersionInTransaction(client,plan.id,{content:{...plan.content,title:plan.title},source:'manual',executionMode:plan.executionMode,references:plan.references,baseVersionId:plan.versionId,basedOnParentVersionId:parent??null,expectedRevision:Number(head.revision),idempotencyKey:`${input.requestKey}:plan:${plan.id}`,createdBy:'user'});await adoptPlanningVersionInTransaction(client,plan.id,{versionId:created.versionId,expectedRevision:created.revision,idempotencyKey:`${input.requestKey}:adopt:${plan.id}`,source:'user',actor:'user'});items.push({kind:'plan',id:plan.id,sourceVersionId:plan.versionId,resultVersionId:created.versionId,sourceRoute:`/new-design/books/${bookId}/planning?stage=${plan.level==='story'?'story_macro':plan.level==='volume'?'outline':'structured'}#plan-${plan.id}`,status:'adopted',targetTitle:null,targetOrder:null,directoryReady:true,directoryRoute:null,summary:'恢复规划按原候选、采用及下游影响流程生效。'});
 }
 for(const chapter of saved.payload.chapters.filter(item=>input.items.some(selected=>selected.kind==='body'&&selected.id===item.id))){let latest=(await client.query('SELECT revision,title FROM new_design.chapter_documents WHERE id=$1 AND book_id=$2',[chapter.id,bookId])).rows[0];if(latest.title!==chapter.title){const object=assertFound((await client.query("SELECT object.*,version.content,version.execution_mode FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='planning_object' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status='active') object JOIN (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key='planning_version' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,object_id uuid,book_id uuid,version integer,base_version_id uuid,based_on_parent_version_id uuid,source text,status text,content jsonb,content_hash char(64),source_body_version_id uuid,created_by text,stale_at timestamptz,stale_reason text,created_at timestamptz,execution_mode text) WHERE record.status='active') version ON version.id=object.adopted_version_id WHERE object.book_id=$1 AND object.card_id=$2 AND object.level='chapter' AND object.status='active'",[bookId,chapter.cardId])).rows[0],'Original chapter plan unavailable.');const originalReferences=(await client.query('SELECT reference_role role,card_id "cardId",card_version_id "cardVersionId",action_key action,note,sort_order "sortOrder" FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'planning_version_reference\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,planning_version_id uuid,planning_object_id uuid,book_id uuid,reference_role text,card_id uuid,card_version_id uuid,action_key text,note text,sort_order integer,created_at timestamptz) WHERE record.status=\'active\') planning_version_reference_record WHERE planning_version_id=$1 ORDER BY sort_order,id',[object.adopted_version_id])).rows;const renamed=await addPlanningVersionInTransaction(client,object.id,{content:{...object.content,title:chapter.title},source:'manual',executionMode:object.execution_mode,references:originalReferences,basedOnParentVersionId:(await client.query('SELECT adopted_version_id FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'planning_object\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,level text,parent_object_id uuid,card_id uuid,title text,sort_order integer,status text,current_version_id uuid,adopted_version_id uuid,revision integer,created_at timestamptz,updated_at timestamptz) WHERE record.status=\'active\') planning_object_record WHERE id=$1',[object.parent_object_id])).rows[0]?.adopted_version_id,baseVersionId:object.adopted_version_id,expectedRevision:Number(object.revision),idempotencyKey:`${input.requestKey}:title:${chapter.id}`,createdBy:'user'});await adoptPlanningVersionInTransaction(client,object.id,{versionId:renamed.versionId,expectedRevision:renamed.revision,idempotencyKey:`${input.requestKey}:title-adopt:${chapter.id}`,source:'user',actor:'user'});latest=(await client.query('SELECT revision,title FROM new_design.chapter_documents WHERE id=$1 AND book_id=$2',[chapter.id,bookId])).rows[0];}const created=await saveChapterCandidateInTransaction(client,chapter.id,{content:chapter.content,operationKind:'copy',baseVersionId:chapter.bodyVersionId,expectedRevision:Number(latest.revision),idempotencyKey:`${input.requestKey}:body:${chapter.id}`,createdBy:'user'});items.push({kind:'body',id:chapter.id,sourceVersionId:chapter.bodyVersionId,resultVersionId:created.versionId,sourceRoute:`/new-design/books/${bookId}/writing?chapter=${chapter.cardId}&bodyVersion=${created.versionId}`,status:'prepared',targetTitle:chapter.title,targetOrder:chapter.logicalOrder,directoryReady:Number(latest.logical_order??(await client.query('SELECT logical_order FROM new_design.chapter_documents WHERE id=$1',[chapter.id])).rows[0].logical_order)===chapter.logicalOrder,directoryRoute:`/new-design/books/${bookId}/composition`,summary:'恢复正文候选已保存；继续原采用与结算。'});}
 const receipt=bookHistoryReceiptSchema.parse({requestKey:input.requestKey,bookId,inputHash:hash,input,beforeSnapshotId:before.id,items,completed:items.every(item=>item.kind==='plan')});await (async()=>{const book=assertFound((await client.query("SELECT space_id FROM new_design.books WHERE id=$1",[bookId])).rows[0],"小说不存在。");const row=await createRecordCard(client,{spaceId:String(book.space_id),typeKey:"book_content_history_restore",title:"整书历史恢复回执",values:{book_id:bookId,request_key:input.requestKey,input_hash:hash,input_payload:input,before_snapshot_id:before.id,receipt}});await recordWorkflowAction(client,{cardId:row.recordCardId,actionKey:"book_history.restore.prepared",requestKey:"book-history:restore:"+bookId+":"+input.requestKey,inputHash:hash,payload:{snapshotId:saved.id,beforeSnapshotId:before.id},receipt:{restoreId:row.id,requestKey:input.requestKey}});})();return presentReceipt(client,receipt);
 });};
export const getBookHistoryWorkspace=(bookId:string,before?:string)=>transaction(bookId,undefined,async client=>{const capability=await bookHistoryCapability(client),current=await currentState(client,bookId);if(!capability.installed)return bookHistoryWorkspaceSchema.parse({bookId,capability,currentFingerprint:current.fingerprint,snapshots:[],nextCursor:null,restores:[],restoresTruncated:false});const snapshots=(await client.query('SELECT * FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_snapshot\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,request_key uuid,request_hash char(64),kind text,label text,source_id uuid,source_hash char(64),payload jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_snapshot_record WHERE book_id=$1 AND ($2::uuid IS NULL OR (created_at,id)<(SELECT created_at,id FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_snapshot\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(id uuid,book_id uuid,request_key uuid,request_hash char(64),kind text,label text,source_id uuid,source_hash char(64),payload jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_snapshot_record WHERE book_id=$1 AND id=$2)) ORDER BY created_at DESC,id DESC LIMIT 51',[bookId,before??null])).rows,restores=(await client.query('SELECT receipt FROM (SELECT fields.* FROM new_design.cards record JOIN new_design.card_types record_type ON record_type.id=record.card_type_id AND record_type.type_key=\'book_content_history_restore\' JOIN new_design.card_versions record_version ON record_version.id=record.current_version_id AND record_version.card_id=record.id CROSS JOIN LATERAL jsonb_to_record(record_version.values) AS fields(book_id uuid,request_key uuid,input_hash char(64),input_payload jsonb,before_snapshot_id uuid,receipt jsonb,created_at timestamptz) WHERE record.status=\'active\') book_content_history_restore_record WHERE book_id=$1 ORDER BY created_at DESC,request_key DESC LIMIT 51',[bookId])).rows;return bookHistoryWorkspaceSchema.parse({bookId,capability,currentFingerprint:current.fingerprint,snapshots:snapshots.slice(0,50).map(summary),nextCursor:snapshots.length>50?snapshots[49].id:null,restores:await Promise.all(restores.slice(0,50).map(row=>presentReceipt(client,row.receipt))),restoresTruncated:restores.length>50});});
