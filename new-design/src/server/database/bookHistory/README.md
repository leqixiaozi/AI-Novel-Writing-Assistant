# 本书规划与正文历史

104仅为默认关闭的手动能力，不注册作者启动。历史保存正式采用规划、原引用版本及现有已采用正文、标题和顺序；当前指纹包括真实对象头，单独标题或目录变化也可识别。GET与比较只读，不创建任务或修改正文。正常稳定里程碑及明确开始整书运行的事务可在已启用能力下保存原历史；关闭时原流程保持。

准备恢复核对本书原快照哈希、完整当前指纹与所选对象修订，锁住原对象，先存当前before_restore。规划复用正常版本／采用及下游失效，正文复用原copy候选和作者回执；随后作者到原写作页处理影响、采用和结算。不复制正式事实／认知／领域状态或stable标志，不删除后来新增章，不重写运行记录。

原恢复回执不可变，当前完成状态只读重核。正文必须实际采用恢复候选并在原稳定检查点结算，标题与顺序也须匹配；顺序差异显示directory_pending并到原编排处理，不提前显示完成。写作链接携带确切bodyVersion，缺失拒绝回退，人工未保存草稿保留。

`title-history-final-01.log`历史完整PG通过，真实模型0，原3正文版本／3次正常结算及新增章保留；覆盖标题单独变化、顺序单独变化、原请求重复、目录待处理与真实采用／结算后完成。保存／比较有独立页面证据；准备恢复原生confirm遇验收浏览器输入超时，页面全链未通过。作者启用、用户验收与私有维护执行器升级／恢复未完成，本功能不是数据库整库回滚。
