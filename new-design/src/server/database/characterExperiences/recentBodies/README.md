# 最近采用正文同步存储边界

sources.ts 在可重复读只读事务冻结完整真实来源，核验全文哈希与实际正文的冻结计划。index.ts 管理独立049原批次、完整输入／快照／注册提示词／输出及未知执行占用；原键只读返回原结果，不重复调用模型。超过完整读取上限明确拒绝，不截断来源。

adoption.ts 拥有候选到正常时间编辑器的出处桥接。载入草稿及影响预览只读；保存前重新核对人物、采用正文和原时间来源。只有明确正常保存才在同一物理事务建立精确正文锚点、AI＋body时间提案、049采用决定及068完整时间命令回执。任一写入失败整体回滚；提交确认丢失读取完整原输入。

提案仍须使用 storyTimeline 正常独立审核，才能建立正式故事时间及该域所拥有的正文叙述引用。不自动修改正文、采用版本、人物持有、认知、初始状态、检查点或章节结算。来源过期拒绝新采用，完整原结果仍可读取。运行记录只导航来源页。

沿用049／068和正常时间证据表，无新增迁移。单批至多20人、最近五章明确选择；整个300人范围的分批续行属于后续合同，不能借用小传分批宣称完成。真实模型、作者库安装和在线服务加载分别验收。
