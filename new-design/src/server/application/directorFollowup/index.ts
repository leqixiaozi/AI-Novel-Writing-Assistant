/** Cross-book follow-up is a read model, never a workflow runner. */
export {getDirectorFollowupWorkspace,getDirectorFollowupDetail,DirectorFollowupReadError} from '../../database/directorFollowup';
