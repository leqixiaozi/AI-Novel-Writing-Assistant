# 提示词组合试运行编排

此 facade 是冻结组合试运行的唯一应用入口。HTTP 与专业资源页复用原预览、原提交与原任务回执，不另建执行器、状态或模型调用链。预览编译仍由 AI composition 模块负责，数据库 claim / finish 保持原合同。

> 白话比喻：这是一张原订单的取餐窗口，换窗口不能重新下单。对应到系统：不同来源页用同一预览和原请求键领取、读取结果。

> 速记方法：原键领取，冻结执行，原账回执；保存回执未知只读核对，不再调用模型。

当前为源码抽取与静态审阅，统一动态验证尚未执行。
