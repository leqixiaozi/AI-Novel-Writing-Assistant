const {test}=require('node:test'),assert=require('node:assert/strict');const {isolatedDatabase}=require('./support/isolatedDatabase.cjs');
test('comic rendering migration installs immutable facts, versions and exports',async t=>{const {pool}=await isolatedDatabase(t,[{id:'109_comic_projects',fileName:'109_comic_projects.sql'},{id:'110_comic_episodes',fileName:'110_comic_episodes.sql'},{id:'111_comic_panels',fileName:'111_comic_panels.sql'},{id:'112_comic_bibles',fileName:'112_comic_bibles.sql'},{id:'114_comic_visual_assets',fileName:'114_comic_visual_assets.sql'},{id:'117_comic_rendering_exports',fileName:'117_comic_rendering_exports.sql'}]);for(const table of ['comic_render_batches','comic_fact_snapshots','comic_render_versions','comic_render_adoptions','comic_export_manifests','comic_export_artifacts'])assert.equal((await pool.query('SELECT to_regclass($1)::text value',[`new_design.${table}`])).rows[0].value,table);assert.equal(Number((await pool.query("SELECT count(*) value FROM pg_trigger WHERE tgname='comic_export_manifests_immutable' AND tgenabled='O'")).rows[0].value),1);});
