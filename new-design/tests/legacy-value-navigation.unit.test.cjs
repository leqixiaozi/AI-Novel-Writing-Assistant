const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');require('../node_modules/tsx/dist/cjs/index.cjs');const nav=require('../src/client/navigation.ts');test('every primary feature route belongs to exactly one navigation group',()=>{for(const route of nav.NEW_DESIGN_PRIMARY_NAV)assert.equal(nav.NEW_DESIGN_NAV_GROUPS.filter(group=>group.items.some(item=>item.href===route.href)).length,1,route.href);assert.equal(nav.newDesignCurrentMenuHref('/new-design/drama/projects/example'),'/new-design/drama');});test('book workflow links switch routes locally and retain navigation progress',()=>{const source=fs.readFileSync(path.join(__dirname,'../src/client/bookNavigation/index.tsx'),'utf8');assert.match(source,/history\.pushState/);assert.match(source,/PopStateEvent/);assert.match(source,/beforeunload/);assert.match(source,/progressCache/);});
