const {test}=require('node:test'),assert=require('node:assert/strict');require('../node_modules/tsx/dist/cjs/index.cjs');const {resolveDramaTab,DRAMA_TABS}=require('../src/client/dramaProjects/navigation.ts');test('drama deep links reject unknown tabs without silently selecting another tab',()=>{assert.equal(resolveDramaTab('unknown').valid,false);assert.equal(resolveDramaTab('unknown').tab,null);assert.equal(resolveDramaTab(null).tab,'source');assert.deepEqual(DRAMA_TABS.map(item=>item.key),['source','strategy','characters','episodes','production','quality','export']);});
