# 旧版书架功能对应审计（2026-09-17）

对照基线：`codex/new-design-card-kernel`，`f9102574`。本次逐项检查实际挂载页面、条件渲染分支、处理函数及新版能力合同。结论：当前新版只完成了部分书架功能及紧凑布局，不能称为旧版功能完整复刻。页面跳转不等同于弹层管理、直接下载或执行命令。

证据来自源码；未执行浏览器交互、真实模型调用、归档用户作品或导出写入测试。此前类型检查、模拟专项测试及 HTTP 200 不能证明下表的端到端交互完成。

## 实际挂载及判断口径

- 旧 `/novels` → `NovelList`，由 `NovelListHeader`、`NovelListFilterBar`、`NovelShelfCard`、`NovelContinueCard`、`NovelProjectCard`、`NovelListPagination` 构成；页面实际挂载 `NovelCoverDialog` 和 `AICockpit` 弹层。
- 旧 `/novels/create`、`/novels/auto-director`、`/create`、`/novels/:id/simple`、`/novels/:id/preview`、`/novels/:id/story` 均在路由注册；短篇按钮受 `creationStudioEnabled` 控制，代码默认开启，当前运行环境覆盖值未独立核对。
- 书架筛选分支提前 return，实际只有搜索、长短篇形式和排序；草稿/已发布及原创/续写按钮实际出现在工作台。其余未渲染的 `isShelf` 分支不算可见功能。
- 实际列表组件没有“更多”下拉菜单，操作是直接按钮和两个弹层；不能把其他页面或未挂载文件的菜单算入本页。
- “完整对应”指该项交互与结果在新版已有对应实现，仍需 UI 验收；“部分对应”表示保留部分能力但入口、步骤、信息或语义有差异；“缺失”表示没有对应实现；“底层不适用”表示旧字段或协议不能直接照搬，仍须与用户确认等价业务方案，不能据此删除需求。

## 逐项差异

| 编号 | 旧版实际功能及处理结果 | 新版现状 | 判定及差异 |
| --- | --- | --- | --- |
| T1 | 顶部“AI 自动导演开书”打开专属准备流程 | 只有“开始新故事”；进入统一开书后可选一键/分步 AI | 部分对应：顶部快捷入口缺失，准备及审阅流程不同 |
| T2 | 顶部“照着一本书写”以 `start=reference` 进入参考开书 | 统一开书中已有“拆书参考”、来源材料、研究与参考包选择 | 部分对应：没有直接进入参考方式的快捷入口，不等于旧参考流程已复刻 |
| T3 | 顶部“手动创建小说”打开手动建书表单 | 统一开书有空白起点与手工填写，但先建草稿、审阅、明确开书 | 部分对应：快捷入口、模板前置和确认步骤不同 |
| T4 | 条件显示“创作短篇”，进入 `/create?form=short_story`；短篇卡片进入独立作品流程 | 新版没有长短篇正式形式及连续短篇工作流 | 缺失／底层不适用：需确认短篇范围与合同，不能仅删按钮或把普通章节当短篇 |
| T5 | 书架/工作台切换，URL 与 `novel-list-view` 本地偏好同步 | 新版切换并记录 URL，还提供回收站 | 部分对应：视图切换已有，返回不带参数时不记住用户偏好 |
| T6 | 书架搜索标题或简介，250ms 延迟查询 | 新版从完整只读快照即时筛选书名/说明，参数记录到 URL | 完整对应搜索结果能力：实现方式不同，不需要重复数据库搜索 |
| T7 | 书架按全部形式/长篇/短篇筛选 | 新版没有形式筛选 | 缺失／底层不适用：没有正式形式字段，不能猜测分类 |
| T8 | 工作台按全部/草稿/已发布筛选 | 新版以正在推进、需要核对、已有正文、准备故事筛选 | 缺失／底层不适用：active/archived、完本及任务完成均不等于已发布；需明确发布语义 |
| T9 | 工作台按全部/原创/续写筛选 | 新版开书支持 continuation，但书架读模型无创作类型字段 | 缺失／底层不适用：开书起点不自动等于正式作品类型；需确认分类依据及历史数据处理 |
| T10 | 书架最近编辑/最近创建/完成度排序；工作台固定最近更新 | 新版所有视图可按更新/创建/采用正文进度排序 | 部分对应：更新时间/创建时间已有；旧完成度来自任务进度或项目状态，新版采用比例不是同一概念 |
| T11 | 24 本分页，边界钳制，加载中禁用翻页，单页隐藏分页 | 新版 24 本、边界钳制及搜索后回首页 | 部分对应：页内数据选择完整；加载禁用、单页隐藏和分页对齐不同 |
| T12 | 排队/运行/待审批作品每 4 秒自动刷新 | 新版只首次读取及手动刷新 | 缺失：运行卡片可能持续显示旧事实，需补只读刷新并保留原请求与错误 |
| T13 | “继续创作”最多三本 running/waiting_approval 作品，并从普通列表排除 | 新版最多三本 hasLiveWork 作品，包含排队，但不包含纯等待审批 | 部分对应：布局及排除重复已有，挑选条件不同，等待处理作品可能未突出 |
| T14 | 书架封面点击阅读预览，正文无内容显示空态 | 新版封面进入本书正式采用正文阅读，无正文也可打开空态 | 部分对应：入口存在；旧预览读取非空当前保存稿，新版只读采用稿，不能默认更改范围 |
| T15 | 未生成封面使用默认封面画及标题叠层；生成状态角标和对应按钮文案 | 新版使用文字占位及文件待核对提示 | 部分对应：正式封面绑定读取已有；默认画、生成中/失败角标及动态操作文案缺失 |
| T16 | 书架显示长篇原创/长篇续写/短篇、写作平台、草稿/已发布、更新时间 | 新版显示执行状态、采用章数、字数、日期 | 缺失／底层不适用：不是等价元信息；平台、形式及发布字段未在书架投影中提供 |
| T17 | 书架进度来自工作流任务，项目与大纲完成时可为 100% | 新版采用正文数/采用章节计划数，未规划时没有比例 | 部分对应：保留真实采用比例，但不能把该比例叫旧工作流进度；需并列显示或确认取舍 |
| T18 | 书名与主要操作按任务失败/取消/待审批及简易/专业/短篇分流 | 新版书名固定进入 overview，主要操作按导演、变化、正文、设定、规划分流 | 部分对应：已有精确导演/章节来源；书名行为与创作体验分流不同，泛 overview 不是精确任务处理 |
| T19 | 工作台整张卡片可点击，Enter/Space 打开对应工作区，子按钮阻止冒泡 | 新版只通过标题及具体链接打开 | 部分对应：单独链接可访问；整卡点击、键盘打开及体验分流缺失 |
| T20 | 工作台展示任务徽标、当前阶段/动作、工作流百分比、最近健康阶段、阻塞原因 | 新版显示简要行动原因、导演范围状态及候选数量 | 部分对应：没有完整阶段、动作及恢复说明，不能将范围结果替代整个开书/导演状态 |
| T21 | 工作台展示章节数、角色数、绑定世界名称、资源就绪分 | 新版主要显示采用/稳定章节和候选，完整快照已有角色/世界计数 | 部分对应：已有事实漏展示；世界数量不等于绑定世界名，所填字段比也不等于旧就绪评分 |
| T22 | 工作台悬停/聚焦显示项目、主线、大纲状态与 Token 累计 | 新版未展示 | 缺失／底层不适用：需按真实正式生命周期及用量账本投影，不能根据任务状态编造 |
| T23 | 工作台主按钮对可继续任务直接 `continueNovelWorkflow`；章节批次可传 `auto_execute_range`，有 pending/成功反馈 | 新版主按钮导航到原导演或来源页，用户再明确执行 | 部分对应：导航不是同一次继续命令；需确认是否保留来源页明确执行原则，不能暗中启动 AI |
| T24 | 工作台“AI 驾驶舱”打开本书弹层并读取实时 projection；可触发下一步或执行详情 | 新版没有本书驾驶舱弹层 | 缺失：导演来源页存在，但不等同于弹层与投影功能 |
| T25 | 驾驶舱展示暂停保护/建议、后台排队接手执行恢复、产物记录、按阶段/步骤用量、自动记录 | 新版书架没有这些信息；部分事实在导演跟进/运行记录来源页 | 部分对应：其他来源页不能当作书架已完整展示；用量与后台健康还需核对合同 |
| T26 | 工作台有任务时独立“查看执行详情”按钮，带精确 taskId 打开任务面板 | 新版没有独立详情入口；主按钮可能进入 run 或 overview | 部分对应：特定导演可打开精确范围，非导演精确任务入口未完整承接 |
| T27 | 工作台“阅读预览”独立按钮 | 新版工作台有“阅读正式正文”链接及封面入口 | 部分对应：入口对应，采用稿范围差异仍需确认 |
| T28 | 工作台顶部按实际任务显示推进中/待确认/可继续/暂停失败及待恢复按钮 | 新版顶部统计全部作品推进/核对/采用章数，没有恢复清单按钮 | 部分对应：新指标不是旧指标；等待与可继续需分别计算，恢复来源需完整接入 |
| T29 | 待恢复按钮调用上下文弹层，支持单任务及全部恢复命令 | 新版书架没有该入口；任务中心只读、来源页分别处理 | 缺失／协议不适用：不能直接搬旧批量恢复；需确认仅列出来源还是增加受控批量执行 |
| C1 | 封面按钮在原书架打开本书 NovelCoverDialog，关闭后刷新卡片 | 新版跳转视觉资产泛来源页 | 部分对应：没有管理弹层，也未默认选择该书当前封面精确版本 |
| C2 | 根据小说资料构建封面输入草稿，可人工修改 | 新版图片生成填写主体与画面要求；没有资料自动形成封面草稿 | 缺失：单图生成不等于完整封面准备链 |
| C3 | 中/英 AI 优化 Prompt、恢复原链路、手动最终 Prompt、发送模式预览 | 新版没有封面 Prompt 优化与原链路/最终发送切换 | 缺失：需要登记正式提示与任务合同，不能拼接临时模型请求 |
| C4 | 独立风格、负向提示、图像厂商、1024×1536/1024²/1536×1024、1—4 张 | 新版独立图像连接版本、画面要求、固定单张；声明尺寸为 1024²/1024×1792/1792×1024 | 部分对应：图像连接能力已有；分离选项、尺寸与多张合同不同，不能仅加不支持选项 |
| C5 | 异步任务排队/生成/成功/失败/取消状态，每 1.5 秒轮询，完成刷新图库 | 新版有原请求及冻结输入核对/完成原回复/结束过期领取，手动刷新与核对 | 部分对应：新请求恢复实现存在；旧异步任务状态和自动轮询没有等价展示；未实测生成 |
| C6 | 封面图库列当前主图/候选、尺寸、本地路径；设为当前封面及删除，删除主图可自动补主图 | 新版有精确图片版本、上传、候选采用、绑定和影响预览归档 | 部分对应：图片管理能力已有，但选择/绑定/归档操作更多；自动补主图与删除语义不同 |
| C7 | 当前无主封面时第一张成功图自动成为主封面 | 新版图片先存候选，明确采用及绑定，不自动设为封面 | 部分对应／语义需确认：自动设图与新版明确采用原则不同，不能擅自改变 |
| E1 | 书架及工作台导出直接 GET `/novels/:id/export?format=txt&scope=full` 下载 Blob，显示 pending/成功/错误 | 新版按钮跳完本与导出；默认 formal/DOCX，先冻结预览再排队 | 部分对应：有导出工作区，缺旧一键 TXT 下载，稿件范围及步骤不同；当前安全启动关闭后台恢复，未证实排队文件会完成 |
| E2 | 旧阅读预览可复制本章、下载本章 TXT、直接下载整本 TXT | 新阅读只提供跳导出工作区 | 缺失：复制和下载本章是独立遗漏；跳工作区不等于整本直接下载 |
| R1 | 旧预览章节目录、正文、返回列表/工作区、按 chapterId 保持选择 | 新阅读有正式采用目录、正文、章节深链、前后章与工作区 | 部分对应：核心阅读有实现，稿件范围与参数合同不同；不应改选其他章节冒充原来源 |
| R2 | 旧预览目录可关闭，移动端抽屉与遮罩，选择章后关闭；显示章节状态及总字数 | 新阅读目录常驻，移动端在正文上方滚动，稳定状态另标 | 部分对应：抽屉/开关、完整章节状态、目录统计布局缺失 |
| R3 | 旧 simple 阅读当前保存稿与审校中的稿，显示任务安全暂停、可恢复/重规划后继续，导出完成章，切专业体验持久化 | 新阅读只读采用稿，无同页任务恢复、审校稿、简易/专业体验切换 | 部分对应／底层不适用：来源页可处理一些任务；原 simple 创作流程没有完整复刻，阅读页不能替代它 |
| A1 | 旧删除作品经确认后 DELETE，直接删除小说 | 新版确认后归档至回收站，保留原资料；精确凭证核对、活动任务阻止归档、原身份恢复 | 按用户已确认替换：功能完整对应用户选择的移除/恢复；不是旧永久删除，不需再以旧语义实现 |
| A2 | 旧作品“草稿”是未发布正式小说状态，不是开书会话；本页无独立开书会话列表 | 新版新增未建立书籍的开书草稿展开列表，原 session 深链继续 | 新增能力：不能拿它作为旧草稿状态筛选已实现的证据；本页无已挂载旧草稿删除可供复刻 |
| S1 | 旧列表骨架、错误重试、空态及多方式创建入口；任务命令/下载操作独立 pending/toast | 新版读取/错误/空态、原未知请求锁定和归档状态提示已有 | 部分对应：基础状态已有，多创建空态快捷入口、每项任务/下载反馈、骨架及细节不同 |
| V1 | 旧书架卡片 230px 列宽、16px 间距、2:3 封面；继续卡片 78×116px；工作台两列项目卡不含封面 | 新版主要尺寸已跟随；工作台仍是带封面的小型资料卡，默认封面画、卡片内容和操作区域不同 | 部分对应布局：只能称紧凑结构对齐，不能称像素及功能完整复刻 |

## 建议补齐范围及需确认事项

1. **先补现有能力的漏接**：视图偏好记忆、受控的只读运行态刷新、等待审批作品突出、加载分页行为；工作台补已有角色/世界/规划/质量事实以及精确原任务/导演入口。这些不需要数据库迁移或调用模型，但不能将新指标伪装成旧评分或旧进度。
2. **按旧操作体验接入现有来源能力**：从书架管理精确本书封面及图库，明确展示对应候选/当前正式封面；把导出与任务信息做成对应可操作入口和反馈。弹层不是泛链接，同一操作结果才算完整对应。本次未直接改这些流程，以免在差异尚未确认时再次自行替换。
3. **创建和作品分类需确认**：恢复 AI、参考、手动快捷入口的具体预选流程；短篇及其作品流程范围；已发布、长短篇、原创/续写和平台的正式来源及历史作品归类。统一开书中的参考、续写已有输入能力，并非完全从零开发，但不能省略书架入口和正式分类。
4. **正文与导出范围需确认**：是否完全读取旧当前保存稿/审校稿，或维持仅明确采用正文；是否提供书架一键全书 TXT，和审阅稿标识/正式稳定稿之间如何对应。不能悄悄将旧 full 改为 formal，也不能绕过冻结版本和稳定检查点。
5. **AI 执行入口需确认**：旧书架直接继续、批次自动执行与单/全部任务恢复，是否恢复同页执行，还是用户明确接受新版在原来源页审阅后执行。新增直接执行须复用正式任务合同、完整凭证与防重复机制；本次不启动 AI。
6. **封面合同需确认**：资料自动形成草稿、AI 中英优化、风格/负向分项、旧尺寸、多张生成、无主图自动采用/删除主图自动补图。这些不能用现有单张请求或改变绑定语义假装完成。
7. **阅读与创作体验需确认**：复制/本章下载、目录抽屉是具体遗漏；完整 simple 模式包括稿件范围、恢复、重规划、导出及体验切换，不能把只读阅读页当作已复刻，须另定实现范围。本次不修改其他页面。

用户已确认的回收站替换继续有效。其余旧版功能未因“底层不同”获得删减授权，必须依 AGENTS.md 与用户确认差异或执行用户给出的修改方式。

## 主要源码依据

- 旧路由：`client/src/router/index.tsx`。
- 旧列表及操作编排：`client/src/pages/novels/NovelList.tsx`。
- 旧列表显示与分支：`client/src/pages/novels/components/list/NovelListHeader.tsx`、`NovelListFilterBar.tsx`、`NovelShelfCard.tsx`、`NovelProjectCard.tsx`、`NovelListPagination.tsx`、`NovelListEmptyState.tsx`、`novelListViewModel.ts`。
- 旧封面：`client/src/pages/novels/components/cover/NovelCoverDialog.tsx`；旧操作 API：`client/src/api/images.ts`、`client/src/api/novel/core.ts`、`client/src/api/novelWorkflow.ts`。
- 旧驾驶舱及恢复：`client/src/components/autoDirector/AICockpit.tsx`、`client/src/components/layout/TaskRecoveryContext.tsx`、`client/src/lib/novelWorkflowTaskUi.ts`。
- 旧阅读及体验：`client/src/pages/novels/NovelPreview.tsx`、`client/src/pages/novels/simpleCreation/SimpleNovelShelfPage.tsx`。
- 新路由与书架：`new-design/src/client/NewDesignPage.tsx`、`bookshelf/index.tsx`、`bookshelf/ReadingPage.tsx`、`new-design/src/common/bookshelf/presentation.ts`、`new-design/src/common/home/index.ts`、`presentation.ts`。
- 新准备与图片：`new-design/src/client/CreateBookPage.tsx`、`bookCreation/DirectorControls.tsx`、`visualAssets/index.tsx`、`imageGeneration/index.tsx`、`new-design/src/common/imageGeneration.ts`、`visualAssets/index.ts`。
- 新导演与导出：`new-design/src/client/productionDirector/index.tsx`、`completionExport/CompletionExportPage.tsx`、`new-design/src/server/database/completionExport/README.md`。
