# 写作调整配置执行修复

目标：在现有候选生成链中提高当前编排、场景预算与事实边界的执行可靠性，不把样本小说专属约束固化到产品。

像按新版施工图装修：旧房是现状参考，不是新版尺寸的裁判。速记：明确授权定范围，当前编排定目标，历史限定知情，旧稿提供素材。

1. 复现基线：第1章候选5379字符，祭品清单、日期、记录去向错误，首尾预算失守。保留此版本不采纳。
2. 测试先行：上下文排除stale计划，当前章明确字数及场景结构；局部调整继续保持范围外逐字不变。审核仍拒绝伪造引文。
3. 通用提示词：明确重写授权优先级、按场景组织、内部检查人物动机和物件状态；细节不能新增推理依据；快慢不通过换事件实现。审核缺失项允许空引文，不拼接伪引文。
4. 实际生成：使用同一配置和简短通用请求，经原候选接口生成，读全文对照；不使用专属补丁或手改候选冒充成功。根据问题迭代。
5. 泛化验证：以另一组原创非灵异场景做小样测试，区分真实模型验证和自动契约测试。
6. 文档：记录实际轮次、证据、剩余风险。仅修改本任务文件，保留现有工作区其他改动。当前已位于codex功能分支，不发布、不自动采纳小说。

验收：关键配置事实无明确违反；首尾与中场分配有可读差异；约3000字目标不显著超长；没有大段重复说明。模型审核只作辅助，不能替代逐项阅读，也不声称对所有小说保证通过。
