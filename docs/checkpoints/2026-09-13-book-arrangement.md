# 全书编排实施检查点 · 2026-09-13

本轮在 `codex/optional-manual-adjustments` 上接入独立“全书编排”菜单，设计依据是用户「拾章」全书编排原型。开发边界见 [全书编排与统一章节轴](../wiki/product/book-arrangement.md)。

## 已交付范围

- 桌面创作导航、移动端更多菜单、原小说工作台快捷入口；页面 `/book-arrangement?novelId=<id>`。
- 选择作品、全书概览、十章窗口、章节区间与单章勾选；章节、卷、事件、场景、人物与参数共用章节轴。密集资料只显示摘要，完整资料在编辑区查看。
- 章节备注、五种表达参数、人物参与区段、编排锁定、草稿保存和本地未保存编辑恢复。未设置与显式 0 分开显示。
- 有效要求前后预览、选择应用、历史预览恢复；已有章级／本次设置优先，应用前冻结的任务不受后来配置变更影响。
- 保存的配置可直接拼装为 AI 大纲调整输入，补充目标可选；复用原规划候选与选择采纳能力。

> 🏠 白话比喻：草稿是旅行便笺，应用要求是下次出行偏好，采纳大纲才是修改行程。对应实现：三种按钮调用不同接口，保存编排不会一起改掉正文和正式规划。
> 🧠 速记：先存、再看、选范围生效。

## 接口与数据

`/api/novels/:id/book-arrangement` 提供 GET；其下 `/draft` 使用 PUT，`/preview` 与 `/:candidateId/apply` 使用 POST。写入要求 `Idempotency-Key`，旧接口的请求合同不变。

复用 `WritingSetting` 的草稿与章级编排记录、`ChapterEditVersion` 的候选及 `DirectorArtifact` 依赖；不新增表或迁移。提示片段由 `prompting/prompts/novel/bookArrangementControls.ts` 渲染，版本为 `v1`，没有新增模型调用入口。

## 验收证据

| 检查 | 结果 | 证据 |
|---|---|---|
| 新后端真实 SQLite 测试 | 11/11 通过 | `D:/cache/book-arrangement-server-tests.log` |
| 原人工调整回归 | 85/85 通过 | `D:/cache/book-arrangement-adjustment-regression.log` |
| 客户端全量测试 | 228 项，222 通过，6 项原有失败；本轮新增状态测试 6/6 通过 | `D:/cache/book-arrangement-client-tests.log` |
| 共享合同与服务端构建／类型检查 | 通过 | 使用项目现有 pnpm 构建和 TypeScript 检查 |
| 客户端生产构建 | 通过 | `D:/cache/book-arrangement-client-build.log` |
| 真实组件隔离浏览器 | 9/9 组通过，18 次替身 API 请求，0 页面错误 | `D:/cache/Node/ai-novel-book-arrangement/browser-DNhzhS/result.json`，同目录 7 张截图 |
| 真实应用只读检查 | 菜单和页面可打开；53 章、5 人物、10 事件、258 场景正常读取 | 本地 5173 页面及 3000 GET 接口 |

客户端 6 项失败名称与上一阶段日志 `D:/cache/writing-adjustments-client-final.log` 一致：导演进度面板、移动端页面落点、任务过滤网格、移动路由元数据、更多菜单全集、旧模型设置路由合同。没有改断言掩盖这些既有差异。

隔离浏览器脚本为 `client/scripts/qa-book-arrangement.mjs`，使用真实 React 页面和受控接口替身，验证共享列坐标、尾窗与短书、空值与 0、同名人物 ID、未保存编辑恢复、保存失败重试、锁定排除、候选范围和窄屏滚动。要求应用还验证了取消全部勾选时禁用、只应用第 4 章、成功回执、重复点击禁用、刷新后已应用参数仍可见。其他章设置、规划、事件和场景通过深比较保持原样。AI 大纲页面本轮验证到配置驱动候选预览，采纳事务复用已验收的原规划服务。

## 保留边界

### 视觉与连续导航补充验收

参考用户截图完善独立浅色外壳、八章连续拖动导航、彩色紧凑区段与右侧检查器。关系、线索与核对只读投影原数据，不新增表或修改保存／预览／应用协议。更新后的工作区排除旧场景版本：该真实作品仍有历史场景记录，但当前有效场景为 0；不将旧记录冒充当前编排依据。

| 检查 | 本次结果 |
|---|---|
| 后端编排 SQLite 测试 | 12/12 通过，`D:/cache/book-arrangement-style-server-tests.log` |
| 客户端全量测试 | 230 项，224 通过，仍为上述 6 项既有失败，`D:/cache/book-arrangement-style-client-tests.log` |
| 前端状态测试 | 8/8 通过，覆盖稳定颜色、区段压排行和历史／计划区分 |
| 构建与类型 | 客户端生产构建、共享包构建、服务端类型检查通过 |
| 内置浏览器 | 同尺寸截图、鼠标拖动、键盘边界、跨章点击、历史页签、隔离保存／预览／应用、390px 窄屏检查通过 |

详细视觉证据见项目根目录 [design-qa.md](../../design-qa.md)。旧 `qa-book-arrangement.mjs` 已更新为新组件选择器且语法检查通过，本次没有重跑其独立浏览器会话；不能把上一阶段的 9/9 组结果当成本次脚本执行结果。

本轮不运行真实模型文学效果评测。自动化写入只发生在临时数据库或接口替身，真实作品仅做 GET 与页面浏览；没有替用户保存编排、改纲或续跑任务。

要求应用后，在章节“人工调整”中解析与生成时沿用；未启用可选合同的旧写作流程保持原行为。人物与曲线显示编排目标，不能视为正文质量或实际戏份的测量。原型中的其他情绪／揭示曲线、拖拽重排及独立主题轨道不在本轮交付范围。

既有全量服务端结果和失败基线保留在上一阶段检查点；本轮影响集中在新增服务与可选要求继承，执行上述 96 项相关服务端检查，不重复无关全量模型／导演测试。

## 卷段编辑补充（2026-09-13）

本次范围为已确认的卷段编辑：点击选卷，编辑名称、摘要、卷目标、主角变化、高潮及下卷衔接；拖动两端调整起止、中部移动、章边界吸附、边缘逐章滚动、键盘修改与撤销。章节表头按用户补充改为“数字 · 名称”，状态独立保留。窄屏详情排在矩阵下方，并提供所选卷段定位入口。上文“拖拽不在交付范围”仅对应此前阶段。

> 🏠 白话比喻：先用便笺标出新的分卷位置，核对目录冲突后再正式修改目录；正文页不随便笺移动而重写。对应实现：拖动与详情输入只进入可恢复草稿，预览和应用分别调用独立接口。
> 🧠 速记：调卷不改文，先存再预览，确认才应用。

共享草稿增加可选 `volumeEdits`，新增 `/book-arrangement/volumes/preview` 和 `/book-arrangement/volumes/:candidateId/apply`。卷候选复用编辑版本，原卷版本与兼容大纲由原持久化能力提交。局部范围通过可选 `affectedVolumeIds` 传入，未选卷和对应 Arc 不重写，原调用缺省行为保持不变。应用消费已采纳卷草稿并保存回执，其他草稿保留。

正文、实际章序、人物资源不自动修改。移出且未分配到其他卷的章规划链接留在调整前版本；跨卷移动保留其原 ID、任务和伏笔引用。所改卷节拍与关联重平衡建议失效，未改卷保留；历史战略审核不代表已审核新卷段。预览明确列出未进行 AI 合理性检查，不宣称文学效果已经达标。

### 本次验证范围

- 前端状态测试 14/14 通过，包括 8 项原编排状态和 6 项卷段范围测试。
- 编排 SQLite 测试 20/20 通过，其中新增 8 项卷段安全测试，覆盖隐式夺章拒绝、跨卷原 ID 保留、未选卷／Arc 不变、部分草稿消费、事务回滚、断网重试、版本／接管／同步保护、锁章与不连续范围、原文档专属字段保留。
- 使用最新服务端构建运行原卷工作区、事务及更新同步测试 20/20 通过。共享包与服务端完整构建最终通过。汇总：`D:/cache/book-arrangement-volume-backend-verification.txt`；旧卷回归原始日志：`D:/cache/book-arrangement-volume-legacy-regression.log`。首次构建发现空值类型与不存在字段引用，修复后完成上述最终构建和测试；早期失败日志不代表最终结果。
- 共享包构建、客户端生产构建通过；最后的恢复按钮并发保护及窄屏定位改动后，客户端类型检查再次通过。构建日志：`D:/cache/book-arrangement-volume-client-build.log`；类型日志：`D:/cache/book-arrangement-volume-client-typecheck.log`。
- 运行中服务只读检查：前端 HTTP 200，工作区 GET 成功返回 53 章、3 卷及新卷预览字段。未对真实作品保存、预览或应用。
- 按本次 `AGENTS.md` 的 UI 验证规则，没有运行浏览器、截图或交互验收；界面验收由用户进行。此前章节中的截图证据不覆盖本次卷段交互。
- 本次未做 PostgreSQL 实例执行和真实模型效果测评，没有增加表或迁移。

## 底座统一与对象交互实施（2026-09-13，当前批次）

用户明确要求在正在运行的底座改造全书编排，并授权修改开发环境《净宅人翻旧账》用于验收。本批直接修改应用路由所用组件、原侧栏与模块接口；不创建独立交互原型。`AppLayout` 统一承载编排页，侧栏“全书编排”从小说工作台进入时携带当前作品 ID。

### 开发作品数据准备

- 作品：`cmtvm8yxq005578y2lcp4fhs6`，53 章、5 人物；补充范围为第 1—8 章及第 46—53 章。
- 实际 SQLite 文件：`D:/project/AI-Novel-Writing-Assistant/server/dev.db`。
- 一致性备份：`D:/infra/backups/ai-novel/arrangement-demo-v1-1789299543373/before.sqlite`，24,055,808 字节，`integrity_check` 为 `ok`；同目录 `before.json` 和 `receipt.json` 保存原草稿及写入清单。
- 脚本：`server/scripts/seed-book-arrangement-demo.cjs`。默认只预览，必须指定作品、备份目录及 `--apply` 才写入；先备份，再事务补缺，重复执行不生成副本。
- 成功补充 68 个对象，重复执行新增 0 个；两次均确认全部 Chapter 行摘要不变。首轮发现 WritingSetting 需要显式 ID，事务已回滚；修正后完成上述写入和验证。
- 工作区 GET 返回 53 章、3 卷、25 事件、36 个有效场景、4 个关系阶段、19 条线索及 264 个原核对／冲突投影。数量描述资料可读取，不表示其已经通过文学核对。
- 新增资料以演示计划标记，包含人物参与、五维档位、0／继承／停用与锁章样例；不新增正式事实、正文或审核通过记录。已写章节保留原稿。

> 🏠 白话比喻：在指定练习本补题，先复印原本，再给新题贴标签。对应实现：指定作品、备份、计划来源、原行摘要和幂等写入清单。
> 🧠 速记：先备份，只补缺，原文不变，有据可查。

### 验证记录

- 数据准备状态测试 `server/tests/bookArrangementDemo.test.js`：2/2 通过，覆盖原输入不变、重跑幂等、0／继承／停用／锁定及稳定人物 ID。
- 为避免计划进入事实消费，4 条本批演示关系改用 `arrangement_plan`；备份与回执在 `D:/infra/backups/ai-novel/arrangement-demo-v1-1789301092783/`。
- 为保证演示场景可实际修改，11 个本批演示计划对齐原章节已有场景卡，更新自身演示场景 22 行、补充 39 行；没有修改原 Chapter 行。备份与回执在 `D:/infra/backups/ai-novel/arrangement-demo-v1-1789301325937/`。最终有效场景 75 个，15 个演示计划中无法对应原场景卡的数量为 0。
- 最后重跑新增 0、升级 0，Chapter 摘要仍一致；回执 `D:/infra/backups/ai-novel/arrangement-demo-v1-1789301338363/receipt.json`。各次备份均通过 SQLite 完整性检查。
- 客户端全量测试 251 项：245 通过，仍为前述 6 项既有失败，没有修改其断言。日志 `D:/cache/book-arrangement-foundation-client-tests.log`。
- 客户端生产构建及其中的 TypeScript 检查通过，日志 `D:/cache/book-arrangement-foundation-client-build.log`；保留原大包体积提示，未将提示当作构建失败。
- 对象候选与原修复稿面板最新定向测试 10/10，包含合法 0／false、只读字段、同名人物 ID、缺失引用移除、丢失响应后的回执恢复、旧组件默认不加载新草稿缓存。
- 原编排及新对象 SQLite 测试 30/30，`D:/cache/book-arrangement-object-full.log`；最后边界修正后的对象专项 10/10，`D:/cache/book-arrangement-object-final-focused.log`；原规划与持久化回归 38/38，`D:/cache/book-arrangement-object-planner-regression.log`。
- 最终合并回归 115/115，覆盖编排、数据准备、关系计划隔离、规划上下文、分层写作、原场景持久化及人工调整合同／服务／交接／路由／场景：`D:/cache/book-arrangement-foundation-server-regression.log`。
- 最终 shared 构建、server 构建和 client 类型检查均通过，日志分别为 `D:/cache/book-arrangement-foundation-shared-build.log`、`D:/cache/book-arrangement-foundation-server-build.log`、`D:/cache/book-arrangement-foundation-client-typecheck.log`。时间线模块对外统一由现有 `index.ts` 暴露新计划能力，调整导入后原场景复用专项再次通过。
- 文档清单检查通过：35 篇公共文档、17 个导演进度键。运行服务只读确认前端路由 HTTP 200，工作区返回 53 章、25 事件、75 有效场景、4 条计划关系和 19 条线索。
- 线索计划进入实际规划与四种正文模式；上下文回归 50/50，`D:/cache/planned-hooks-context-regression.log`，隔离 SQLite 专项 2/2，`D:/cache/planned-hooks-sqlite-tests.log`。其中一部分与最终合并回归重合，不将数量相加当作独立覆盖。

### 本批交互与兼容结果

底座侧栏保留“全书编排”入口与当前作品参数。页面使用原主题、原表单及右侧 `AppDialogContent`，彩色人物／卷轨道与连续章节导航保留；章节点击直接显示章设置。人物区段支持拖拉、复制、拆分和合并，参数支持曲线拖点与批量范围，卷候选支持选择待改卷和冲突定位。

事件、场景、关系、线索以原 ID 预览和应用；历史快照只读。核对从章节或原问题发起，复用原修复候选、手改、审核、采纳与同步重试。安全刷新保留本地编辑和原 CAS，另一窗口变更不能被“刷新”自动授权覆盖。结果不明确的对象采纳必须先恢复同一候选回执，不能变成第二次新增。

独立代码审查发现并修正了场景行／场景卡不同步、末场景重新生成、时间线约束遗漏、未来关系进入当前事实、采纳响应丢失后重复新增，以及删除场景无法恢复回执的问题；新线索的计划消费通道单独验证。

最终独立复审确认关系和线索目标通过可选合同进入原规划／写作链，无匹配计划时不添加新提示片段，不增加模型调用。应用底座实际地址为 `http://127.0.0.1:5173/book-arrangement?novelId=cmtvm8yxq005578y2lcp4fhs6`；本批没有新增独立原型页面、数据库表或迁移。

按当前项目规则，本批没有运行浏览器、截图或自动界面操作；前文旧阶段截图不能替代本批验收。用户可在左侧“全书编排”选择《净宅人翻旧账》，重点体验第 1—8、46—53 章：章节标题、彩色拖拉、右侧编辑与范围选择、预览应用、关闭重开和刷新恢复。真实模型文学效果与 PostgreSQL 实例执行没有在本批验证。

## 章内场景编排实施（2026-09-13）

本批把已有场景从单对象表单升级为章内工作区。用户可从全书矩阵的场景标签或“编排所选章节场景”进入；上方彩色篇幅条支持拖动相邻边界，中部卡片支持排序、拆分、合并、新增和删除，右侧编辑故事安排、节奏表达、人物与边界。编辑恢复按章节版本保存在本地，返回全书后继续使用原章节轴和对象弹窗。

实现继续复用原 `ChapterPlanScene` 和 `Chapter.sceneCards`。预览生成 `arrangement_scene` 编辑候选，不改正式资料；应用在一个事务中同步场景行、顺序、完整场景卡和章节目标字数，并保存幂等回执。锁章、人工接管、导演租约、待同步任务、章节版本或依赖版本变化都会阻止旧候选覆盖新资料。已有章节正文在预览和应用前后均保持不变。

> 🏠 白话比喻：先在排练表上调整每幕时长和出场顺序，导演确认后才换正式排练单；已经录好的成片不会被排练表覆盖。对应实现：章内场景草稿、候选和正式资料分层，应用只改后续写作依据。
> 🧠 速记：同一批场景一起预览，一次落库；计划可改，正文不动。

### 本次验证范围

- 前端场景状态测试 4/4 通过，覆盖相邻预算、锁定保护、稳定 ID 排序和连续序号。
- 客户端全书编排测试 31/31 通过；共享包构建、客户端类型检查和服务端类型检查通过。
- 服务端全书编排 SQLite 测试 32/32 通过，覆盖整章预览无写入、原子应用、正文不变、回执重放，以及既有卷段、对象、接管和同步保护回归。
- 按项目 UI 验证规则，本批未运行浏览器、截图或自动界面操作；视觉与真实鼠标交互由用户在本地运行页验收。

## 导航范围与操作层级补充（2026-09-13）

根据运行页四处标注，章节导航改为直接操作：拖动框体平移，拖左右边缘在 4—16 章间缩放，不增加需要理解和保存的范围参数。导航上方增加全书卷段缩略，卷块复用矩阵对应卷的颜色并可点击定位。顶部按钮只保留整书资料和人物关系两个全书入口，低频操作进入“更多”；人物关系面板用页签区分底座初始关系与当前所选章节的关系阶段，具体卷段和章节仍从矩阵轨道及章节名称进入。整书右侧面板可进入底座原故事主线、世界观和人物编辑，避免另存一份冲突资料。事件在单元格只显示紧凑标题，悬停／键盘聚焦再显示两行缩略信息，点击仍打开原右侧编辑。章内篇幅条和场景卡在底座窄工作区保持横向结构，空间不足时局部横向滚动，不把名称与任务挤成竖排。

> 🏠 白话比喻：取景框的边缘直接决定能看多宽，上方城区色带帮助快速跳转；总控台把全城、街区和门牌事务分开放。对应实现：浏览范围、卷定位和资料编辑层级各有一个入口。
> 🧠 速记：拖中间、拉两边、点卷跳转；整书定规则、卷段定承诺、章节定执行。

本批前端编排状态测试 33/33 通过，覆盖缩放边界、短书和不连续卷段；客户端 TypeScript 检查与生产构建通过。生产构建保留原有大分包警告，不影响产物生成。按项目 UI 验证规则，没有运行浏览器、截图或自动鼠标验收，视觉与真实拖动由用户在本地运行页确认。

事件与场景的“+N”随后按运行页反馈改为同色标签：点击在原章节格内展开全部对象，点击具体标题才进入该对象配置；不再借“+N”打开章节总面板。章节名称仍是章级设置入口。

人物关系入口随后升级为底座关系图谱。初始页读取正式人物与静态关系，点击人物后在图右侧直接修改六项常用基础资料，保存后同步更新全书编排中的人物名称与定位；章节页按章序读取动态关系快照与计划，关系线可继续进入原关系阶段对象配置。右侧面板扩大到最大 1080 像素，桌面端并排显示图谱与编辑，窄屏自动上下排列。

事件／场景章节格新增底座主题右键菜单。菜单就地提供章节设置、整章场景编排、新增场景、新增事件、新增关系阶段和新增伏笔；点击外部、滚动、缩放或按 Escape 关闭。新增场景会直接打开该章场景工作区并创建、选中新场景，其余新增对象继续走原候选预览与应用流程。

表达轨道从矩阵工具栏提升到顶部整书入口，与“整书资料”“人物关系”并列。右侧面板统一管理五种已有标准表达参数，可添加到矩阵、恢复基础轨道，并直接进入所选章节的参数配置；没有建立无法绑定现有写作合同的自定义参数。核对结果改为四维章节热力表，按 AI 味、逻辑性、人物一致性和节奏偏差显示最高未处理严重度与数量，点击格子仍进入原问题详情和修复流程。对象抽屉移除内容区及普通底栏的重复关闭，只保留抽屉统一关闭入口；完成采纳后的“完成并关闭”继续作为结果动作。

> 🏠 白话比喻：总控台选择要盯住的仪表，体检表用颜色指出哪一章哪项异常；需要处置时再点开原始报告。对应实现：标准表达轨道负责写作目标，核对热力表负责扫描，详情面板负责证据和修复。
> 🧠 速记：轨道管目标，热图找问题，详情看证据。

本批全书编排状态测试 35/35 通过，客户端类型检查与生产构建通过，文档清单检查通过。以 1101×898 的亮色和深色主题运行页检查 32 个热力格，四项维度文字完整、热图区本身无横向溢出；实测顶部表达轨道打开、跳转当前章参数、风险格打开原核对详情，以及对象常态仅保留一个关闭入口。客户端全量测试仍有既有 6 项导航／移动端合同失败，与本批文件无关，没有修改断言掩盖。
